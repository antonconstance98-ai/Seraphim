---
phase: 37-verification-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/verify.md
  - ~/.claude/plugins/seraphim/commands/uat.md
  - ~/.claude/plugins/seraphim/commands/validate.md
autonomous: true
requirements: [VFY-01, VFY-02, VFY-03, VFY-04]

must_haves:
  truths:
    - "User can run /seraphim:verify and receive a VERIFICATION.md with REQ-ID traceability and at least one REQUIRES_HUMAN_JUDGMENT item"
    - "User can run /seraphim:uat and have UAT.md accumulate test results across sessions with persistent YAML frontmatter state"
    - "User can run /seraphim:validate and receive a VALIDATION.md identifying coverage gaps via nyquist-auditor subagent"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/verify.md"
      provides: "Goal-backward verification command"
      contains: "REQUIRES_HUMAN_JUDGMENT"
    - path: "~/.claude/plugins/seraphim/commands/uat.md"
      provides: "Conversational UAT command with persistent state"
      contains: "UAT.md"
    - path: "~/.claude/plugins/seraphim/commands/validate.md"
      provides: "Nyquist gap auditing command"
      contains: "nyquist-auditor"
  key_links:
    - from: "verify.md"
      to: "PLAN.md frontmatter requirements: field"
      via: "reads requirements array from plan frontmatter"
      pattern: "requirements:"
    - from: "uat.md"
      to: ".planning/phases/{slug}/UAT.md"
      via: "atomic tmp+renameSync writes"
      pattern: "renameSync"
---

<objective>
Create the three core verification commands: verify (goal-backward traceability), uat (conversational persistent testing), and validate (Nyquist gap auditing).

Purpose: These commands close the loop between building features and proving they work, with traceability back to REQ-IDs.
Output: Three command files in ~/.claude/plugins/seraphim/commands/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-verification-dashboard/37-CONTEXT.md
@.planning/phases/37-verification-dashboard/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify.md and uat.md commands</name>
  <files>~/.claude/plugins/seraphim/commands/verify.md, ~/.claude/plugins/seraphim/commands/uat.md</files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/debug.md
    ~/.claude/plugins/seraphim/commands/forensics.md
    .planning/phases/36-human-tasks-debugging/36-VERIFICATION.md
    .planning/phases/37-verification-dashboard/37-RESEARCH.md
  </read_first>
  <action>
**verify.md** (per D-01, D-02):
- Frontmatter: `description: "Goal-backward verification against requirements"`, `argument-hint: "<phase-slug>"`, `allowed-tools: ["Read", "Bash", "Write"]`
- Step 1: Parse `$ARGUMENTS` for phase-slug. Resolve phase directory at `.planning/phases/{slug}/`
- Step 2: Read all `*-PLAN.md` files in the phase dir. Extract `requirements:` array and `must_haves:` block (truths, artifacts, key_links) from YAML frontmatter using the parseFrontmatter pattern (split on `---` delimiters, parse YAML between them)
- Step 3: Read REQUIREMENTS.md from `.planning/REQUIREMENTS.md`. For each REQ-ID found in plan frontmatter, extract the requirement description
- Step 4: Spawn a verifier subagent with these instructions:
  - "You are a verification agent. For each must-have truth, check if the codebase satisfies it by tracing actual code paths (not just file existence). For each artifact, verify the file exists AND contains the expected exports/patterns. For each key_link, trace the connection from source to target."
  - "You MUST include at least one item with status REQUIRES_HUMAN_JUDGMENT — identify the most subjective quality aspect that only a human can evaluate (UX quality, naming clarity, error message helpfulness, etc.)"
  - "Evidence must be traceable code path verification, not just string presence. For each artifact, trace from the call site through to the output." (per Pitfall 3)
- Step 5: Write VERIFICATION.md to `.planning/phases/{slug}/{num}-VERIFICATION.md` using the established format:
  ```
  ---
  phase: {slug}
  verified: {ISO timestamp}
  status: passed | failed | partial
  score: N/N must-haves verified
  ---
  ## Goal Achievement
  ### Observable Truths
  | # | Truth | Status | Evidence |
  ### Required Artifacts
  | Artifact | Expected | Status | Details |
  ### Key Link Verification
  | From | To | Via | Status | Details |
  ```
- Use atomic tmp+renameSync write pattern for the output file

**uat.md** (per D-02):
- Frontmatter: `description: "Conversational UAT with persistent state"`, `argument-hint: "<phase-slug>"`, `allowed-tools: ["Read", "Write", "Bash"]`
- Step 1: Parse `$ARGUMENTS` for phase-slug. Resolve phase dir
- Step 2: Check for existing UAT.md at `.planning/phases/{slug}/UAT.md`
  - If exists: read it, parse YAML frontmatter (status, total, tested, passed, failed), find first item with `status: pending`
  - If not exists: read VERIFICATION.md from the phase dir, extract all must-have truths and artifacts as UAT items. Generate UAT.md with all items set to `status: pending`. Use this format:
    ```
    ---
    phase: {slug}
    status: in_progress
    last_updated: {ISO timestamp}
    total: N
    tested: 0
    passed: 0
    failed: 0
    ---
    ## UAT Items
    ### [UAT-01] {feature description from truth}
    **status:** pending
    **tested:**
    **tester:** human
    **notes:**
    ```
- Step 3: Present the next pending UAT item to the user. Show: ID, description, what to test, expected behavior
- Step 4: Wait for user response (passed/failed + notes). Update the item's status, tested timestamp, and notes
- Step 5: Update YAML frontmatter counts (tested, passed, failed). Update overall status: `complete` if all tested, `has_failures` if any failed
- Step 6: Write UAT.md using atomic tmp+renameSync pattern
- Step 7: Show summary: "UAT-{NN}: {status}. Progress: {tested}/{total} ({passed} passed, {failed} failed)"
  </action>
  <verify>
    <automated>test -f ~/.claude/plugins/seraphim/commands/verify.md && test -f ~/.claude/plugins/seraphim/commands/uat.md && grep -q "REQUIRES_HUMAN_JUDGMENT" ~/.claude/plugins/seraphim/commands/verify.md && grep -q "UAT.md" ~/.claude/plugins/seraphim/commands/uat.md && grep -q "renameSync" ~/.claude/plugins/seraphim/commands/uat.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - verify.md exists with frontmatter containing `allowed-tools: ["Read", "Bash", "Write"]`
    - verify.md contains "REQUIRES_HUMAN_JUDGMENT" string
    - verify.md reads PLAN.md frontmatter `requirements:` field
    - verify.md writes VERIFICATION.md with the established format (Observable Truths table, Required Artifacts table, Key Link Verification table)
    - uat.md exists with frontmatter containing `allowed-tools: ["Read", "Write", "Bash"]`
    - uat.md reads/creates UAT.md with YAML frontmatter tracking status/total/tested/passed/failed
    - uat.md uses atomic tmp+renameSync write pattern
    - uat.md finds first pending item and presents it to user
  </acceptance_criteria>
  <done>verify.md writes VERIFICATION.md with REQ-ID traceability and mandatory REQUIRES_HUMAN_JUDGMENT item. uat.md creates/updates UAT.md with persistent state across sessions using atomic writes.</done>
</task>

<task type="auto">
  <name>Task 2: Create validate.md command</name>
  <files>~/.claude/plugins/seraphim/commands/validate.md</files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/verify.md
    .planning/phases/37-verification-dashboard/37-RESEARCH.md
  </read_first>
  <action>
**validate.md** (per D-03):
- Frontmatter: `description: "Nyquist gap auditing — identifies verification coverage gaps"`, `argument-hint: "<phase-slug>"`, `allowed-tools: ["Read", "Write", "Bash"]`
- Step 1: Parse `$ARGUMENTS` for phase-slug. Resolve phase dir
- Step 2: Read VERIFICATION.md from `.planning/phases/{slug}/`. If not found, tell user to run `/seraphim:verify` first and stop
- Step 3: Spawn a nyquist-auditor subagent with these instructions:
  - "You are a Nyquist auditor. Read the VERIFICATION.md and identify coverage gaps — areas where verification is insufficient, edge cases not tested, integration points not validated, or truths only partially verified."
  - "For each gap found, generate an additional test scenario that would close the gap. Include: gap description, what to test, expected outcome, severity (critical/important/minor)."
  - "Check: Are there any must-have truths marked as VERIFIED that have weak evidence? Are there key_links that were checked for existence but not for correct behavior? Are there artifacts that exist but may not function correctly?"
- Step 4: Write VALIDATION.md to `.planning/phases/{slug}/VALIDATION.md` with format:
  ```
  ---
  phase: {slug}
  validated: {ISO timestamp}
  gaps_found: N
  critical: N
  important: N
  minor: N
  ---
  ## Coverage Gaps
  ### [GAP-01] {description}
  **severity:** critical | important | minor
  **what_to_test:** {test scenario}
  **expected_outcome:** {expected result}
  **source_truth:** {which must-have truth this relates to}
  ```
- Use atomic tmp+renameSync write pattern
  </action>
  <verify>
    <automated>test -f ~/.claude/plugins/seraphim/commands/validate.md && grep -q "nyquist-auditor" ~/.claude/plugins/seraphim/commands/validate.md && grep -q "VALIDATION.md" ~/.claude/plugins/seraphim/commands/validate.md && grep -q "renameSync" ~/.claude/plugins/seraphim/commands/validate.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - validate.md exists with frontmatter containing `allowed-tools: ["Read", "Write", "Bash"]`
    - validate.md contains "nyquist-auditor" subagent dispatch
    - validate.md reads VERIFICATION.md as input
    - validate.md writes VALIDATION.md with gap severity levels (critical/important/minor)
    - validate.md uses atomic tmp+renameSync write pattern
    - validate.md stops with helpful message if VERIFICATION.md not found
  </acceptance_criteria>
  <done>validate.md spawns nyquist-auditor subagent that reads VERIFICATION.md, identifies coverage gaps, and writes VALIDATION.md with severity-classified gap items.</done>
</task>

</tasks>

<verification>
- All three command files exist in ~/.claude/plugins/seraphim/commands/
- verify.md includes REQUIRES_HUMAN_JUDGMENT mandate in subagent prompt
- uat.md uses persistent UAT.md with incremental accumulation
- validate.md reads VERIFICATION.md and writes VALIDATION.md
- All commands use atomic tmp+renameSync write pattern
- All commands follow the established .md frontmatter format
</verification>

<success_criteria>
- verify.md produces VERIFICATION.md with REQ-ID traceability from PLAN.md frontmatter
- uat.md creates/resumes UAT.md sessions with pending/passed/failed tracking
- validate.md spawns nyquist-auditor and writes VALIDATION.md with coverage gaps
</success_criteria>

<output>
After completion, create `.planning/phases/37-verification-dashboard/37-01-SUMMARY.md`
</output>
