---
phase: 32-foundations
plan: 03
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - .planning/phases/32-foundations/SCHEMA-AUDIT.md
autonomous: true
requirements: [FOUND-04]

must_haves:
  truths:
    - "Every v3.2 data concept is mapped to an existing structure or justified new file"
    - "The audit confirms extend-not-duplicate for all 7 concepts"
  artifacts:
    - path: ".planning/phases/32-foundations/SCHEMA-AUDIT.md"
      provides: "Schema extension audit mapping v3.2 concepts to existing structures"
      contains: "extend"
  key_links: []
---

<objective>
Write the schema extension audit document confirming every v3.2 data concept maps to an existing structure.

Purpose: FOUND-04 requires a schema extension audit proving all v3.2 data concepts extend existing structures (no parallel files). This is a documentation artifact that gates v3.2 feature work.
Output: SCHEMA-AUDIT.md with mapping table and justifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-foundations/32-CONTEXT.md
@.planning/phases/32-foundations/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCHEMA-AUDIT.md</name>
  <files>.planning/phases/32-foundations/SCHEMA-AUDIT.md</files>
  <read_first>
    - .planning/phases/32-foundations/32-RESEARCH.md (Schema Extension Audit section with the 7 concepts table)
    - .planning/phases/32-foundations/32-CONTEXT.md (D-09 and D-10 requirements for audit format)
    - .planning/REQUIREMENTS.md (all v3.2 requirement categories to ensure complete coverage)
    - .seraphim/roadmap.json (current milestone-feature structure to reference)
  </read_first>
  <action>
    Per D-09 and D-10, create `.planning/phases/32-foundations/SCHEMA-AUDIT.md` with:

    **Header:** "# Schema Extension Audit — v3.2 Idea-to-Shipped Journey" with date and purpose statement.

    **Principle section:** State the "extend, not duplicate" rule from D-10. Any new top-level data store must justify why the existing store cannot be extended.

    **Mapping table** with columns: v3.2 Concept | Existing Home | Extension Approach | New Store? | Justification

    The 7 concepts to map (from research):

    1. **Seeds** — `.planning/seeds/` directory (exists) — Add `index.jsonl` as lookup index, each seed is SEED-NNN.md — No new store — directory already exists from prior workflow
    2. **Requirements** — `.seraphim/roadmap.json` features array — Add `requirements[]` array with `req_id` and `scope` fields to feature objects — No new store — extends existing roadmap structure
    3. **Waves** — `.seraphim/roadmap.json` milestones array — Add `waves[]` array to milestone/feature objects for wave-structured planning — No new store — extends existing roadmap structure
    4. **Discuss decisions** — `.planning/phases/NN-slug/NN-CONTEXT.md` — Already exists, decisions captured in CONTEXT.md per current GSD workflow — No new file — existing pattern
    5. **Research items** — `.seraphim/research.json` (new file) — Follows roadmap.js atomic write pattern (readResearch/writeResearch) — Yes, new file — justified because research items are cross-feature with independent lifecycle; cannot scope to a single feature's roadmap entry
    6. **Human task enrichment** — Existing HumanTaskSnapshot type — Add optional `skills_to_learn: string`, `thought_prompt: string`, `research_task: string` fields — No new store — extends existing type
    7. **Progress/velocity** — `.seraphim/timeline.jsonl` + `task-completions.jsonl` — Computed at read time from existing append-only logs — No new store — computed, no persistence needed

    **Summary section:** "6 of 7 concepts extend existing structures. research.json is the only new file, justified by cross-feature scope. This satisfies D-10."

    **Verification checklist:** For each concept, state whether the target file/structure exists today (yes/no) and what extension is needed.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-foundations/SCHEMA-AUDIT.md && grep -c "extend" .planning/phases/32-foundations/SCHEMA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/32-foundations/SCHEMA-AUDIT.md` exists
    - File contains all 7 concept names: "Seeds", "Requirements", "Waves", "Discuss", "Research", "Human task", "Progress"
    - File contains "extend, not duplicate" or equivalent principle statement
    - File contains "research.json" as the only justified new file
    - File contains "6 of 7" or equivalent summary count
  </acceptance_criteria>
  <done>SCHEMA-AUDIT.md maps all 7 v3.2 data concepts to existing structures with justifications. Only research.json is a new file, justified by cross-feature scope.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/32-foundations/SCHEMA-AUDIT.md` passes
- `grep -c "Seeds\|Requirements\|Waves\|Discuss\|Research\|Human task\|Progress" .planning/phases/32-foundations/SCHEMA-AUDIT.md` returns 7+
</verification>

<success_criteria>
1. SCHEMA-AUDIT.md exists at the path specified in D-09
2. All 7 v3.2 data concepts are mapped with extension approach and justification
3. The audit confirms extend-not-duplicate per D-10
</success_criteria>

<output>
After completion, create `.planning/phases/32-foundations/32-03-SUMMARY.md`
</output>
