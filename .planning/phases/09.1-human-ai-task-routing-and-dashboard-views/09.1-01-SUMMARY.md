---
phase: 09.1-human-ai-task-routing-and-dashboard-views
plan: "01"
subsystem: markers-and-architect
tags: [cognitive-division, task-routing, markers, architect]
requirements: [PIPE-04]

dependency_graph:
  requires: []
  provides: [emitTask helper, assignee attribute on TASK markers]
  affects: [forge.md, dashboard task lists]

tech_stack:
  added: []
  patterns: [emitMarker wrapper, HTML comment attribute round-trip]

key_files:
  created: []
  modified:
    - ~/.claude/plugins/seraphim/lib/markers.js
    - ~/.claude/plugins/seraphim/commands/architect.md

decisions:
  - "assignee defaults to 'ai' — human is the exception, not the default"
  - "warn-only (not abort) on missing assignee in validation — backwards compatibility"
  - "four human-assignment triggers: creative ideation, domain expertise, physical actions, subjective quality sign-off"

metrics:
  duration: "~5 min"
  completed: "2026-04-09"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 2
---

# Phase 09.1 Plan 01: Assignee Attribute for TASK Markers Summary

**One-liner:** Added `emitTask` helper to markers.js and updated architect.md to tag every SERAPHIM:TASK with `assignee="human|ai"` — the foundation for cognitive division routing in Forge and the dashboard.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add emitTask helper to markers.js | 5ab6ae9 | `lib/markers.js` |
| 2 | Update architect.md with assignee tagging instructions | 957c317 | `commands/architect.md` |

## What Was Built

### markers.js — emitTask helper

Added `emitTask(id, type, assignee)` function that wraps `emitMarker('TASK', ...)` with:
- `id` and `type` coerced to strings
- `assignee` defaulting to `'ai'` when omitted
- Exported alongside all existing marker helpers

The `parseMarkers` function already captures arbitrary `key="val"` pairs generically — no parser changes were needed. The round-trip test (emit → parse → check `.assignee`) passes cleanly.

### architect.md — cognitive division rules

Five targeted edits made to architect.md:

1. **Inline Opus path (Step 8):** Added rule 5 listing the four human-assignment triggers and defaulting everything else to `ai`.
2. **Step 9 output format:** Updated TASK marker examples — task-1 shows `assignee="ai"`, task-2 shows `assignee="human"` — so both cases appear in the template.
3. **External dispatch prompt (Step 8):** Added instruction 6 to the INSTRUCTIONS block covering the same cognitive division rules for non-Opus models.
4. **Step 10 validation:** Added `untaggedTasks` warning block — warns to console but does not abort, preserving backwards compatibility with any existing blueprints lacking the attribute.

Total `assignee` occurrences in architect.md: **9** (minimum required: 5).

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. Both files are fully wired. The `assignee` attribute will flow through to Forge and the dashboard in subsequent plans (09.1-02 and 09.1-03).

## Self-Check: PASSED

- `/home/alucardmessangeroflight/.claude/plugins/seraphim/lib/markers.js` — modified, emitTask exported
- `/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/architect.md` — modified, 9 assignee occurrences
- Commit 5ab6ae9 exists in plugin repo
- Commit 957c317 exists in plugin repo
- Node smoke test: PASS
- grep -c 'assignee' architect.md: 9
