---
phase: 33-core-command-layer
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - ~/.claude/plugins/seraphim/commands/requirements.md
  - ~/.claude/plugins/seraphim/commands/discuss.md
  - ~/.claude/plugins/seraphim/commands/assumptions.md
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, PLAN-04, PLAN-05]

must_haves:
  truths:
    - "User can define requirements with REQ-IDs via /seraphim:requirements with AI suggestion and human approval"
    - "User can lock implementation decisions via /seraphim:discuss producing CONTEXT.md"
    - "User can surface Claude's assumptions via /seraphim:assumptions"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/requirements.md"
      provides: "Requirements definition command"
      contains: "seraphim:requirements"
    - path: "~/.claude/plugins/seraphim/commands/discuss.md"
      provides: "Implementation decision locking"
      contains: "seraphim:discuss"
    - path: "~/.claude/plugins/seraphim/commands/assumptions.md"
      provides: "Assumption surfacing"
      contains: "seraphim:assumptions"
  key_links:
    - from: "commands/requirements.md"
      to: "lib/requirements.js"
      via: "node -e require for addRequirement"
      pattern: "requirements"
    - from: "commands/discuss.md"
      to: "CONTEXT.md output"
      via: "Write tool creates CONTEXT.md"
      pattern: "<domain>.*<decisions>.*<code_context>"
---

<objective>
Create the requirements definition, discussion, and assumptions commands.

Purpose: These commands bridge idea capture to planning — defining what must be built, locking implementation decisions, and surfacing hidden assumptions.
Output: 3 command files in ~/.claude/plugins/seraphim/commands/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-core-command-layer/33-CONTEXT.md
@.planning/phases/33-core-command-layer/33-RESEARCH.md
@.planning/phases/33-core-command-layer/33-01-SUMMARY.md

<interfaces>
From ~/.claude/plugins/seraphim/lib/requirements.js (created in Plan 01):
```javascript
module.exports = {
  readRequirements,   // (projectRoot) => { categories: {}, reqs: {} }
  writeRequirements,  // (projectRoot, data) => void
  nextReqId,          // (requirements) => 'REQ-NNN'
  addRequirement,     // (projectRoot, { description, category, scope }) => { id, ... }
  updateRequirement   // (projectRoot, reqId, updates) => void
};
```

GSD CONTEXT.md format (from discuss-phase.md, verified in 33-RESEARCH.md Pattern 5):
```markdown
<domain>## Phase Boundary\n[scope]</domain>
<decisions>## Implementation Decisions\n### [Category]\n- **D-01:** Decision</decisions>
<code_context>## Existing Code Insights\n### Reusable Assets</code_context>
<specifics>## Specific Ideas</specifics>
<deferred>## Deferred Ideas</deferred>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements.md command</name>
  <files>~/.claude/plugins/seraphim/commands/requirements.md</files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/new-milestone.md
    ~/.claude/plugins/seraphim/lib/requirements.js
    /home/alucardmessangeroflight/projects/seraphim/.planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `~/.claude/plugins/seraphim/commands/requirements.md` following established command pattern (per D-01, D-04).

    **`/seraphim:requirements`** — per REQ-01, REQ-02, REQ-03:
    - Frontmatter: description "Define requirements with AI-suggested REQ-IDs", argument-hint "[feature-ref] [--matrix]", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root (bash walk-up)
    - Step 2: Read existing requirements via `node -e "const r = require('${PLUGIN_ROOT}/lib/requirements'); console.log(JSON.stringify(r.readRequirements('${PROJECT_ROOT}')));"`
    - Step 3: If --matrix flag: display traceability matrix (REQ-IDs mapped to phases, features, verification status per REQ-03) and exit
    - Step 4: Read context — look for CONTEXT.md, RESEARCH.md, seed files, roadmap.json to understand the feature scope
    - Step 5: AI suggests requirements grouped by category with scoping:
      - **v1** (must-have for current milestone)
      - **future** (good idea but not now)
      - **out-of-scope** (explicitly excluded)
      Per REQ-02, display grouped by category with scope labels
    - Step 6: Present suggestions to user for approval — user can approve all, modify, add, remove, re-scope
    - Step 7: Write approved requirements via requirements.js addRequirement for each
    - Step 8: Display summary: N requirements added across M categories

    The command is conversational — AI suggests, human approves (per REQ-01). It must NOT auto-commit requirements without user review.
  </action>
  <verify>
    <automated>bash -c "test -f ~/.claude/plugins/seraphim/commands/requirements.md && grep -q 'requirements' ~/.claude/plugins/seraphim/commands/requirements.md && grep -q 'description:' ~/.claude/plugins/seraphim/commands/requirements.md && grep -q 'matrix' ~/.claude/plugins/seraphim/commands/requirements.md && echo 'requirements.md OK' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/plugins/seraphim/commands/requirements.md
    - Contains YAML frontmatter with description, argument-hint, allowed-tools
    - References lib/requirements.js via node -e require pattern
    - Contains --matrix flag handling for traceability display (REQ-03)
    - Contains grouped category display with v1/future/out-of-scope scoping (REQ-02)
    - Contains user approval step before writing (REQ-01 "human approves")
    - Uses project root walk-up pattern
  </acceptance_criteria>
  <done>requirements.md exists, supports AI suggestion + human approval flow, --matrix flag for traceability, category grouping with scope</done>
</task>

<task type="auto">
  <name>Task 2: Create discuss.md and assumptions.md commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/discuss.md
    ~/.claude/plugins/seraphim/commands/assumptions.md
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/new-milestone.md
    /home/alucardmessangeroflight/.claude/get-shit-done/workflows/discuss-phase.md
    /home/alucardmessangeroflight/projects/seraphim/.planning/phases/33-core-command-layer/33-CONTEXT.md
  </read_first>
  <action>
    **discuss.md** (`/seraphim:discuss`) — per PLAN-04, D-08:
    - Frontmatter: description "Lock implementation decisions before planning", argument-hint "[phase-ref]", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root
    - Step 2: Identify the target phase from $ARGUMENTS or current phase in roadmap
    - Step 3: Read available context — roadmap.json, requirements.json, RESEARCH.md if exists, codebase patterns
    - Step 4: Conduct interactive discussion with user about implementation approach:
      - Surface decisions that need to be made (technology choices, data formats, integration patterns)
      - For each decision: present options with tradeoffs, ask user to lock a choice
      - Number decisions as D-01, D-02, etc.
    - Step 5: Write CONTEXT.md to `.planning/phases/{phase-dir}/` with EXACT GSD format (per D-08):
      - `<domain>` — phase boundary/scope
      - `<decisions>` — locked decisions with D-NN numbering
      - `<code_context>` — reusable assets and established patterns
      - `<specifics>` — user's specific preferences
      - `<deferred>` — ideas explicitly deferred
    - Step 6: Confirm: "CONTEXT.md written with N decisions locked"

    CRITICAL: The output format MUST match GSD discuss-phase exactly (per D-08, Pitfall 4). The exact XML tags are: `<domain>`, `<decisions>`, `<code_context>`, `<specifics>`, `<deferred>`.

    **assumptions.md** (`/seraphim:assumptions`) — per PLAN-05:
    - Frontmatter: description "Surface Claude's assumptions about a phase", argument-hint "[phase-ref]", allowed-tools ["Read", "Bash"]
    - Step 1: Resolve project root
    - Step 2: Read CONTEXT.md if exists, RESEARCH.md, roadmap.json, requirements.json for the target phase
    - Step 3: Enumerate implicit assumptions Claude is making:
      - Technical assumptions (e.g., "assuming Node.js stdlib is sufficient")
      - Scope assumptions (e.g., "assuming single-user, no concurrency")
      - Dependency assumptions (e.g., "assuming Phase 32 DDL is applied")
      - Data assumptions (e.g., "assuming .seraphim/ directory pattern")
    - Step 4: Present numbered list of assumptions for user to confirm, challenge, or add to
    - Step 5: Optionally update CONTEXT.md with clarified assumptions as new decisions
    - This is read-only analysis — no file writes required unless user wants to lock assumptions as decisions
  </action>
  <verify>
    <automated>bash -c "test -f ~/.claude/plugins/seraphim/commands/discuss.md && grep -q '<domain>' ~/.claude/plugins/seraphim/commands/discuss.md && test -f ~/.claude/plugins/seraphim/commands/assumptions.md && grep -q 'assumptions' ~/.claude/plugins/seraphim/commands/assumptions.md && echo 'discuss+assumptions OK' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - discuss.md exists with YAML frontmatter
    - discuss.md contains all 5 GSD CONTEXT.md section tags: `<domain>`, `<decisions>`, `<code_context>`, `<specifics>`, `<deferred>`
    - discuss.md produces CONTEXT.md output (grep for 'CONTEXT.md')
    - discuss.md numbers decisions as D-01, D-02 etc.
    - assumptions.md exists with YAML frontmatter
    - assumptions.md reads CONTEXT.md and RESEARCH.md for context
    - assumptions.md enumerates assumptions in numbered list format
    - Both use project root walk-up pattern
  </acceptance_criteria>
  <done>discuss.md creates GSD-compatible CONTEXT.md with locked decisions, assumptions.md surfaces implicit assumptions for user review</done>
</task>

</tasks>

<verification>
All 3 command files exist with valid YAML frontmatter. discuss.md output matches GSD CONTEXT.md format exactly.
</verification>

<success_criteria>
- requirements.md supports AI suggest + human approve flow with category grouping and scope labels
- requirements.md has --matrix flag for traceability
- discuss.md produces CONTEXT.md with exact GSD XML section format
- assumptions.md surfaces numbered assumptions for review
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-command-layer/33-03-SUMMARY.md`
</output>
