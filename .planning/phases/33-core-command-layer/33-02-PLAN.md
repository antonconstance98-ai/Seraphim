---
phase: 33-core-command-layer
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - ~/.claude/plugins/seraphim/commands/seed.md
  - ~/.claude/plugins/seraphim/commands/note.md
  - ~/.claude/plugins/seraphim/commands/add-todo.md
  - ~/.claude/plugins/seraphim/commands/check-todos.md
  - ~/.claude/plugins/seraphim/commands/promote.md
autonomous: true
requirements: [SEED-01, SEED-03, SEED-05, SEED-06, SEED-07]

must_haves:
  truths:
    - "User can capture a raw idea via /seraphim:seed with freeform input"
    - "User can capture zero-friction notes via /seraphim:note"
    - "User can add structured todos via /seraphim:add-todo with area tagging"
    - "User can list and select pending todos via /seraphim:check-todos"
    - "User can promote a seed to a feature via /seraphim:promote"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/seed.md"
      provides: "Seed capture command"
      contains: "seraphim:seed"
    - path: "~/.claude/plugins/seraphim/commands/note.md"
      provides: "Zero-friction note capture"
      contains: "seraphim:note"
    - path: "~/.claude/plugins/seraphim/commands/add-todo.md"
      provides: "Structured todo addition"
      contains: "seraphim:add-todo"
    - path: "~/.claude/plugins/seraphim/commands/check-todos.md"
      provides: "Todo listing and selection"
      contains: "seraphim:check-todos"
    - path: "~/.claude/plugins/seraphim/commands/promote.md"
      provides: "Seed to feature promotion"
      contains: "seraphim:promote"
  key_links:
    - from: "commands/seed.md"
      to: "lib/seed-store.js"
      via: "node -e require for writeSeed"
      pattern: "seed-store"
    - from: "commands/promote.md"
      to: "lib/roadmap.js"
      via: "node -e require for addFeature"
      pattern: "roadmap"
---

<objective>
Create the five seed/note/todo commands that let users capture ideas and manage lightweight task tracking.

Purpose: These commands are the "idea capture" funnel — the very first step of the idea-to-shipped journey.
Output: 5 command files in ~/.claude/plugins/seraphim/commands/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-core-command-layer/33-CONTEXT.md
@.planning/phases/33-core-command-layer/33-RESEARCH.md
@.planning/phases/33-core-command-layer/33-01-SUMMARY.md

<interfaces>
From ~/.claude/plugins/seraphim/lib/seed-store.js (created in Plan 01):
```javascript
module.exports = {
  nextSeedId,        // (seedsDir) => 'SEED-NNN'
  parseFrontmatter,  // (content) => { frontmatter, body }
  writeSeed,         // (seedsDir, { title, tags, trigger_conditions, body }) => { id, filename }
  readSeed,          // (seedPath) => { frontmatter, body }
  readIndex,         // (indexPath) => [records]
  appendToIndex,     // (indexPath, record) => void
  surfaceMatchingSeeds // (seedsDir, milestoneContext) => [matching records]
};
```

From ~/.claude/plugins/seraphim/lib/roadmap.js:
```javascript
module.exports = { readRoadmap, writeRoadmap, findFeature };
```

From ~/.claude/plugins/seraphim/lib/requirements.js (created in Plan 01):
```javascript
module.exports = { readRequirements, writeRequirements, nextReqId, addRequirement, updateRequirement };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed.md, note.md, and promote.md commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/seed.md
    ~/.claude/plugins/seraphim/commands/note.md
    ~/.claude/plugins/seraphim/commands/promote.md
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/new-milestone.md
    ~/.claude/plugins/seraphim/commands/start.md
    ~/.claude/plugins/seraphim/lib/seed-store.js
    ~/.claude/plugins/seraphim/lib/roadmap.js
  </read_first>
  <action>
    Create three command files following the exact .md frontmatter + prose pattern from new-milestone.md (per D-01, D-04).

    **seed.md** (`/seraphim:seed`) — per SEED-01, D-05:
    - Frontmatter: description "Capture a raw idea as a seed", argument-hint "[title] [--tags tag1,tag2]", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root (bash walk-up pattern, per D-03)
    - Step 2: Parse arguments — extract title from positional args, --tags from flag
    - Step 3: Ask user for braindump-style freeform input if not provided inline
    - Step 4: Call seed-store.js writeSeed via `node -e` with PLUGIN_ROOT pattern: `node -e "const ss = require('${PLUGIN_ROOT}/lib/seed-store'); const result = ss.writeSeed('${PROJECT_ROOT}/.planning/seeds', { title: ..., tags: [...], trigger_conditions: [], body: '...' }); console.log(JSON.stringify(result));"`
    - Step 5: Confirm to user with seed ID and file path

    **note.md** (`/seraphim:note`) — per SEED-05:
    - Frontmatter: description "Capture a zero-friction note (one write, no questions)", argument-hint "<note text>", allowed-tools ["Write", "Bash"]
    - Step 1: Resolve project root
    - Step 2: Take entire $ARGUMENTS as note body — NO interaction, NO questions (zero-friction per SEED-05)
    - Step 3: Create `.planning/notes/` if missing, compute next NOTE-NNN.md by scanning directory
    - Step 4: Write NOTE-NNN.md with minimal frontmatter (id, created date) + body
    - Step 5: Confirm with one line: "Noted: NOTE-NNN"

    **promote.md** (`/seraphim:promote`) — per SEED-03:
    - Frontmatter: description "Promote a seed to a feature with requirements", argument-hint "<seed-id>", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root
    - Step 2: Read the specified SEED-NNN.md via seed-store.js readSeed
    - Step 3: Analyze seed content and suggest requirements (REQ-IDs) grouped by category
    - Step 4: Ask user to approve/modify the suggested requirements
    - Step 5: Write approved requirements via requirements.js addRequirement
    - Step 6: Add feature to roadmap.json via roadmap.js (slug derived from seed title)
    - Step 7: Update seed status from 'dormant' to 'promoted' in the SEED file
    - Step 8: Confirm promotion with feature ID and REQ-IDs created
  </action>
  <verify>
    <automated>bash -c "for cmd in seed note promote; do test -f ~/.claude/plugins/seraphim/commands/\$cmd.md && grep -q 'description:' ~/.claude/plugins/seraphim/commands/\$cmd.md && echo \"\$cmd.md OK\" || echo \"\$cmd.md MISSING\"; done"</automated>
  </verify>
  <acceptance_criteria>
    - seed.md exists with frontmatter containing `description:` and `allowed-tools:`
    - seed.md contains "seed-store" reference for lib integration
    - seed.md contains project root walk-up bash pattern
    - note.md exists and does NOT contain any user prompt/question (zero-friction)
    - note.md writes to `.planning/notes/NOTE-NNN.md`
    - promote.md exists and references both seed-store.js and requirements.js
    - promote.md references roadmap.js for feature creation
    - All three files use YAML frontmatter with description, argument-hint, allowed-tools
  </acceptance_criteria>
  <done>Three command files exist, each follows established .md command pattern, seed.md calls seed-store.js, note.md is zero-friction, promote.md bridges seeds to features+requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create add-todo.md and check-todos.md commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/add-todo.md
    ~/.claude/plugins/seraphim/commands/check-todos.md
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/new-milestone.md
    ~/.claude/plugins/seraphim/commands/seed.md
  </read_first>
  <action>
    Create two todo management commands following the established .md command pattern (per D-01, D-04).

    **add-todo.md** (`/seraphim:add-todo`) — per SEED-06:
    - Frontmatter: description "Add a structured todo with area tagging", argument-hint "<description> [--area <area>]", allowed-tools ["Write", "Bash"]
    - Step 1: Resolve project root (bash walk-up pattern)
    - Step 2: Parse $ARGUMENTS — description is positional text, --area is optional tag (default: "general")
    - Step 3: Append JSONL record to `.planning/todos.jsonl`: `{ "id": "TODO-NNN", "description": "...", "area": "...", "status": "pending", "created": "YYYY-MM-DD" }`
    - Compute TODO-NNN by reading existing todos.jsonl, finding max ID, incrementing
    - Step 4: Confirm: "Added: TODO-NNN [area] — description"

    The JSONL append uses the same pattern as index.jsonl in seed-store.js:
    ```bash
    node -e "
      const fs = require('fs');
      const todosPath = '${PROJECT_ROOT}/.planning/todos.jsonl';
      // read existing, find max, append new
    "
    ```

    **check-todos.md** (`/seraphim:check-todos`) — per SEED-07:
    - Frontmatter: description "List and manage pending todos", argument-hint "[--area <area>] [--done <todo-id>]", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root
    - Step 2: Read `.planning/todos.jsonl` via node -e JSONL reader
    - Step 3: If --done TODO-NNN provided: mark that todo as status: "done", rewrite todos.jsonl
    - Step 4: If --area provided: filter todos by area
    - Step 5: Display pending todos grouped by area in a readable format
    - Step 6: Ask user if they want to mark any as done or take action on one
  </action>
  <verify>
    <automated>bash -c "for cmd in add-todo check-todos; do test -f ~/.claude/plugins/seraphim/commands/\$cmd.md && grep -q 'description:' ~/.claude/plugins/seraphim/commands/\$cmd.md && echo \"\$cmd.md OK\" || echo \"\$cmd.md MISSING\"; done"</automated>
  </verify>
  <acceptance_criteria>
    - add-todo.md exists with frontmatter containing `description:` and `allowed-tools:`
    - add-todo.md writes to `.planning/todos.jsonl` in JSONL format
    - add-todo.md supports --area flag for tagging
    - check-todos.md exists with frontmatter
    - check-todos.md reads `.planning/todos.jsonl`
    - check-todos.md supports --done flag for marking complete
    - check-todos.md groups display by area
    - Both use project root walk-up pattern
  </acceptance_criteria>
  <done>Both todo command files exist, add-todo appends to JSONL, check-todos reads/filters/completes todos</done>
</task>

</tasks>

<verification>
All 5 command files exist in ~/.claude/plugins/seraphim/commands/ with valid YAML frontmatter. Each references appropriate lib files via node -e require pattern.
</verification>

<success_criteria>
- 5 new .md command files created: seed.md, note.md, add-todo.md, check-todos.md, promote.md
- All follow established command pattern (YAML frontmatter + prose steps)
- All use project root walk-up pattern for discovery
- seed.md and promote.md call seed-store.js
- promote.md calls requirements.js and roadmap.js
- note.md is zero-friction (no questions asked)
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-command-layer/33-02-SUMMARY.md`
</output>
