---
phase: 33-core-command-layer
plan: 04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - ~/.claude/plugins/seraphim/commands/plan.md
autonomous: true
requirements: [PLAN-03, PLAN-06, EXEC-06]

must_haves:
  truths:
    - "User can generate a wave-structured PLAN.md via /seraphim:plan with tasks grouped by dependency wave"
    - "Plan verification loop runs planner + checker with max 3 iterations"
    - "Dependencies are resolved via Kahn's algorithm before tasks appear in a wave"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/plan.md"
      provides: "Wave-structured plan generation command"
      contains: "seraphim:plan"
  key_links:
    - from: "commands/plan.md"
      to: "lib/wave-planner.js"
      via: "node -e require for resolveWaves"
      pattern: "wave-planner"
    - from: "commands/plan.md"
      to: "lib/requirements.js"
      via: "node -e require for reading requirements"
      pattern: "requirements"
---

<objective>
Create the plan command that generates wave-structured PLAN.md files with dependency resolution via Kahn's algorithm and optional checker verification loop.

Purpose: This is the planning engine — it turns requirements and context into executable wave-structured plans.
Output: 1 command file in ~/.claude/plugins/seraphim/commands/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-core-command-layer/33-CONTEXT.md
@.planning/phases/33-core-command-layer/33-RESEARCH.md
@.planning/phases/33-core-command-layer/33-01-SUMMARY.md

<interfaces>
From ~/.claude/plugins/seraphim/lib/wave-planner.js (created in Plan 01):
```javascript
module.exports = {
  resolveWaves,        // (tasks) => [[wave0_ids], [wave1_ids], ...]
  readWaves,           // (projectRoot, featureId) => waves array
  writeWaves,          // (projectRoot, featureId, waves) => void
  validateDependencies // (tasks) => { valid, errors }
};
```

From ~/.claude/plugins/seraphim/lib/requirements.js (created in Plan 01):
```javascript
module.exports = {
  readRequirements,   // (projectRoot) => { categories, reqs }
  writeRequirements, writeRequirements, nextReqId, addRequirement, updateRequirement
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plan.md command with wave resolution and checker loop</name>
  <files>~/.claude/plugins/seraphim/commands/plan.md</files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/forge.md
    ~/.claude/plugins/seraphim/commands/run.md
    ~/.claude/plugins/seraphim/lib/wave-planner.js
    ~/.claude/plugins/seraphim/lib/requirements.js
    /home/alucardmessangeroflight/.claude/get-shit-done/workflows/plan-phase.md
  </read_first>
  <action>
    Create `~/.claude/plugins/seraphim/commands/plan.md` following established command pattern (per D-01, D-04).

    **`/seraphim:plan`** — per PLAN-03, PLAN-06, D-07, D-10:
    - Frontmatter: description "Generate wave-structured plans with dependency resolution", argument-hint "[phase-ref] [--no-check]", allowed-tools ["Read", "Write", "Bash"]
    - Step 1: Resolve project root (bash walk-up)
    - Step 2: Identify target phase from $ARGUMENTS or current phase in roadmap
    - Step 3: Gather context:
      - Read CONTEXT.md (locked decisions) from phase directory
      - Read RESEARCH.md if exists
      - Read requirements.json for phase requirements
      - Read roadmap.json for feature scope
    - Step 4: Analyze requirements and break into tasks with dependencies:
      - For each requirement, determine implementation tasks
      - Identify task dependencies (which tasks need other tasks' output)
      - Build task objects: `{ id, name, description, depends_on, files, done_criteria }`
    - Step 5: Resolve dependencies into waves via wave-planner.js:
      ```bash
      node -e "
        const wp = require('${PLUGIN_ROOT}/lib/wave-planner');
        const tasks = ${TASKS_JSON};
        const waves = wp.resolveWaves(tasks);
        console.log(JSON.stringify(waves));
      "
      ```
      If cycle detected, report error and ask user to resolve dependency conflict
    - Step 6: Generate PLAN.md with wave-structured task layout:
      - Frontmatter with phase, plan number, wave, depends_on, files_modified, autonomous, requirements, must_haves
      - Tasks grouped by wave number
      - Each task has: name, files, action, verify, done
      - Write to `.planning/phases/{phase-dir}/{phase}-{NN}-PLAN.md`
    - Step 7: Checker verification loop (per D-10, PLAN-06):
      - If `--no-check` flag or `config.plan_check === false`: skip
      - Otherwise spawn checker subagent: "Review this PLAN.md for gaps, missing done-criteria, unreachable tasks, dependency cycles. Return PASS or list of issues."
      - If PASS: continue
      - If issues: revise PLAN.md, increment iteration counter
      - Max 3 iterations. If still failing after 3, warn user and continue with current plan
    - Step 8: Update roadmap.json with waves via wave-planner.js writeWaves
    - Step 9: Confirm: "Plan {NN} created with {N} tasks in {M} waves. Checker: {PASS/SKIP/issues}"

    The plan output format must be compatible with /seraphim:execute consumption — tasks grouped by wave, each with clear done criteria.
  </action>
  <verify>
    <automated>bash -c "test -f ~/.claude/plugins/seraphim/commands/plan.md && grep -q 'wave-planner' ~/.claude/plugins/seraphim/commands/plan.md && grep -q 'resolveWaves' ~/.claude/plugins/seraphim/commands/plan.md && grep -q 'checker' ~/.claude/plugins/seraphim/commands/plan.md && grep -q 'PLAN.md' ~/.claude/plugins/seraphim/commands/plan.md && echo 'plan.md OK' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/plugins/seraphim/commands/plan.md
    - Contains YAML frontmatter with description, argument-hint, allowed-tools
    - References lib/wave-planner.js for resolveWaves (grep for 'wave-planner')
    - References lib/requirements.js for reading requirements
    - Contains checker loop logic with max 3 iterations (grep for 'checker' and '3')
    - Contains --no-check flag handling
    - Output is PLAN.md format with wave-structured tasks
    - Uses project root walk-up pattern
  </acceptance_criteria>
  <done>plan.md exists, generates wave-structured PLAN.md using Kahn's algorithm from wave-planner.js, includes optional checker verification loop capped at 3 iterations</done>
</task>

</tasks>

<verification>
plan.md command file exists and references wave-planner.js resolveWaves. Checker loop documented with 3-iteration cap.
</verification>

<success_criteria>
- /seraphim:plan generates PLAN.md with tasks grouped into dependency waves
- Kahn's algorithm used for dependency resolution (via wave-planner.js)
- Checker verification loop with max 3 iterations (optional via --no-check or config)
- Plan output compatible with /seraphim:execute consumption
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-command-layer/33-04-SUMMARY.md`
</output>
