---
phase: 33-core-command-layer
plan: 05
type: execute
wave: 3
depends_on: ["33-04"]
files_modified:
  - ~/.claude/plugins/seraphim/commands/execute.md
  - ~/.claude/plugins/seraphim/commands/execute-plan.md
  - ~/.claude/plugins/seraphim/commands/autonomous.md
  - ~/.claude/plugins/seraphim/commands/quick.md
  - ~/.claude/plugins/seraphim/commands/fast.md
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-05]

must_haves:
  truths:
    - "User can execute all plans in a phase via /seraphim:execute with wave-based parallel execution"
    - "User can execute a single plan via /seraphim:execute-plan"
    - "User can run all remaining phases autonomously via /seraphim:autonomous"
    - "User can execute small ad-hoc tasks via /seraphim:quick"
    - "User can execute trivial tasks inline via /seraphim:fast"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/execute.md"
      provides: "Phase execution with wave parallelism"
      contains: "seraphim:execute"
    - path: "~/.claude/plugins/seraphim/commands/execute-plan.md"
      provides: "Single plan execution"
      contains: "seraphim:execute-plan"
    - path: "~/.claude/plugins/seraphim/commands/autonomous.md"
      provides: "Full autonomous pipeline"
      contains: "seraphim:autonomous"
    - path: "~/.claude/plugins/seraphim/commands/quick.md"
      provides: "Ad-hoc task execution"
      contains: "seraphim:quick"
    - path: "~/.claude/plugins/seraphim/commands/fast.md"
      provides: "Trivial inline execution"
      contains: "seraphim:fast"
  key_links:
    - from: "commands/execute.md"
      to: "lib/wave-planner.js"
      via: "reads waves from PLAN.md to group parallel execution"
      pattern: "wave"
    - from: "commands/autonomous.md"
      to: "commands/discuss.md + plan.md + execute.md"
      via: "sequential subagent dispatch per phase"
      pattern: "discuss.*plan.*execute"
---

<objective>
Create the five execution commands: execute (phase-level wave parallelism), execute-plan (single plan), autonomous (multi-phase pipeline), quick (ad-hoc), and fast (inline trivial).

Purpose: These commands are the "do the work" layer — they consume plans and produce implemented code.
Output: 5 command files in ~/.claude/plugins/seraphim/commands/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-core-command-layer/33-CONTEXT.md
@.planning/phases/33-core-command-layer/33-RESEARCH.md
@.planning/phases/33-core-command-layer/33-01-SUMMARY.md
@.planning/phases/33-core-command-layer/33-04-SUMMARY.md

<interfaces>
From ~/.claude/plugins/seraphim/lib/wave-planner.js (created in Plan 01):
```javascript
module.exports = {
  resolveWaves,        // (tasks) => [[wave0_ids], [wave1_ids], ...]
  readWaves,           // (projectRoot, featureId) => waves array
  writeWaves,          // (projectRoot, featureId, waves) => void
  validateDependencies // (tasks) => { valid, errors }
};
```

PLAN.md format (produced by /seraphim:plan in Plan 04):
- Frontmatter with wave, depends_on, files_modified
- Tasks in XML format grouped by wave
- Each task has name, files, action, verify, done
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create execute.md and execute-plan.md commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/execute.md
    ~/.claude/plugins/seraphim/commands/execute-plan.md
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/forge.md
    ~/.claude/plugins/seraphim/commands/run.md
    /home/alucardmessangeroflight/.claude/get-shit-done/workflows/execute-plan.md
    ~/.claude/plugins/seraphim/lib/wave-planner.js
  </read_first>
  <action>
    **execute.md** (`/seraphim:execute`) — per EXEC-01, EXEC-06, D-09:
    - Frontmatter: description "Execute all plans in a phase with wave-based parallelism", argument-hint "[phase-ref]", allowed-tools ["Read", "Write", "Bash", "Edit", "Glob", "Grep"]
    - Step 1: Resolve project root
    - Step 2: Identify target phase from $ARGUMENTS or current phase
    - Step 3: Discover PLAN.md files in `.planning/phases/{phase-dir}/` — sort by plan number
    - Step 4: Group plans by `wave` frontmatter field
    - Step 5: For each wave (sequential between waves):
      - Spawn parallel executor subagents for all plans in this wave (per D-09)
      - Each executor reads its PLAN.md and executes tasks sequentially within the plan
      - Wait for all agents in wave to complete before starting next wave
      - If any agent fails, report error and ask user whether to continue or abort
    - Step 6: After all waves complete:
      - Update phase state via phase-state.js
      - Write SUMMARY.md for each completed plan
      - Report completion status: N/M plans succeeded, wave breakdown

    The executor subagent pattern matches GSD execute-phase (per D-09) — each plan gets its own agent with full tool access. Independent plans run in parallel within a wave.

    **execute-plan.md** (`/seraphim:execute-plan`) — per EXEC-02:
    - Frontmatter: description "Execute a single plan", argument-hint "<plan-file>", allowed-tools ["Read", "Write", "Bash", "Edit", "Glob", "Grep"]
    - Step 1: Resolve project root
    - Step 2: Read the specified PLAN.md file from $ARGUMENTS
    - Step 3: Execute tasks sequentially as described in the plan
    - Step 4: For each task: read files in read_first, execute action, run verify, check acceptance_criteria
    - Step 5: Write SUMMARY.md after completion
    - Step 6: Report: "Plan {NN} complete — N/M tasks succeeded"

    This is the simpler single-plan version — no wave grouping needed, just sequential task execution within one plan.
  </action>
  <verify>
    <automated>bash -c "test -f ~/.claude/plugins/seraphim/commands/execute.md && grep -q 'wave' ~/.claude/plugins/seraphim/commands/execute.md && test -f ~/.claude/plugins/seraphim/commands/execute-plan.md && grep -q 'PLAN.md' ~/.claude/plugins/seraphim/commands/execute-plan.md && echo 'execute commands OK' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - execute.md exists with YAML frontmatter including full tool access (Read, Write, Bash, Edit, Glob, Grep)
    - execute.md groups plans by wave and executes waves sequentially (grep for 'wave')
    - execute.md spawns parallel agents within a wave (grep for 'parallel')
    - execute-plan.md exists with YAML frontmatter
    - execute-plan.md reads a single PLAN.md and executes tasks sequentially
    - Both use project root walk-up pattern
    - Both write SUMMARY.md after completion
  </acceptance_criteria>
  <done>execute.md handles wave-based parallel execution of multiple plans, execute-plan.md handles single plan execution</done>
</task>

<task type="auto">
  <name>Task 2: Create autonomous.md, quick.md, and fast.md commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/autonomous.md
    ~/.claude/plugins/seraphim/commands/quick.md
    ~/.claude/plugins/seraphim/commands/fast.md
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/commands/run.md
    ~/.claude/plugins/seraphim/commands/forge.md
    ~/.claude/plugins/seraphim/commands/execute.md
  </read_first>
  <action>
    **autonomous.md** (`/seraphim:autonomous`) — per EXEC-03:
    - Frontmatter: description "Run all remaining phases autonomously (discuss, plan, execute per phase)", argument-hint "[--from <phase>]", allowed-tools ["Read", "Write", "Bash", "Edit", "Glob", "Grep"]
    - Step 1: Resolve project root
    - Step 2: Read roadmap.json, find all incomplete phases
    - Step 3: If --from specified, start from that phase; otherwise start from first incomplete
    - Step 4: For each remaining phase (sequential):
      a. Run `/seraphim:discuss` for this phase (dispatch as subagent reading discuss.md)
      b. Run `/seraphim:plan` for this phase (dispatch as subagent reading plan.md)
      c. Run `/seraphim:execute` for this phase (dispatch as subagent reading execute.md)
      d. Report phase completion
    - Step 5: After all phases: report milestone completion status

    Per D-01, each step dispatches as a subagent (not inline reimplementation). This follows the run.md pipeline orchestrator pattern.

    **quick.md** (`/seraphim:quick`) — per EXEC-04:
    - Frontmatter: description "Execute a small ad-hoc task with atomic commit and state tracking", argument-hint "<task description>", allowed-tools ["Read", "Write", "Bash", "Edit", "Glob", "Grep"]
    - Step 1: Resolve project root
    - Step 2: Take $ARGUMENTS as the task description
    - Step 3: Execute the task directly — no planning phase, no PLAN.md, just do it
    - Step 4: After completion, create atomic git commit with descriptive message
    - Step 5: Log task completion to `.planning/quick-tasks.jsonl` with: `{ "id", "description", "date", "commit", "files_changed" }`
    - Step 6: Confirm: "Quick task done — commit: {hash}"

    This is the simplified forge pattern — single task, commit after, state tracked.

    **fast.md** (`/seraphim:fast`) — per EXEC-05:
    - Frontmatter: description "Execute a trivial task inline (no subagents, no ceremony)", argument-hint "<task description>", allowed-tools ["Read", "Write", "Bash", "Edit"]
    - Step 1: Take $ARGUMENTS as the task description
    - Step 2: Execute immediately — no project root check, no lib calls, no state tracking, no commit
    - Step 3: Just do the task and report what was done

    This is the absolute minimum — no overhead at all. For typo fixes, one-line changes, quick edits.
  </action>
  <verify>
    <automated>bash -c "for cmd in autonomous quick fast; do test -f ~/.claude/plugins/seraphim/commands/\$cmd.md && grep -q 'description:' ~/.claude/plugins/seraphim/commands/\$cmd.md && echo \"\$cmd.md OK\" || echo \"\$cmd.md MISSING\"; done"</automated>
  </verify>
  <acceptance_criteria>
    - autonomous.md exists and references discuss, plan, execute as sequential subagent dispatches
    - autonomous.md loops over remaining phases from roadmap
    - quick.md exists, creates git commit after task, logs to quick-tasks.jsonl
    - quick.md does NOT create PLAN.md (simplified flow)
    - fast.md exists and is minimal — no project root check, no state tracking, no commit
    - fast.md has fewer allowed-tools than other commands (no Glob, no Grep)
    - All three have YAML frontmatter
  </acceptance_criteria>
  <done>autonomous.md chains discuss+plan+execute per phase, quick.md does ad-hoc tasks with commits, fast.md does trivial inline tasks with zero ceremony</done>
</task>

</tasks>

<verification>
All 5 execution command files exist in ~/.claude/plugins/seraphim/commands/ with valid YAML frontmatter. execute.md implements wave-based parallel execution.
</verification>

<success_criteria>
- /seraphim:execute groups plans by wave and runs waves in parallel
- /seraphim:execute-plan executes a single plan's tasks sequentially
- /seraphim:autonomous chains discuss+plan+execute for all remaining phases
- /seraphim:quick does ad-hoc tasks with atomic commits
- /seraphim:fast does trivial tasks with zero overhead
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-command-layer/33-05-SUMMARY.md`
</output>
