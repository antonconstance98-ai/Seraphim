---
phase: 07-charts-hook-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/hooks/codex-dashboard-generator.js
  - /home/alucard/.claude/dashboard/dashboard.html
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03]
must_haves:
  truths:
    - "Line chart (cost/baseline) and bar chart (savings%) render in dashboard"
    - "Daily/weekly toggle regroups line chart client-side"
    - "Chart.js inlined from sidecar; no external script src= URLs"
    - "Absent Chart.js: typeof Chart guard, silent return"
    - "Normalised dates rejected; empty timeSeries no-crash"
  artifacts:
    - path: "/home/alucard/.claude/hooks/codex-dashboard-generator.js"
      contains: "timeSeries"
    - path: "/home/alucard/.claude/dashboard/dashboard.html"
      contains: "costSavingsChart"
  key_links:
    - {from: "computeMetrics", to: "byDate", via: "regex range + Date roundtrip + UTC bucket key"}
    - {from: "DASHBOARD_DATA.timeSeries", to: "buildLine", via: "destroy-then-empty-guard"}
    - {from: "typeof Chart==='undefined'", to: "IIFE exit", via: "first IIFE statement"}
---
<objective>
Add timeSeries to computeMetrics(), add chart canvases and guarded IIFE to HTML template, regenerate. Satisfies CHART-01/02/03.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Add timeSeries to computeMetrics()</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <read_first>Full file. Find: step 2 accumulators, step 4 session-grouping end, step 10 end, step 11 return.</read_first>
  <action>
A) After `const unattributed=...`: `const byDate=new Map();`
B) In step 4 loop, after per-session cost/opus accumulation. Two-layer validation then UTC bucket:
  (1) regex: /^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$/.test(ts.slice(0,10))
  (2) roundtrip: dt=new Date(ts); !isNaN(dt) AND dt.toISOString().slice(0,10)===ts.slice(0,10)
  Note: all hooks emit Z-suffix UTC timestamps; roundtrip is exact for Z-suffix.
  bucket key: new Date(ts).toISOString().slice(0,10) — UTC day
  accumulate safeNum(cost_usd) and safeNum(opus_baseline_usd) into byDate[key]
C) After step 10: timeSeries = byDate entries as {date,cost,opusBaseline} sorted asc; [] when empty.
D) Add timeSeries to return object after taskTypeDistribution.
  </action>
  <verify>
    <automated>node -e "const {computeMetrics:c}=require('/home/alucard/.claude/hooks/codex-dashboard-generator');const r=(t,v)=>({timestamp:t,cost_usd:v,opus_baseline_usd:v,project_name:'p',model:'m',task_type:'e',tokens:{input:1,cached_input:0,output:1}});const d=c([r('2026-04-01T10:00:00Z',.01),r('2026-04-01T18:00:00Z',.005),r('2026-04-02T09:00:00Z',.02),r('2026-99-99T00:00:00Z',.01),r('2026-02-31T00:00:00Z',.01),r(null,.01)]);if(d.timeSeries.length!==2||d.timeSeries[0].date!=='2026-04-01'||c([]).timeSeries.length)throw new Error('fail');console.log('PASS')"</automated>
  </verify>
  <done>2 UTC-day buckets; invalid dates excluded; empty→[]; PASS</done>
</task>
<task type="auto">
  <name>Task 2: Add chart canvases and IIFE to HTML template, regenerate</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <read_first>generateDashboard() HTML template. chartJsSource inlines chart.min.js via fs.readFileSync — no change needed. Find: `&lt;!-- Section 2 --&gt;` and `renderSessionTable(1);\n&lt;/script&gt;`.</read_first>
  <action>
Edit 1 — Before Section 2 insert:
- Section id="charts": h2 + btn-daily/btn-weekly (onclick=setChartGrouping), canvas id="costSavingsChart" h=100
- Section id="project-chart": h2 + canvas id="projectBarChart" h=120

Edit 2 — Replace `renderSessionTable(1);\n</script>` with `renderSessionTable(1);` + IIFE + `</script>`:
- Line 1: `if(typeof Chart==='undefined')return;` — ALL code inside
- groupTS(mode): daily→DASHBOARD_DATA.timeSeries; weekly→ISO Monday-UTC buckets, skip NaN
- buildLine(mode): if(inst)inst.destroy(); inst=null; if series.length===0 return; new Chart line
- buildBar(): filter Unattributed, sort parseFloat(savingsPct) desc NaN→0, if empty return; hbar
- setChartGrouping(mode): update btn styles, rebuild line; window.setChartGrouping=setChartGrouping
- Tail: buildLine('daily');buildBar();

Run: `node /home/alucard/.claude/hooks/codex-dashboard-generator.js`
  </action>
  <verify>
    <automated>node -e "require('/home/alucard/.claude/hooks/codex-dashboard-generator')" && node /home/alucard/.claude/hooks/codex-dashboard-generator.js && H=/home/alucard/.claude/dashboard/dashboard.html && [ ! -f "$H" ]&&{echo FAIL:missing;exit 1;};for p in costSavingsChart projectBarChart btn-daily "typeof Chart" "series.length===0"; do grep -q "$p" "$H"||{echo FAIL:$p;exit 1;};done&&grep -q "new Chart" "$H"&&! grep -q '<script src=' "$H"&&echo PASS</automated>
  </verify>
  <done>Generator exits 0; dashboard.html has 5 patterns + new Chart + no external script src; PASS</done>
</task>
</tasks>
<success_criteria>CHART-01/02/03 satisfied. Chart.js inlined. typeof Chart guard, destroy-then-empty-guard, UTC-day bucketing. No external script tags.</success_criteria>
<output>After completion, create `.planning/phases/07-charts-hook-integration/07-01-SUMMARY.md`</output>
