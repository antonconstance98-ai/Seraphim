---
phase: 07-charts-hook-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/hooks/codex-dashboard-generator.js
  - /home/alucard/.claude/dashboard/dashboard.html
autonomous: false
requirements: [CHART-01, CHART-02, CHART-03]
must_haves:
  truths:
    - "Line chart (cost/baseline) and bar chart (savings%) render in dashboard"
    - "Daily/weekly toggle regroups line chart client-side"
    - "Chart.js inlined from sidecar; no external script src= URLs"
    - "Absent Chart.js: typeof Chart guard, silent return"
    - "Normalised dates rejected; empty timeSeries no-crash"
  artifacts:
    - path: "/home/alucard/.claude/hooks/codex-dashboard-generator.js"
      contains: "timeSeries"
    - path: "/home/alucard/.claude/dashboard/dashboard.html"
      contains: "costSavingsChart"
  key_links:
    - {from: "computeMetrics", to: "byDate", via: "typeof+regex+roundtrip, UTC bucket key"}
    - {from: "DASHBOARD_DATA.timeSeries", to: "buildLine", via: "destroy-then-empty-guard"}
    - {from: "typeof Chart==='undefined'", to: "IIFE exit", via: "first IIFE statement"}
---
<objective>
Add timeSeries to computeMetrics(), add chart canvases and guarded IIFE to HTML template, regenerate. Satisfies CHART-01/02/03.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Add timeSeries to computeMetrics()</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <action>
Read full file. Find: step 2 accumulators, step 4 session-grouping end, step 10 end, step 11 return.
A) After `const unattributed=...`: `const byDate=new Map();`
B) In step 4 loop — three-layer validation then UTC bucket:
  (1) typeof ts==='string' AND ts.length>=10
  (2) regex: /^\d{4}-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$/.test(ts.slice(0,10))
  (3) roundtrip: dt=new Date(ts); !isNaN(dt) AND dt.toISOString().slice(0,10)===ts.slice(0,10)
  bucket key: new Date(ts).toISOString().slice(0,10)
  accumulate safeNum(cost_usd) and safeNum(opus_baseline_usd) into byDate[key]
C) After step 10: timeSeries={date,cost,opusBaseline} sorted asc; [] when empty.
D) Add timeSeries to return after taskTypeDistribution.
  </action>
  <verify>
    <automated>node -e "const {computeMetrics:c}=require('/home/alucard/.claude/hooks/codex-dashboard-generator');const r=(t,v)=>({timestamp:t,cost_usd:v,opus_baseline_usd:v,project_name:'p',model:'m',task_type:'e',tokens:{input:1,cached_input:0,output:1}});const d=c([r('2026-04-01T10:00:00Z',.01),r('2026-04-01T18:00:00Z',.005),r('2026-04-02T09:00:00Z',.02),r('2026-99-99T00:00:00Z',.01),r('2026-02-31T00:00:00Z',.01),r(null,.01),r(42,.01)]);if(d.timeSeries.length!==2||d.timeSeries[0].date!=='2026-04-01'||c([]).timeSeries.length)throw new Error('fail');console.log('PASS')"</automated>
  </verify>
  <done>2 UTC-day buckets; invalid/non-string excluded; empty→[]; PASS</done>
</task>
<task type="auto">
  <name>Task 2: Add chart canvases and IIFE, regenerate</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <action>
Read generateDashboard() template. chartJsSource inlines chart.min.js via fs.readFileSync. Find Section 2 comment and `renderSessionTable(1);\n</script>`.
Edit 1 — Before Section 2: canvas#costSavingsChart (h=100) with btn-daily/btn-weekly; canvas#projectBarChart (h=120).
Edit 2 — Replace `renderSessionTable(1);\n</script>` with renderSessionTable(1); + IIFE + </script>:
- if(typeof Chart==='undefined')return;
- groupTS(mode): daily→DASHBOARD_DATA.timeSeries; weekly→ISO Monday-UTC via Thursday algorithm
- buildLine(mode): destroy+null inst; if(!series||!series.length)return; new Chart line
- buildBar(): (DASHBOARD_DATA.projectTable||[]).filter(r=>r.name!=='Unattributed'), sort parseFloat(savingsPct) desc; if empty return; hbar
- setChartGrouping(mode): update btns, buildLine(mode); window.setChartGrouping=setChartGrouping
- Tail: buildLine('daily');buildBar();
Run: `node /home/alucard/.claude/hooks/codex-dashboard-generator.js`
  </action>
  <verify>
    <automated>node -e "require('/home/alucard/.claude/hooks/codex-dashboard-generator')" && node /home/alucard/.claude/hooks/codex-dashboard-generator.js && H=/home/alucard/.claude/dashboard/dashboard.html && [ ! -f "$H" ]&&{echo FAIL;exit 1;};for p in costSavingsChart projectBarChart btn-daily btn-weekly "typeof Chart" "series.length===0" setChartGrouping groupTS; do grep -q "$p" "$H"||{echo FAIL:$p;exit 1;};done&&! grep -q '<script src=' "$H"&&echo PASS</automated>
  </verify>
  <done>Generator exits 0; 8 patterns present; no external script src; PASS</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual chart verification</name>
  <action>Human verification</action>
  <verify>Human approved</verify>
  <done>Approved</done>
  <what-built>Charts: line+bar</what-built>
  <how-to-verify>Open file:///home/alucard/.claude/dashboard/dashboard.html — confirm charts render, Weekly toggle works.</how-to-verify>
  <resume-signal>Type "approved" or describe what failed</resume-signal>
</task>
</tasks>
<success_criteria>CHART-01/02/03: charts render, toggle works, no CDN, no crash on absent Chart.js.</success_criteria>
<output>After completion, create `.planning/phases/07-charts-hook-integration/07-01-SUMMARY.md`</output>
