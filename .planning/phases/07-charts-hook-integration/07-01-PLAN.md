---
phase: 07-charts-hook-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/hooks/codex-dashboard-generator.js
  - /home/alucard/.claude/dashboard/dashboard.html
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03]
must_haves:
  truths:
    - "Line chart (cost/baseline) and bar chart (savings% by project) render in dashboard"
    - "Daily/weekly toggle regroups line chart client-side"
    - "No external src= URLs in dashboard.html; Chart.js inlined"
    - "Absent Chart.js: typeof Chart guard at IIFE top, silent return"
    - "Impossible dates and empty timeSeries handled without crash"
  artifacts:
    - path: "/home/alucard/.claude/hooks/codex-dashboard-generator.js"
      contains: "timeSeries"
    - path: "/home/alucard/.claude/dashboard/dashboard.html"
      contains: "costSavingsChart"
  key_links:
    - {from: "computeMetrics step 4", to: "byDate", via: "new Date() calendar validation"}
    - {from: "DASHBOARD_DATA.timeSeries", to: "buildLine", via: "series.length===0 guard"}
    - {from: "typeof Chart==='undefined'", to: "IIFE exit", via: "first IIFE statement"}
---
<objective>
Two edits to codex-dashboard-generator.js: (1) add timeSeries to computeMetrics(), (2) add chart canvases and guarded Chart.js IIFE to HTML template, then regenerate. Satisfies CHART-01/02/03.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Add timeSeries to computeMetrics()</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <read_first>Full file. Find: step 2 accumulators, step 4 session-grouping end, step 10 end, step 11 return.</read_first>
  <action>
A) After `const unattributed=...`: `const byDate=new Map();`
B) After session grouping in step 4 — reuse `cost`/`opus` locals (safeNum'd). Use new Date() to reject impossible dates; slice(0,10) gives UTC calendar date:
  if string timestamp length>=10 AND !isNaN(new Date(ts.slice(0,10)+'T00:00:00Z')): accumulate cost+opus into byDate[dateKey]
C) After step 10: build timeSeries = byDate entries as {date,cost,opusBaseline} sorted ascending. [] when empty.
D) Add `timeSeries:timeSeries` to return after taskTypeDistribution.
  </action>
  <verify>
    <automated>node -e "const {computeMetrics:c}=require('/home/alucard/.claude/hooks/codex-dashboard-generator');const r=(t,v)=>({timestamp:t,cost_usd:v,opus_baseline_usd:v,project_name:'p',model:'m',task_type:'e',tokens:{input:1,cached_input:0,output:1}});const d=c([r('2026-04-01T10:00:00Z',.01),r('2026-04-01T18:00:00Z',.005),r('2026-04-02T09:00:00Z',.02),r('2026-99-99T00:00:00Z',.01),r(null,.01)]);if(d.timeSeries.length!==2||d.timeSeries[0].date!=='2026-04-01'||c([]).timeSeries.length)throw new Error('fail');console.log('PASS')"</automated>
  </verify>
  <done>2 buckets (2026-04-01 merged, 2026-04-02), impossible/null excluded, empty→[]; prints PASS</done>
</task>
<task type="auto">
  <name>Task 2: Add chart canvases and IIFE to HTML template, regenerate</name>
  <files>/home/alucard/.claude/hooks/codex-dashboard-generator.js</files>
  <read_first>Re-read generateDashboard() HTML template. Find: `&lt;!-- Section 2: Per-Project Table ... --&gt;` and `renderSessionTable(1);\n&lt;/script&gt;`.</read_first>
  <action>
Edit 1 — Before Section 2 comment, insert:
- Section id="charts": h2 + btn-daily/btn-weekly toggle buttons (onclick=setChartGrouping), canvas id="costSavingsChart" height="100"
- Section id="project-chart": h2 + canvas id="projectBarChart" height="120"

Edit 2 — Replace `renderSessionTable(1);\n</script>` with `renderSessionTable(1);` + IIFE + `</script>`:
- Line 1: `if(typeof Chart==='undefined')return;` — all code inside this guard
- groupTS(mode): daily→DASHBOARD_DATA.timeSeries; weekly→ISO 8601 Monday-UTC week buckets (YYYY-WNN), skip NaN dates
- buildLine(mode): if series.length===0 return; Chart.js line chart, #00d4ff cost / #ffab00 dashed baseline, responsive dark theme
- buildBar(): filter Unattributed, sort parseFloat(savingsPct) desc (NaN→0), if rows.length===0 return; horizontal bar indexAxis:'y' #00e676
- setChartGrouping(mode): update btn styles, rebuild line; window.setChartGrouping=setChartGrouping
- Tail: buildLine('daily');buildBar();

Run: `node /home/alucard/.claude/hooks/codex-dashboard-generator.js`
  </action>
  <verify>
    <automated>node -e "require('/home/alucard/.claude/hooks/codex-dashboard-generator')" && node /home/alucard/.claude/hooks/codex-dashboard-generator.js && H=/home/alucard/.claude/dashboard/dashboard.html && { [ ! -f "$H" ]&&echo PASS&&exit 0; }; for p in costSavingsChart projectBarChart btn-daily "typeof Chart" "series.length===0"; do grep -q "$p" "$H"||{echo FAIL:$p;exit 1;};done&&[ "$(grep -c 'src="http' $H||echo 0)"=0 ]&&echo PASS</automated>
  </verify>
  <done>Generator exits 0; dashboard.html has 5 required patterns, zero external src URLs; prints PASS</done>
</task>
</tasks>
<success_criteria>CHART-01: line chart from timeSeries. CHART-02: daily/weekly toggle. CHART-03: bar chart sorted by savingsPct. typeof Chart guard, series.length===0 guard, new Date() validation, no CDN.</success_criteria>
<output>Create </output>
