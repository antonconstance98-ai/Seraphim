---
phase: 07-charts-hook-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/settings.json
autonomous: true
requirements:
  - INTG-02

must_haves:
  truths:
    - "Opening a new Claude Code session triggers codex-global-aggregator.js automatically"
    - "The aggregator hook runs after codex-cost-reporter.js (per-project first, then global)"
    - "Session open delay from the hook is within the 30-second timeout — sessions never hang"
    - "The hook entry is valid JSON and does not break the existing SessionStart array"
  artifacts:
    - path: "/home/alucard/.claude/settings.json"
      provides: "Updated SessionStart hooks array with codex-global-aggregator.js entry"
      contains: "codex-global-aggregator.js"
  key_links:
    - from: "~/.claude/settings.json SessionStart hooks"
      to: "codex-global-aggregator.js"
      via: "command entry added after codex-cost-reporter.js"
      pattern: "codex-global-aggregator\\.js"
---

<objective>
Register `codex-global-aggregator.js` as a SessionStart hook in `~/.claude/settings.json` so the dashboard auto-regenerates on every session open. The aggregator already calls `generateDashboard()` internally — no additional wiring is needed beyond adding the hook entry.

Purpose: Satisfy INTG-02 — the final integration requirement of the v1.1 milestone.
Output: Updated `~/.claude/settings.json` with the aggregator in the SessionStart hooks array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-charts-hook-integration/07-CONTEXT.md

<interfaces>
<!-- Current SessionStart hooks array in ~/.claude/settings.json -->

Current state (before this plan):
```json
"SessionStart": [
  {
    "hooks": [
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/gsd-check-update.js\""
      },
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-cost-reporter.js\"",
        "timeout": 15
      }
    ]
  }
]
```

Target state (after this plan) — append one entry to the hooks array:
```json
"SessionStart": [
  {
    "hooks": [
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/gsd-check-update.js\""
      },
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-cost-reporter.js\"",
        "timeout": 15
      },
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"",
        "timeout": 30
      }
    ]
  }
]
```

Key constraints:
- timeout: 30 (discovery can be slow across many projects; 30s matches ROADMAP success criteria of under 2s for warm runs but allows cold scan headroom)
- Place AFTER codex-cost-reporter.js (per-project cost reporting first, then global aggregation)
- The existing array has ONE object with a "hooks" sub-array — append to that sub-array, do not add a second top-level object
- All other sections of settings.json (permissions, PostToolUse, Stop, SubagentStop, PreToolUse, statusLine, enabledPlugins, etc.) must remain UNCHANGED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aggregator to SessionStart hooks in settings.json</name>
  <files>/home/alucard/.claude/settings.json</files>

  <read_first>
    - /home/alucard/.claude/settings.json — read the ENTIRE file before editing. Confirm the exact current structure of the SessionStart array. The file is ~150 lines of JSON; read it fully to avoid accidentally corrupting other hook sections (PostToolUse, Stop, SubagentStop, PreToolUse).
  </read_first>

  <action>
Edit `/home/alucard/.claude/settings.json`.

Locate the SessionStart hooks array. It currently contains one object with a "hooks" sub-array holding two entries: `gsd-check-update.js` and `codex-cost-reporter.js`.

Append the following entry as the THIRD element of that "hooks" sub-array, after the `codex-cost-reporter.js` entry:

```json
{
  "type": "command",
  "command": "node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"",
  "timeout": 30
}
```

The resulting SessionStart section must be exactly:
```json
"SessionStart": [
  {
    "hooks": [
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/gsd-check-update.js\""
      },
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-cost-reporter.js\"",
        "timeout": 15
      },
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"",
        "timeout": 30
      }
    ]
  }
]
```

Do not modify any other section of settings.json. The file must remain valid JSON after the edit.
  </action>

  <verify>
    <automated>node -e "const s = require('/home/alucard/.claude/settings.json'); const hooks = s.hooks.SessionStart[0].hooks; const agg = hooks.find(h => h.command && h.command.includes('codex-global-aggregator')); if (!agg) throw new Error('aggregator hook not found'); if (agg.timeout !== 30) throw new Error('timeout should be 30, got ' + agg.timeout); const reporter = hooks.find(h => h.command && h.command.includes('codex-cost-reporter')); const reporterIdx = hooks.indexOf(reporter); const aggIdx = hooks.indexOf(agg); if (aggIdx <= reporterIdx) throw new Error('aggregator must come after cost-reporter'); console.log('PASS: aggregator hook registered at index ' + aggIdx + ' with timeout ' + agg.timeout)"</automated>
  </verify>

  <acceptance_criteria>
    - `settings.json` is valid JSON — `node -e "require('/home/alucard/.claude/settings.json')"` exits 0
    - SessionStart hooks array has exactly 3 entries (was 2)
    - Third entry has `"command"` containing `"codex-global-aggregator.js"` and `"timeout": 30`
    - `codex-global-aggregator.js` entry appears AFTER `codex-cost-reporter.js` entry
    - All other top-level keys in settings.json (permissions, PostToolUse, Stop, SubagentStop, PreToolUse, statusLine, enabledPlugins, extraKnownMarketplaces, effortLevel, voiceEnabled, skipDangerousModePermissionPrompt, preferences) are unchanged
    - Automated node check exits 0 with "PASS:" output
  </acceptance_criteria>

  <done>settings.json has the aggregator in SessionStart hooks; valid JSON; automated check passes</done>
</task>

</tasks>

<verification>
Run these checks after the edit:

1. `node -e "require('/home/alucard/.claude/settings.json'); console.log('JSON valid')"`
   — Expected: "JSON valid"

2. `node -e "const s = require('/home/alucard/.claude/settings.json'); console.log(JSON.stringify(s.hooks.SessionStart[0].hooks.map(h => h.command), null, 2))"`
   — Expected: array with 3 entries ending in `"node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\""`

3. `node -e "const s = require('/home/alucard/.claude/settings.json'); const h = s.hooks.SessionStart[0].hooks; if (h.length !== 3) throw new Error('expected 3 hooks, got ' + h.length); console.log('Hook count: ' + h.length + ' — OK')"`
   — Expected: "Hook count: 3 — OK"
</verification>

<success_criteria>
- INTG-02: `codex-global-aggregator.js` is registered in the SessionStart hooks array with timeout: 30, positioned after `codex-cost-reporter.js`
- settings.json remains valid JSON with all other configuration unchanged
- On next session start, Claude Code will automatically run the aggregator which calls generateDashboard(), keeping dashboard.html current
</success_criteria>

<output>
After completion, create `.planning/phases/07-charts-hook-integration/07-02-SUMMARY.md`
</output>
