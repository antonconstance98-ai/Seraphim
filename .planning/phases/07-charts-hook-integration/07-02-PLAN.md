---
phase: 07-charts-hook-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/settings.json
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "Every new Claude Code session runs codex-global-aggregator.js automatically"
    - "Aggregator runs after codex-cost-reporter.js"
    - "settings.json remains valid JSON, all other sections unchanged"
  artifacts:
    - path: "/home/alucard/.claude/settings.json"
      contains: "codex-global-aggregator.js"
  key_links:
    - {from: "SessionStart group with cost-reporter", to: "codex-global-aggregator.js", via: "idempotent append, timeout:30"}
---
<objective>
Idempotently add codex-global-aggregator.js after codex-cost-reporter.js in the SessionStart hook group. Satisfies INTG-02.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Append aggregator to SessionStart hooks</name>
  <files>/home/alucard/.claude/settings.json</files>
  <action>
Check: `grep -c generateDashboard /home/alucard/.claude/hooks/codex-global-aggregator.js` >=1.
Find the SessionStart group containing "codex-cost-reporter". If "codex-global-aggregator" already in that group, skip. Else append:
`{"type":"command","command":"node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"","timeout":30}`
Leave all other sections unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('/home/alucard/.claude/settings.json');const g=s.hooks.SessionStart.find(g=>g.hooks.some(h=>(h.command||'').includes('codex-cost-reporter')));if(!g)throw new Error('no group');const h=g.hooks;const ai=h.findIndex(x=>(x.command||'').includes('codex-global-aggregator'));const ri=h.findIndex(x=>(x.command||'').includes('codex-cost-reporter'));if(ai<0)throw new Error('not found');if(h[ai].timeout!==30)throw new Error('timeout');if(ai<=ri)throw new Error('order');console.log('PASS')"</automated>
  </verify>
  <done>Aggregator after cost-reporter, timeout:30; PASS</done>
</task>
</tasks>
<success_criteria>INTG-02: aggregator after cost-reporter, timeout:30, settings.json valid JSON.</success_criteria>
<output>After completion, create `.planning/phases/07-charts-hook-integration/07-02-SUMMARY.md`</output>
