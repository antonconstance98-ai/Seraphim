---
phase: 07-charts-hook-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/settings.json
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "Every new Claude Code session runs codex-global-aggregator.js automatically"
    - "Aggregator runs after codex-cost-reporter.js, timeout 30s"
    - "settings.json remains valid JSON, all other sections unchanged"
  artifacts:
    - path: "/home/alucard/.claude/settings.json"
      contains: "codex-global-aggregator.js"
  key_links:
    - {from: "SessionStart[0].hooks", to: "codex-global-aggregator.js", via: "third entry, after cost-reporter, timeout:30"}
---
<objective>
Add codex-global-aggregator.js as third SessionStart hook in ~/.claude/settings.json. Aggregator already calls generateDashboard() — no other wiring needed. Satisfies INTG-02.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Append aggregator to SessionStart hooks</name>
  <files>/home/alucard/.claude/settings.json</files>
  <read_first>/home/alucard/.claude/settings.json — full file. SessionStart[0].hooks has 2 entries: gsd-check-update.js and codex-cost-reporter.js.</read_first>
  <action>
Append to SessionStart[0].hooks (after codex-cost-reporter.js, do NOT add a second top-level object):
`{"type":"command","command":"node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"","timeout":30}`
Leave all other sections unchanged (permissions, PostToolUse, Stop, SubagentStop, PreToolUse, statusLine, enabledPlugins).
  </action>
  <verify>
    <automated>node -e "const s=require('/home/alucard/.claude/settings.json');const h=s.hooks.SessionStart[0].hooks;const a=h.findIndex(x=>(x.command||'').includes('codex-global-aggregator'));if(a<0||h[a].timeout!==30||h.length!==3)throw new Error('fail');console.log('PASS')"</automated>
  </verify>
  <done>3 hooks in SessionStart, aggregator present with timeout:30, settings.json valid; prints PASS</done>
</task>
</tasks>
<success_criteria>
INTG-02: codex-global-aggregator.js in SessionStart hooks, timeout:30, after codex-cost-reporter.js. settings.json valid JSON, all other sections unchanged.
</success_criteria>
<output>After completion, create `.planning/phases/07-charts-hook-integration/07-02-SUMMARY.md`</output>
