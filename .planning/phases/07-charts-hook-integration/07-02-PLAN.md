---
phase: 07-charts-hook-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/.claude/settings.json
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "Every new Claude Code session runs codex-global-aggregator.js automatically"
    - "Aggregator runs after codex-cost-reporter.js"
    - "settings.json remains valid JSON, all other sections unchanged"
  artifacts:
    - path: "/home/alucard/.claude/settings.json"
      contains: "codex-global-aggregator.js"
  key_links:
    - {from: "SessionStart[0].hooks[2]", to: "codex-global-aggregator.js", via: "timeout:30"}
---
<objective>
Add codex-global-aggregator.js as third SessionStart hook in ~/.claude/settings.json after codex-cost-reporter.js. Satisfies INTG-02.
</objective>
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>@.planning/phases/07-charts-hook-integration/07-CONTEXT.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Append aggregator to SessionStart hooks</name>
  <files>/home/alucard/.claude/settings.json</files>
  <action>
Pre-check: `grep -c generateDashboard /home/alucard/.claude/hooks/codex-global-aggregator.js` must be >=1. If 0, stop.

Append to SessionStart[0].hooks after codex-cost-reporter.js entry (do NOT add a second top-level object):
`{"type":"command","command":"node \"/home/alucard/.claude/hooks/codex-global-aggregator.js\"","timeout":30}`
Leave all other sections unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('/home/alucard/.claude/settings.json');const h=s.hooks.SessionStart[0].hooks;const ai=h.findIndex(x=>(x.command||'').includes('codex-global-aggregator'));const ri=h.findIndex(x=>(x.command||'').includes('codex-cost-reporter'));if(ai<0)throw new Error('not found');if(h[ai].timeout!==30)throw new Error('timeout');if(ai<=ri)throw new Error('order');if(h.length!==3)throw new Error('count:'+h.length);console.log('PASS')"</automated>
  </verify>
  <done>SessionStart has 3 hooks; aggregator after cost-reporter with timeout:30; prints PASS</done>
</task>
</tasks>
<success_criteria>INTG-02: aggregator in SessionStart after cost-reporter, timeout:30, settings.json valid JSON.</success_criteria>
<output>After completion, create `.planning/phases/07-charts-hook-integration/07-02-SUMMARY.md`</output>
