---
phase: 36-human-tasks-debugging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/migrations/002-human-tasks-enrichment.sql
  - lib/push-client.js
  - dashboard/app/api/ingest/route.ts
  - commands/inbox.md
autonomous: true
requirements: [HTASK-01, HTASK-02, HTASK-03]

must_haves:
  truths:
    - "Human tasks carry optional skills_to_learn, thought_prompt, research_task fields"
    - "Inbox command displays enrichment fields when present"
    - "Neon schema accepts the three new columns"
  artifacts:
    - path: "dashboard/migrations/002-human-tasks-enrichment.sql"
      provides: "Three new nullable TEXT columns on human_tasks table"
      contains: "ALTER TABLE human_tasks"
    - path: "lib/push-client.js"
      provides: "scanPendingTasks reads enrichment attrs from markers"
      contains: "skills_to_learn"
    - path: "dashboard/app/api/ingest/route.ts"
      provides: "INSERT includes three enrichment columns"
      contains: "skills_to_learn"
    - path: "commands/inbox.md"
      provides: "Inline display of enrichment fields"
      contains: "thought_prompt"
  key_links:
    - from: "lib/push-client.js"
      to: "dashboard/app/api/ingest/route.ts"
      via: "payload.human_tasks[] with enrichment fields"
      pattern: "skills_to_learn.*thought_prompt.*research_task"
    - from: "commands/inbox.md"
      to: "forge-log.md markers"
      via: "marker attribute parsing"
      pattern: "skills_to_learn"
---

<objective>
Enrich the human task pipeline with three optional fields: skills_to_learn, thought_prompt, and research_task. This spans the full data path from Neon schema through push-client to inbox display.

Purpose: Transform the human task inbox from a bare task list into a coaching tool that surfaces learning opportunities and prompts deeper thinking (per D-01, D-02, D-03).
Output: Migration SQL, updated push-client, updated ingest route, enriched inbox display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-human-tasks-debugging/36-CONTEXT.md
@.planning/phases/36-human-tasks-debugging/36-RESEARCH.md

<interfaces>
<!-- Key files to read for existing patterns -->

From lib/push-client.js (lines 103-160):
- scanPendingTasks(projectRoot, projectName) — reads forge-log markers, returns human_tasks[]
- Marker attrs parsed via regex get(key) pattern
- Payload includes human_tasks array sent to /api/ingest

From dashboard/app/api/ingest/route.ts:
- INSERT INTO human_tasks with ON CONFLICT upsert
- Uses tagged-template sql`` from @neondatabase/serverless

From dashboard/migrations/001-initial-pm-schema.sql:
- human_tasks table: project_name, task_id, type, status, feature_id, urgency, synced_at

From commands/inbox.md:
- Reads forge-log.md directly, displays task list with type/status/description
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration and push-client enrichment</name>
  <files>dashboard/migrations/002-human-tasks-enrichment.sql, lib/push-client.js, dashboard/app/api/ingest/route.ts</files>
  <read_first>
    - dashboard/migrations/001-initial-pm-schema.sql (existing schema)
    - lib/push-client.js (scanPendingTasks function, ~lines 103-160)
    - dashboard/app/api/ingest/route.ts (INSERT INTO human_tasks block)
  </read_first>
  <action>
1. Create `dashboard/migrations/002-human-tasks-enrichment.sql` per D-01:
   ```sql
   ALTER TABLE human_tasks ADD COLUMN IF NOT EXISTS skills_to_learn TEXT;
   ALTER TABLE human_tasks ADD COLUMN IF NOT EXISTS thought_prompt TEXT;
   ALTER TABLE human_tasks ADD COLUMN IF NOT EXISTS research_task TEXT;
   ```
   All three nullable — IF NOT EXISTS for idempotency.

2. In `lib/push-client.js` `scanPendingTasks()`:
   - After existing attribute reads (task_id, type, status, etc.), add reads for the three new optional fields using the same `get(key)` regex pattern
   - Add to the returned task object: `skills_to_learn: get('skills_to_learn') || null`, `thought_prompt: get('thought_prompt') || null`, `research_task: get('research_task') || null`
   - Per Pitfall 1: skills_to_learn stored as comma-separated string in marker attrs

3. In `dashboard/app/api/ingest/route.ts`:
   - Extend the INSERT INTO human_tasks column list with: skills_to_learn, thought_prompt, research_task
   - Extend VALUES with: `${t.skills_to_learn ?? null}`, `${t.thought_prompt ?? null}`, `${t.research_task ?? null}` (per Pitfall 2: use `??` not `||`)
   - Extend ON CONFLICT DO UPDATE SET with: skills_to_learn = EXCLUDED.skills_to_learn, thought_prompt = EXCLUDED.thought_prompt, research_task = EXCLUDED.research_task
  </action>
  <verify>
    <automated>grep -q "skills_to_learn" dashboard/migrations/002-human-tasks-enrichment.sql && grep -q "skills_to_learn" lib/push-client.js && grep -q "skills_to_learn" dashboard/app/api/ingest/route.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "IF NOT EXISTS" dashboard/migrations/002-human-tasks-enrichment.sql returns 3 lines
    - grep "skills_to_learn" lib/push-client.js returns at least 1 line
    - grep "skills_to_learn.*?? null" dashboard/app/api/ingest/route.ts returns at least 1 line
  </acceptance_criteria>
  <done>Migration file exists with 3 ALTER TABLE statements. push-client reads 3 enrichment fields from markers. Ingest route inserts 3 new columns with null fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Inbox enrichment display</name>
  <files>commands/inbox.md</files>
  <read_first>
    - commands/inbox.md (full file — understand existing display loop)
  </read_first>
  <action>
Per D-03, extend the inbox command's task display loop to show enrichment fields inline:

1. After the existing task description line, add conditional rendering:
   - If `skills_to_learn` is present: display as bracketed tags on a "Skills:" line, e.g. `[TypeScript] [SQL]` — split comma-separated string per Pitfall 1
   - If `thought_prompt` is present: display on a "Think:" line with the prompt text
   - If `research_task` is present: display on a "Research:" line with the task text

2. All three are optional — only render lines for fields that are non-null and non-empty.

3. Match the existing indentation and formatting style in inbox.md.
  </action>
  <verify>
    <automated>grep -q "skills_to_learn" commands/inbox.md && grep -q "thought_prompt" commands/inbox.md && grep -q "research_task" commands/inbox.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "skills_to_learn" commands/inbox.md returns at least 1 line
    - grep "thought_prompt" commands/inbox.md returns at least 1 line
    - grep "Skills:" commands/inbox.md returns at least 1 line
  </acceptance_criteria>
  <done>Inbox displays skills as tags, thought-prompt, and research-task inline when fields are present. Absent fields produce no output.</done>
</task>

</tasks>

<verification>
- Migration SQL has 3 idempotent ALTER TABLE statements
- push-client.js scanPendingTasks returns objects with the 3 new optional fields
- ingest route.ts INSERT/UPSERT includes all 3 new columns with ?? null fallback
- inbox.md conditionally renders all 3 enrichment fields
</verification>

<success_criteria>
Human task enrichment pipeline is complete end-to-end: schema, data extraction, storage, and display. All three fields (skills_to_learn, thought_prompt, research_task) flow from marker emission through Neon to inbox display.
</success_criteria>

<output>
After completion, create `.planning/phases/36-human-tasks-debugging/36-01-SUMMARY.md`
</output>
