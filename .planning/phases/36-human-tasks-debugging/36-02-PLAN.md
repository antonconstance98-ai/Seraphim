---
phase: 36-human-tasks-debugging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/repair.js
  - commands/debug.md
  - commands/forensics.md
autonomous: true
requirements: [DBG-01, DBG-03, DBG-04]

must_haves:
  truths:
    - "User can run /seraphim:debug and have persistent state across resets"
    - "User can run /seraphim:forensics and receive a read-only report"
    - "Failed tasks trigger RETRY/DECOMPOSE/PRUNE/ESCALATE cascade within budget"
  artifacts:
    - path: "lib/repair.js"
      provides: "Strategy cascade logic for auto-repair"
      exports: ["selectStrategy", "STRATEGIES"]
      contains: "RETRY"
    - path: "commands/debug.md"
      provides: "Persistent debug command"
      contains: ".planning/debug/"
    - path: "commands/forensics.md"
      provides: "Read-only forensics command"
      contains: "forensics"
  key_links:
    - from: "commands/debug.md"
      to: ".planning/debug/{slug}.md"
      via: "read-then-append with tmp+rename"
      pattern: "renameSync"
    - from: "lib/repair.js"
      to: "commands/debug.md"
      via: "selectStrategy called during debug sessions"
      pattern: "selectStrategy"
---

<objective>
Build the debug command with persistent state, forensics command with read-only diagnostics, and repair.js strategy cascade for auto-repair of failed tasks.

Purpose: Give users systematic debugging that survives session resets (D-04), read-only post-mortem analysis (D-06), and automatic repair strategies before human escalation (D-05).
Output: lib/repair.js, commands/debug.md, commands/forensics.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-human-tasks-debugging/36-CONTEXT.md
@.planning/phases/36-human-tasks-debugging/36-RESEARCH.md

<interfaces>
<!-- Existing debug state file pattern (confirmed in codebase) -->

From .planning/debug/seraphim-plugin-not-loading.md:
Debug state files use YAML frontmatter with fields: status, trigger, created (ISO8601), updated (ISO8601).
Sections: Current Focus (hypothesis, test, expecting, next_action), Symptoms, Evidence, Eliminated, Resolution.

From lib/roadmap.js (atomic write pattern):
- Write to tmp file, then fs.renameSync to target

From commands/autonomous.md (subagent dispatch pattern):
- Subagent spawned with restricted allowed-tools
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/repair.js strategy cascade</name>
  <files>lib/repair.js</files>
  <read_first>
    - lib/roadmap.js (atomic write pattern for reference)
    - lib/requirements.js (module export pattern for reference)
  </read_first>
  <action>
Create `lib/repair.js` per D-05 with:

1. Export `STRATEGIES = ['RETRY', 'DECOMPOSE', 'PRUNE', 'ESCALATE']`

2. Export `selectStrategy(task, repairHistory)`:
   - Count retries in repairHistory where strategy === 'RETRY'
   - Count decomposes where strategy === 'DECOMPOSE'
   - If retries < 2, return 'RETRY'
   - If decomposes < 1, return 'DECOMPOSE'
   - Return 'ESCALATE' (PRUNE is a manual option, not auto-selected)
   - Budget: max 2 retries, 1 decompose before escalation per D-05

3. Export `formatRepairReport(task, strategy, reason)` returning a structured string for debug state files.

4. Pure logic module -- no file I/O, no side effects. Caller handles state writes.

Follow existing module.exports pattern from lib/requirements.js.
  </action>
  <verify>
    <automated>node -e "const r = require('./lib/repair.js'); console.assert(r.selectStrategy({}, []) === 'RETRY'); console.assert(r.selectStrategy({}, [{strategy:'RETRY'},{strategy:'RETRY'}]) === 'DECOMPOSE'); console.assert(r.selectStrategy({}, [{strategy:'RETRY'},{strategy:'RETRY'},{strategy:'DECOMPOSE'}]) === 'ESCALATE'); console.log('PASS')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - node -e require/selectStrategy test prints PASS
    - grep "STRATEGIES" lib/repair.js returns the array
    - grep "module.exports" lib/repair.js confirms exports
  </acceptance_criteria>
  <done>repair.js exports selectStrategy with correct budget thresholds: 2 retries, 1 decompose, then escalate.</done>
</task>

<task type="auto">
  <name>Task 2: debug.md and forensics.md commands</name>
  <files>commands/debug.md, commands/forensics.md</files>
  <read_first>
    - commands/do.md (command file structure pattern)
    - commands/autonomous.md (subagent dispatch pattern)
    - .planning/debug/seraphim-plugin-not-loading.md (existing debug state example, if exists)
  </read_first>
  <action>
1. Create `commands/debug.md` per D-04 and D-07:
   - Command accepts a slug argument (required) describing the issue
   - On invocation: check `.planning/debug/{slug}.md` exists
     - If exists: read existing file, parse YAML frontmatter, display current state (hypothesis, findings, timeline)
     - If not exists: create new file with YAML frontmatter (status: investigating, created: ISO8601, updated: ISO8601) and empty sections (Current Focus, Symptoms, Evidence, Eliminated, Resolution)
   - Instruct Claude to investigate the issue systematically:
     - Read relevant source files, git log, error outputs
     - Update the debug file: append new `## Session {N}` block with findings
     - Update frontmatter `updated` timestamp
     - Use tmp+rename atomic write pattern per Pitfall 3
   - If repair.js is available, call selectStrategy to suggest next repair action
   - Per D-07: accept optional `--from-uat {gap-id}` flag to link debug session to a UAT verification failure

2. Create `commands/forensics.md` per D-06:
   - Read-only diagnostic command -- explicitly state NO Write, NO Edit, NO Bash mutations, NO commits
   - Spawns a subagent with restricted allowed-tools: Read, Bash (read-only: git log, git diff, git show, grep, cat, ls only)
   - Subagent analyzes: git history around the issue, error patterns in logs, state file inconsistencies
   - Writes report to `.planning/debug/forensics/{slug}-{timestamp}.md`
   - Report includes: timeline, root cause hypothesis, affected files, recommended fix (but does NOT implement it)
   - Per Pitfall 4: forensics.md frontmatter must restrict tools explicitly
  </action>
  <verify>
    <automated>grep -q "debug" commands/debug.md && grep -q ".planning/debug/" commands/debug.md && grep -q "forensics" commands/forensics.md && grep -q "Read" commands/forensics.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "slug" commands/debug.md returns at least 1 line
    - grep "renameSync\|tmp.*rename\|atomic" commands/debug.md returns at least 1 line
    - grep -i "read.only\|no.*write\|no.*edit" commands/forensics.md returns at least 1 line
    - grep "allowed.tools\|Read.*Bash" commands/forensics.md returns at least 1 line
  </acceptance_criteria>
  <done>debug.md creates/appends persistent debug state files with atomic writes. forensics.md produces read-only reports with restricted subagent tools. Both commands reference .planning/debug/ directory.</done>
</task>

</tasks>

<verification>
- repair.js passes inline node -e unit test for all three strategy thresholds
- debug.md references .planning/debug/{slug}.md pattern and atomic writes
- forensics.md explicitly restricts tools and writes to .planning/debug/forensics/
- No npm dependencies added
</verification>

<success_criteria>
Users can debug systematically with persistent state, run read-only forensics, and failed tasks auto-repair within the 2-retry/1-decompose budget before escalating to human.
</success_criteria>

<output>
After completion, create `.planning/phases/36-human-tasks-debugging/36-02-SUMMARY.md`
</output>
