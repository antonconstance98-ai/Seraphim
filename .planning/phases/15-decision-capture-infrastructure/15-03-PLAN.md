---
phase: 15-decision-capture-infrastructure
plan: "03"
type: execute
wave: 1
depends_on: ["15-01", "15-02"]
files_modified:
  - ~/.claude/hooks/codex-review-gate.js
  - ~/.claude/hooks/minimax-post-scan.js
  - ~/.claude/hooks/decision-logger.js
autonomous: true
gap_closure: true
requirements: [DCAP-01, DCAP-02]

must_haves:
  truths:
    - "token-log records from codex-review-gate.js contain an outcome field (initially null)"
    - "token-log records from minimax-post-scan.js contain an outcome field (initially null)"
    - "decision-logger.js Stop ALLOW records contain non-null review_model_latency_ms when review-gate ran"
  artifacts:
    - path: "~/.claude/hooks/codex-review-gate.js"
      provides: "outcome: null in both Codex and MiniMax token-log records; review_model_latency_ms in shared state"
      contains: "outcome: null"
    - path: "~/.claude/hooks/minimax-post-scan.js"
      provides: "outcome: null in token-log record"
      contains: "outcome: null"
    - path: "~/.claude/hooks/decision-logger.js"
      provides: "review_model_latency_ms read from per-model keys"
      contains: "review_codex_latency_ms"
  key_links:
    - from: "codex-review-gate.js"
      to: "hook-signal shared state"
      via: "writeHookSignal review_model_latency_ms"
      pattern: "writeHookSignal.*review_model_latency_ms"
    - from: "decision-logger.js"
      to: "hook-signal shared state"
      via: "reads review_model_latency_ms key"
      pattern: "state\\.review_model_latency_ms"
---

<objective>
Close two verification gaps from Phase 15:

1. Add `outcome: null` to token-log records written by codex-review-gate.js and minimax-post-scan.js (DCAP-01). Outcome is null at write time because it can only be determined later (via dismiss/commit signals). This aligns token-log records with ROADMAP SC1.

2. Fix the review_model_latency_ms key mismatch (DCAP-02). Review-gate writes per-model keys (review_codex_latency_ms, review_minimax_latency_ms) but decision-logger reads review_model_latency_ms. Fix: add a writeHookSignal for review_model_latency_ms in review-gate (max of both), so decision-logger reads it correctly.

Gap 3 (adaptive:false enforcement, DCAP-05) is INTENTIONALLY DEFERRED to Phases 16-17. SC4 is accepted as-is.

Purpose: Ensure all token-log records have a consistent schema and decision-log ALLOW events have accurate latency data.
Output: Three patched hook files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-decision-capture-infrastructure/15-01-SUMMARY.md
@.planning/phases/15-decision-capture-infrastructure/15-VERIFICATION.md

<interfaces>
<!-- Key functions and patterns the executor needs. -->

From ~/.claude/hooks/hook-signal.js:
```javascript
function writeHookSignal(cwd, eventId, key, value)  // appends {key: value} to .hook-state/{eventId}.jsonl
function readHookState(cwd, eventId)                 // merges all lines, last wins per key
```

From ~/.claude/hooks/codex-review-gate.js (lines 217-221):
```javascript
writeHookSignal(cwd, eventId, 'review_verdict', shouldBlock ? 'BLOCK' : 'ALLOW');
writeHookSignal(cwd, eventId, 'review_block_category', shouldBlock ? classifyBlockCategory(blockSummary) : null);
writeHookSignal(cwd, eventId, 'review_codex_latency_ms', codexModelLatencyMs);
writeHookSignal(cwd, eventId, 'review_minimax_latency_ms', minimaxModelLatencyMs);
writeHookSignal(cwd, eventId, 'review_hook_latency_ms', Date.now() - hookStartMs);
```

From ~/.claude/hooks/decision-logger.js (line 140):
```javascript
review_model_latency_ms: state.review_model_latency_ms ?? null,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add outcome field to review-gate and post-scan token-log records, add review_model_latency_ms signal</name>
  <files>~/.claude/hooks/codex-review-gate.js, ~/.claude/hooks/minimax-post-scan.js</files>
  <read_first>
    - ~/.claude/hooks/codex-review-gate.js (lines 148-221 — both token-log record blocks and shared state signals)
    - ~/.claude/hooks/minimax-post-scan.js (lines 351-378 — token-log record block)
  </read_first>
  <action>
**codex-review-gate.js — two changes:**

1. Add `outcome: null` to BOTH token-log record objects:
   - In the Codex token-log record (around line 150-172): add `outcome: null` as a field in the `record` object, after the `rate_limit_pct` line (line 167) and before the Phase 15 latency comment (line 169). The exact line to add is:
     ```javascript
     outcome: null,
     ```
   - In the MiniMax token-log record (around line 184-204): add `outcome: null` as a field in the `record` object, after the `cost_usd` line (line 199) and before the Phase 15 latency comment (line 201). The exact line to add is:
     ```javascript
     outcome: null,
     ```

2. Add a 6th writeHookSignal call for `review_model_latency_ms` after the existing 5 signals (after line 221). Compute max of both model latencies, handling null/undefined gracefully:
   ```javascript
   const reviewModelLatencyMs = Math.max(codexModelLatencyMs || 0, minimaxModelLatencyMs || 0) || null;
   writeHookSignal(cwd, eventId, 'review_model_latency_ms', reviewModelLatencyMs);
   ```
   Use `|| null` at the end so that if both are 0/null, the value is null rather than 0.

**minimax-post-scan.js — one change:**

1. Add `outcome: null` to the token-log record object (around line 355-367): add `outcome: null` as a field after the `cost_usd` line (line 366) and before the Phase 15 latency comment (line 368). The exact line to add is:
   ```javascript
   outcome: null,
   ```
  </action>
  <verify>
    <automated>grep -n "outcome: null" ~/.claude/hooks/codex-review-gate.js | wc -l | grep -q "2" && grep -n "outcome: null" ~/.claude/hooks/minimax-post-scan.js | wc -l | grep -q "1" && grep -n "review_model_latency_ms" ~/.claude/hooks/codex-review-gate.js | wc -l | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "outcome: null" ~/.claude/hooks/codex-review-gate.js returns exactly 2 matches (one per token-log record)
    - grep "outcome: null" ~/.claude/hooks/minimax-post-scan.js returns exactly 1 match
    - grep "review_model_latency_ms" ~/.claude/hooks/codex-review-gate.js returns exactly 1 match (the writeHookSignal call)
    - node -e "require('/home/alucard/.claude/hooks/codex-review-gate.js')" does NOT throw (syntax valid)
    - node -e "require('/home/alucard/.claude/hooks/minimax-post-scan.js')" does NOT throw (syntax valid)
  </acceptance_criteria>
  <done>Both review-gate token-log records have outcome: null; post-scan token-log record has outcome: null; review-gate writes review_model_latency_ms to shared state as max of per-model latencies.</done>
</task>

<task type="auto">
  <name>Task 2: Verify decision-logger reads review_model_latency_ms correctly (no change needed if Task 1 wires it)</name>
  <files>~/.claude/hooks/decision-logger.js</files>
  <read_first>
    - ~/.claude/hooks/decision-logger.js (lines 125-155 — Stop record construction)
  </read_first>
  <action>
**Verify only — decision-logger.js already reads `state.review_model_latency_ms` at line 140.** Task 1 adds the matching writeHookSignal in review-gate, so the key mismatch is resolved without changing decision-logger.

Run the verification commands to confirm the wiring is complete end-to-end:
1. Confirm decision-logger reads `review_model_latency_ms` from state (should already exist at line 140)
2. Confirm review-gate now writes `review_model_latency_ms` to shared state (added in Task 1)
3. Run a syntax check on all three modified/verified files

If decision-logger.js line 140 does NOT contain `state.review_model_latency_ms`, then the file was changed since verification. In that case, add the field back:
```javascript
review_model_latency_ms: state.review_model_latency_ms ?? null,
```
But this should not be needed — the file already has this line.
  </action>
  <verify>
    <automated>grep -n "state.review_model_latency_ms" ~/.claude/hooks/decision-logger.js | grep -q "review_model_latency_ms" && grep -n "writeHookSignal.*review_model_latency_ms" ~/.claude/hooks/codex-review-gate.js | grep -q "review_model_latency_ms" && echo "WIRED" || echo "BROKEN"</automated>
  </verify>
  <acceptance_criteria>
    - grep "state.review_model_latency_ms" ~/.claude/hooks/decision-logger.js returns at least 1 match
    - grep "writeHookSignal.*review_model_latency_ms" ~/.claude/hooks/codex-review-gate.js returns at least 1 match
    - The key written by review-gate matches the key read by decision-logger (both are "review_model_latency_ms")
  </acceptance_criteria>
  <done>decision-logger.js reads review_model_latency_ms from shared state; codex-review-gate.js writes review_model_latency_ms to shared state; key names match; ALLOW events in decision-log will have non-null review latency when review-gate ran.</done>
</task>

</tasks>

<verification>
End-to-end gap closure verification:

1. **DCAP-01 (outcome field):**
   - `grep -c "outcome: null" ~/.claude/hooks/codex-review-gate.js` returns `2`
   - `grep -c "outcome: null" ~/.claude/hooks/minimax-post-scan.js` returns `1`

2. **DCAP-02 (latency key mismatch):**
   - `grep "writeHookSignal.*review_model_latency_ms" ~/.claude/hooks/codex-review-gate.js` returns 1 match
   - `grep "state.review_model_latency_ms" ~/.claude/hooks/decision-logger.js` returns 1 match
   - Both reference the same key string `review_model_latency_ms`

3. **Syntax validation (all three files load without error):**
   - `node -e "require('/home/alucard/.claude/hooks/codex-review-gate.js')" 2>&1; echo $?` returns 0 or the module loads (may print to stderr for missing stdin but should not throw a SyntaxError)
   - `node -e "require('/home/alucard/.claude/hooks/minimax-post-scan.js')" 2>&1; echo $?`
   - `node -e "require('/home/alucard/.claude/hooks/decision-logger.js')" 2>&1; echo $?`

4. **DCAP-05 (adaptive:false) — INTENTIONALLY DEFERRED:** SC4 enforcement is a Phase 16-17 responsibility. No action taken in this plan.
</verification>

<success_criteria>
- All token-log records from review-gate and post-scan include `outcome: null`
- review-gate writes `review_model_latency_ms` (max of per-model latencies) to shared state
- decision-logger reads `review_model_latency_ms` from shared state (already wired)
- No syntax errors in any modified file
- Gap 3 (DCAP-05 enforcement) explicitly not addressed (deferred by design)
</success_criteria>

<output>
After completion, create `.planning/phases/15-decision-capture-infrastructure/15-03-SUMMARY.md`
</output>
