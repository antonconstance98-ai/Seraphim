---
phase: 34-research-session-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/pause.md
  - ~/.claude/plugins/seraphim/commands/resume.md
  - ~/.claude/plugins/seraphim/commands/session-report.md
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03]

must_haves:
  truths:
    - "User can run /seraphim:pause and find HANDOFF.json + .continue-here.md in .seraphim/"
    - "User can run /seraphim:resume and have context restored then handoff files deleted"
    - "User can run /seraphim:session-report and get a session summary written to .planning/session-reports/"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/pause.md"
      provides: "Session-level pause with HANDOFF.json capture"
    - path: "~/.claude/plugins/seraphim/commands/resume.md"
      provides: "Session resume with context injection and file cleanup"
    - path: "~/.claude/plugins/seraphim/commands/session-report.md"
      provides: "Session report generation from git log + token estimation"
  key_links:
    - from: "pause.md"
      to: ".seraphim/HANDOFF.json"
      via: "node -e JSON write"
      pattern: "HANDOFF\\.json"
    - from: "resume.md"
      to: ".seraphim/HANDOFF.json"
      via: "node -e JSON read + delete"
      pattern: "HANDOFF\\.json"
---

<objective>
Build session management commands: pause (captures full context), resume (restores and cleans up), and session-report (generates work summary).

Purpose: Users can pause work mid-session and resume in a future session without losing context. Session reports provide accountability and token tracking.
Output: Rewritten pause.md + resume.md + new session-report.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-research-session-navigation/34-CONTEXT.md
@.planning/phases/34-research-session-navigation/34-RESEARCH.md

@~/.claude/plugins/seraphim/commands/pause.md
@~/.claude/plugins/seraphim/commands/resume.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pause.md and resume.md for session-level handoff</name>
  <files>~/.claude/plugins/seraphim/commands/pause.md, ~/.claude/plugins/seraphim/commands/resume.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/pause.md (existing pipeline-scoped implementation to replace)
    - ~/.claude/plugins/seraphim/commands/resume.md (existing pipeline-scoped implementation to replace)
    - ~/.claude/plugins/seraphim/lib/phase-state.js (understand existing state reading for backward compat)
    - ~/.claude/plugins/seraphim/commands/discuss.md (project root resolution pattern)
  </read_first>
  <action>
**pause.md** (per D-05, SESS-01):
- Frontmatter: `description: "Pause work and capture full session context for later resumption"`, no required arguments (session-level, not pipeline-scoped), `allowed-tools: ["Read", "Bash"]`
- Step 1: Resolve project root (walk up for `.seraphim/config.json`)
- Step 2: Check for existing HANDOFF.json — if exists, warn user about existing pause state and ask to confirm overwrite (anti-pattern from RESEARCH.md pitfall 2 analogy)
- Step 3: Gather context via bash:
  - Read STATE.md frontmatter for current phase number
  - Check `.planning/phases/{phase}-*/` for current plan files (find any in-progress plans)
  - Run `git status --short` for uncommitted changes
  - Run `git branch --show-current` for active branch
  - Read STATE.md decisions/blockers sections for open decisions
- Step 4: Write HANDOFF.json to `.seraphim/HANDOFF.json` with schema per D-05:
  ```json
  {
    "captured_at": "ISO",
    "phase": "34",
    "plan": "34-01-PLAN.md or null",
    "task_position": "human-readable position or null",
    "active_branch": "master",
    "uncommitted_changes": ["M file.txt"],
    "open_decisions": [],
    "next_suggested_command": "/seraphim:next"
  }
  ```
- Step 5: Write `.seraphim/.continue-here.md` — plain English summary: what was being worked on, where it stopped, exact command to resume, any uncommitted changes to be aware of
- Step 6: Confirm to user with summary of captured state

**resume.md** (per D-06, SESS-02):
- Frontmatter: `description: "Resume work from a previous session"`, no required arguments, `allowed-tools: ["Read", "Bash"]`
- Step 1: Resolve project root
- Step 2: Read `.seraphim/HANDOFF.json` — if not found, tell user "No paused session found. Nothing to resume."
- Step 3: Delete HANDOFF.json and .continue-here.md IMMEDIATELY after reading (per pitfall 5 in RESEARCH.md — delete before injecting context to avoid stale data on crash)
- Step 4: Display resume banner with all captured context: phase, plan, task position, uncommitted changes, open decisions
- Step 5: Suggest next command from `next_suggested_command` field
  </action>
  <verify>
    <automated>grep -q "HANDOFF.json" ~/.claude/plugins/seraphim/commands/pause.md && grep -q "HANDOFF.json" ~/.claude/plugins/seraphim/commands/resume.md && grep -q "continue-here" ~/.claude/plugins/seraphim/commands/pause.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "HANDOFF.json" ~/.claude/plugins/seraphim/commands/pause.md
    - grep -q "continue-here.md" ~/.claude/plugins/seraphim/commands/pause.md
    - grep -q "HANDOFF.json" ~/.claude/plugins/seraphim/commands/resume.md
    - grep -q "No paused session" ~/.claude/plugins/seraphim/commands/resume.md
    - grep -q "captured_at" ~/.claude/plugins/seraphim/commands/pause.md
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/pause.md
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/resume.md
  </acceptance_criteria>
  <done>pause.md captures full session context to HANDOFF.json + .continue-here.md; resume.md reads, deletes files immediately, then displays context and suggests next action</done>
</task>

<task type="auto">
  <name>Task 2: Create session-report.md command</name>
  <files>~/.claude/plugins/seraphim/commands/session-report.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/discuss.md (command structure pattern)
    - .planning/session-reports/ (check existing report format if any exist)
  </read_first>
  <action>
**session-report.md** (per D-07, SESS-03):
- Frontmatter: `description: "Generate a session work summary with commits, files, and token estimate"`, `allowed-tools: ["Read", "Write", "Bash"]`
- Step 1: Resolve project root
- Step 2: Gather git data:
  - `git log --since="today" --oneline` for today's commits
  - `git log --since="today" --format="%H" | wc -l` for commit count
  - `git log --since="today" --stat --format=""` for files changed summary
- Step 3: Estimate token spend — find most recent JSONL in `~/.claude/projects/` for this project. The project hash is derived from the project root path (replace `/` with `-`, strip leading `-`). Read the latest `.jsonl` file, sum `usage.input_tokens` and `usage.output_tokens` for records with today's date.
- Step 4: Generate timestamp `YYYY-MM-DD-HHMM` for filename
- Step 5: Create `.planning/session-reports/` directory if missing
- Step 6: Write report to `.planning/session-reports/{timestamp}.md` with sections:
  - **Summary**: one-paragraph overview of what was accomplished
  - **Commits**: list of commits from today
  - **Files Changed**: summary of modified files
  - **Token Estimate**: input tokens, output tokens, total
  - **Duration**: session start to now (estimate from first commit timestamp)
- Step 7: Display report path and brief summary to user
  </action>
  <verify>
    <automated>grep -q "session-report" ~/.claude/plugins/seraphim/commands/session-report.md && grep -q "git log" ~/.claude/plugins/seraphim/commands/session-report.md && grep -q "description:" ~/.claude/plugins/seraphim/commands/session-report.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/session-report.md
    - grep -q "git log" ~/.claude/plugins/seraphim/commands/session-report.md
    - grep -q "session-reports" ~/.claude/plugins/seraphim/commands/session-report.md
    - grep -q "input_tokens" ~/.claude/plugins/seraphim/commands/session-report.md
  </acceptance_criteria>
  <done>session-report.md generates work summary from git log + token estimation, writes to .planning/session-reports/</done>
</task>

</tasks>

<verification>
1. pause.md writes HANDOFF.json with all D-05 fields
2. resume.md deletes handoff files before context injection (pitfall 5)
3. session-report.md reads git log and estimates tokens
4. All three commands have valid YAML frontmatter
</verification>

<success_criteria>
- pause.md captures session-level context (not pipeline-scoped) per D-05
- resume.md restores context and cleans up per D-06
- session-report.md generates summary with commits + token estimate per D-07
- No required arguments on pause/resume (session-level, not pipeline-specific)
</success_criteria>

<output>
After completion, create `.planning/phases/34-research-session-navigation/34-02-SUMMARY.md`
</output>
