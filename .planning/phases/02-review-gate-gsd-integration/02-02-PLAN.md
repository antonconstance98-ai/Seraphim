---
phase: 02-review-gate-gsd-integration
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ~/.claude/hooks/codex-wave-validator.js
  - ~/.claude/hooks/codex-wave-validator-worker.js
  - ~/.claude/hooks/codex-plan-reviewer.js
  - ~/.claude/settings.json
autonomous: true
requirements:
  - GSD-01
  - GSD-02
  - GSD-03

must_haves:
  truths:
    - "After all plans in a wave complete (all have SUMMARY.md), Codex validation is dispatched automatically"
    - "Wave validation runs non-blocking — Claude continues executing the next wave while validation runs in the background"
    - "Critical issues from wave validation halt the next wave before any plan in it starts"
    - "Non-critical issues from wave validation surface as advisory context at the next natural stopping point"
    - "Wave validation results are persisted to .planning/wave-N-validation.json so they survive session boundaries"
    - "Plan-phase finalization is blocked until Codex reviews — enforced via a SubagentStop hook on gsd-planner subagents that calls codex-plan-reviewer.js and blocks on issues (per D-07)"
    - "The REVIEWS.md filename matches GSD's detection pattern so has_reviews and reviews_path populate correctly"
  artifacts:
    - path: "~/.claude/hooks/codex-wave-validator.js"
      provides: "PostToolUse hook detecting wave completion and dispatching non-blocking Codex validation"
      min_lines: 130
    - path: "~/.claude/hooks/codex-wave-validator-worker.js"
      provides: "Standalone background worker script spawned by codex-wave-validator.js for non-blocking Codex invocation"
      min_lines: 60
    - path: "~/.claude/hooks/codex-plan-reviewer.js"
      provides: "Standalone script that reviews PLAN.md files via Codex and writes REVIEWS.md"
      min_lines: 80
    - path: "~/.claude/settings.json"
      provides: "codex-wave-validator.js registered in PostToolUse, codex-plan-reviewer.js registered as SubagentStop hook for plan-phase enforcement (D-07)"
      contains: "codex-wave-validator.js"
  key_links:
    - from: "~/.claude/hooks/codex-wave-validator.js"
      to: "~/.claude/get-shit-done/bin/gsd-tools.cjs"
      via: "execSync phase-plan-index query"
      pattern: "phase-plan-index"
    - from: "~/.claude/hooks/codex-wave-validator.js"
      to: "~/.claude/hooks/codex-wave-validator-worker.js"
      via: "spawn detached child process"
      pattern: "codex-wave-validator-worker\\.js"
    - from: "~/.claude/hooks/codex-wave-validator-worker.js"
      to: "~/.claude/hooks/codex-exec.js"
      via: "require('./codex-exec') for Codex invocation"
      pattern: "require.*codex-exec"
    - from: "~/.claude/hooks/codex-wave-validator.js"
      to: ".planning/wave-N-validation.json"
      via: "sentinel file for result persistence"
      pattern: "wave-.*-validation\\.json"
    - from: "~/.claude/hooks/codex-plan-reviewer.js"
      to: ".planning/phases/*/NN-REVIEWS.md"
      via: "writes review file matching GSD detection pattern"
      pattern: "REVIEWS\\.md"
    - from: "~/.claude/settings.json"
      to: "~/.claude/hooks/codex-plan-reviewer.js"
      via: "SubagentStop hook registration for plan-phase enforcement (D-07)"
      pattern: "codex-plan-reviewer"
---

<objective>
Create the GSD wave-boundary validation hook, background worker script, plan-phase review script, and enforce plan-phase review blocking via SubagentStop hook. After this plan, every GSD wave completion triggers automatic Codex validation, and plan finalization is blocked until Codex reviews per D-07.

Purpose: Catches integration issues between plans at wave boundaries before the next wave starts, and catches spec gaps in plans before execution begins. Both provide early feedback when changes are cheapest. The SubagentStop enforcement mechanism ensures D-07 (plan-phase blocked until Codex reviews) is not optional.

Output: `codex-wave-validator.js` (PostToolUse hook), `codex-wave-validator-worker.js` (background worker), `codex-plan-reviewer.js` (standalone script + SubagentStop hook), updated `settings.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-gate-gsd-integration/02-CONTEXT.md
@.planning/phases/02-review-gate-gsd-integration/02-RESEARCH.md
@.planning/phases/02-review-gate-gsd-integration/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from Phase 1 and Phase 2 Plan 01 artifacts. -->

From ~/.claude/hooks/codex-exec.js:
```javascript
function runCodexExec(prompt, options) -> Promise<{success, exitCode, output, tokens, rateLimitPct, error}>
  // options: { cwd, timeoutMs (default 300000), model (default 'gpt-5.4') }
function parseCodexTokens(jsonlOutput) -> {input_tokens, cached_input_tokens, output_tokens, ...} | null
function computeCost(tokens, model) -> number
```

From gsd-tools.cjs phase-plan-index output:
```json
{
  "phase": "02",
  "plans": ["02-01", "02-02"],
  "waves": { "1": ["02-01"], "2": ["02-02"] },
  "incomplete": ["02-02"],
  "has_checkpoints": false
}
```
Wave N is complete when: waves[N].every(id => !incomplete.includes(id))

GSD REVIEWS.md detection (from init.cjs line 231):
- Pattern: files ending with `-REVIEWS.md` or exactly `REVIEWS.md`
- Naming convention: `{padded_phase}-REVIEWS.md` (e.g., `02-REVIEWS.md`)
- Location: phase directory (e.g., `.planning/phases/02-review-gate-gsd-integration/`)

Token log JSONL record schema (from codex-token-logger.js):
```javascript
{
  timestamp, session_id, model, source, task_type,
  tokens: { input, cached_input, output, reasoning_output },
  cost_usd, rate_limit_pct
}
```

Wave validation result file schema (new, defined here):
```json
{
  "phase": "02",
  "wave": 1,
  "status": "pass|critical|advisory",
  "timestamp": "ISO string",
  "pid": 12345,
  "plans_validated": ["02-01"],
  "issues": [
    { "severity": "critical|advisory", "description": "..." }
  ],
  "summary": "one-line summary"
}
```

Current ~/.claude/settings.json PostToolUse entry:
```json
"PostToolUse": [{
  "matcher": "Bash|Edit|Write|MultiEdit|Agent|Task",
  "hooks": [
    { "type": "command", "command": "node \".../gsd-context-monitor.js\"", "timeout": 10 },
    { "type": "command", "command": "node \".../codex-token-logger.js\"", "timeout": 10 }
  ]
}]
```
New codex-wave-validator.js will be added as the 3rd hook in this list.

SubagentStop hook stdin schema (from Claude Code docs — same as Stop hook):
```json
{
  "session_id": "abc123",
  "transcript_path": "/home/.../.claude/projects/.../transcript.jsonl",
  "cwd": "/home/...",
  "hook_event_name": "SubagentStop",
  "stop_hook_active": false
}
```
Note: SubagentStop fires when a subagent (like gsd-planner) completes. It CAN block via `{ "decision": "block", "reason": "..." }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create codex-wave-validator.js PostToolUse hook and codex-wave-validator-worker.js background worker</name>
  <files>~/.claude/hooks/codex-wave-validator.js, ~/.claude/hooks/codex-wave-validator-worker.js</files>
  <read_first>
    - ~/.claude/hooks/codex-exec.js (runCodexExec for Codex invocation)
    - ~/.claude/hooks/gsd-context-monitor.js (reference PostToolUse hook pattern — stdin timeout, additionalContext output, fail-silent)
    - ~/.claude/hooks/codex-token-logger.js (token logging pattern — record schema, appendFileSync)
    - .planning/phases/02-review-gate-gsd-integration/02-RESEARCH.md (Pattern 4: GSD Wave-Boundary Validation — wave detection, non-blocking spawn, sentinel files)
  </read_first>
  <action>
**File 1: Create `~/.claude/hooks/codex-wave-validator-worker.js`** — a standalone background worker that the wave validator spawns for non-blocking Codex invocation. This replaces the fragile inline shell-escaped Node.js approach.

**Header (lines 1-15):**
- Shebang: `#!/usr/bin/env node`
- Header: `// codex-hook-version: 2.0.0` and `// Codex Wave Validator Worker — Background process`
- Comment: `// Spawned by codex-wave-validator.js for non-blocking wave validation`
- Comment: `// Usage: node codex-wave-validator-worker.js --phase 02 --wave 1 --cwd /path --sentinel /path/wave-1-validation.json`
- `'use strict';`
- Require: `fs`, `path`
- Require: `./codex-exec` (runCodexExec, parseCodexTokens, computeCost)

**Argument parsing (lines 16-30):**
- Parse `--phase`, `--wave`, `--cwd`, `--sentinel`, `--prompt-file` from `process.argv`
- `--prompt-file` points to a temp file containing the validation prompt (avoids shell escaping issues)
- Read the prompt from the prompt file, then delete the prompt file
- Validate all args present, exit 1 if missing

**Codex invocation (lines 31-55):**
- Call `await runCodexExec(prompt, { cwd, timeoutMs: 180000, model: 'gpt-5.4' })`
- Extract text from JSONL output: parse each line as JSON, collect `event_msg` with `payload.type === 'message'`, concatenate `payload.content` or `payload.text`
- Parse verdict: scan all text lines for `/^PASS/i`, `/^CRITICAL/i`, `/^ADVISORY/i`
- Collect individual issues: lines matching `/^\[CRITICAL\]/i` or `/^\[ADVISORY\]/i`

**Sentinel file update (lines 56-80):**
- Read existing sentinel JSON from `--sentinel` path
- Update: `status` (pass/critical/advisory), `summary`, `issues` array, `timestamp`
- Write updated sentinel back to disk

**Token logging (lines 81-100):**
- If `result.tokens` is not null:
  - Build JSONL record with `task_type: 'wave-validation'`
  - Use same schema as codex-token-logger.js: timestamp, session_id (null for background), model, source, task_type, tokens, cost_usd, rate_limit_pct
  - Append to `path.join(cwd, '.planning', 'token-log.jsonl')`

**Error handling:**
- Entire main body in async IIFE with try/catch
- On error, update sentinel to `{ status: 'advisory', summary: 'Validation failed: ' + error.message }`
- Write error to stderr
- Exit 0 (background process, no one to block)

---

**File 2: Create `~/.claude/hooks/codex-wave-validator.js`** — a PostToolUse hook that detects wave completion and dispatches non-blocking Codex validation by spawning the worker.

**Header and requires (lines 1-20):**
- Shebang: `#!/usr/bin/env node`
- Header: `// codex-hook-version: 2.0.0` and `// Codex Wave Validator — PostToolUse hook`
- Comment: `// Required by: GSD-01 (wave-boundary validation), GSD-02 (non-blocking background validation)`
- `'use strict';`
- Require: `fs`, `path`, `child_process` (execSync, spawn)
- Constants:
  - `const GSD_TOOLS = '/home/alucard/.claude/get-shit-done/bin/gsd-tools.cjs';`
  - `const STALE_SECONDS = 600;` (10 minutes — delete validation results older than this)

**Stdin parsing (lines 21-35):**
- Stdin timeout: `const stdinTimeout = setTimeout(() => process.exit(0), 10000);` (10s for PostToolUse)
- Standard stdin read pattern (same as gsd-context-monitor.js)
- Parse JSON

**Step 1 — Detect SUMMARY.md write (lines 36-55):**
- Check `data.tool_name` is one of `Write`, `Edit`, `Bash`
- Check `data.tool_input` or `data.tool_result` for a filepath containing `SUMMARY.md`
  - For Write/Edit: check `data.tool_input.file_path` contains `SUMMARY.md`
  - For Bash: check `data.tool_result` contains `SUMMARY.md` (from a write/cat/mv command)
- If no SUMMARY.md detected, proceed to Step 1b (check for pending results)
- Extract `cwd` from `data.cwd`

**Step 1b — Check for pending validation results (lines 56-95):**
- Before exiting (on any PostToolUse call), check if a completed wave validation result exists that hasn't been surfaced yet
- List `.planning/wave-*-validation.json` files in `data.cwd`
- For each file, read and parse JSON
- If `status === 'critical'`, inject as `additionalContext`:
  ```
  [WAVE VALIDATION CRITICAL] Wave {N} validation found critical issues:
  {issues list}
  DO NOT start wave {N+1} until these are resolved.
  ```
- If `status === 'advisory'`, inject as `additionalContext`:
  ```
  [WAVE VALIDATION] Wave {N} completed. Advisory: {summary}
  ```
- If `status === 'pass'`, inject: `[WAVE VALIDATION] Wave {N} passed Codex review.`
- After surfacing, rename the file to `wave-N-validation.surfaced.json` (prevents re-surfacing)
- If no pending results, and no SUMMARY.md detected, `process.exit(0)`

**Step 2 — Query wave status (lines 96-120):**
- Run `execSync('node "' + GSD_TOOLS + '" phase-plan-index ' + phase, { cwd, timeout: 10000 })`
- Extract `phase` from the SUMMARY.md path: match `/(\\d{2})-\\d{2}-SUMMARY\\.md/` to get the phase number (e.g., `02`)
- Parse result as JSON: `{ waves, incomplete }`
- Determine which wave the just-completed plan belongs to
- Check if the entire wave is now complete: `wavePlans.every(id => !waveStatus.incomplete.includes(id))`
- If wave not complete, `process.exit(0)` (more plans in this wave still pending)

**Step 3 — Dispatch non-blocking Codex validation (lines 121-175):**
- Collect all SUMMARY.md contents for plans in the completed wave:
  - For each plan ID in the wave group, read `{phaseDir}/{planId}-SUMMARY.md`
  - Concatenate all summaries (truncate combined to 6000 chars)
- Build validation prompt:
  ```
  You are reviewing the output of wave {N} in a GSD phase execution.
  
  Validate the following completed plan summaries for:
  1. CRITICAL issues: broken imports, security flaws, missing required files, undefined references
  2. ADVISORY issues: minor inconsistencies, style issues, optimization opportunities
  
  Output format (FIRST LINE must be one of):
  - "PASS" if no issues found
  - "CRITICAL: <one-line summary>" if critical issues exist
  - "ADVISORY: <one-line summary>" if only advisory issues exist
  
  Then list each issue on its own line prefixed with [CRITICAL] or [ADVISORY].
  
  Plan summaries:
  {summaries}
  ```
- Write sentinel file BEFORE spawning: `.planning/wave-{N}-validation.json` with:
  ```json
  { "phase": "{phase}", "wave": {N}, "status": "pending", "timestamp": "ISO", "pid": null, "plans_validated": [...], "issues": [], "summary": "" }
  ```
- Write validation prompt to a temp file: `.planning/wave-{N}-prompt.tmp`
- Spawn worker as a detached child process:
  ```javascript
  const child = spawn('node', [
    path.join(__dirname, 'codex-wave-validator-worker.js'),
    '--phase', phase,
    '--wave', String(waveNum),
    '--cwd', cwd,
    '--sentinel', sentinelPath,
    '--prompt-file', promptFilePath
  ], {
    cwd,
    env: process.env,
    stdio: 'ignore',
    detached: true
  });
  child.unref();
  ```
- Update sentinel with PID: read sentinel, set `pid: child.pid`, write back
- Output advisory context: `{ additionalContext: '[WAVE COMPLETE] Wave {N} finished. Codex validation dispatched (non-blocking). Results will surface at next stopping point.' }`

**Stale sentinel cleanup (in Step 1b):**
- When checking for pending results, also delete any `.surfaced.json` files older than STALE_SECONDS

**Error handling:**
- Entire main body in try/catch
- On any error, `process.exit(0)` (never block tool execution)
- Write errors to stderr for debugging
  </action>
  <verify>
    <automated>node -c /home/alucard/.claude/hooks/codex-wave-validator.js && node -c /home/alucard/.claude/hooks/codex-wave-validator-worker.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/hooks/codex-wave-validator.js
    - File exists at ~/.claude/hooks/codex-wave-validator-worker.js
    - Both files start with `#!/usr/bin/env node`
    - codex-wave-validator.js contains `codex-hook-version: 2.0.0`
    - codex-wave-validator.js contains `GSD-01` and `GSD-02` in comments
    - codex-wave-validator.js contains string `phase-plan-index` (gsd-tools.cjs query)
    - codex-wave-validator.js contains string `SUMMARY.md` (trigger detection)
    - codex-wave-validator.js contains string `wave-` and `validation.json` (sentinel file pattern)
    - codex-wave-validator.js contains `detached: true` and `child.unref()` (non-blocking spawn per D-08)
    - codex-wave-validator.js spawns `codex-wave-validator-worker.js` (not inline shell)
    - codex-wave-validator.js contains string `CRITICAL` and `ADVISORY` (issue severity parsing in Step 1b)
    - codex-wave-validator.js contains `process.exit(0)` in outer catch block (fail-silent)
    - codex-wave-validator.js contains `stdinTimeout` with value `10000` (10s PostToolUse timeout)
    - codex-wave-validator-worker.js contains `require('./codex-exec')` importing runCodexExec
    - codex-wave-validator-worker.js contains string `token-log.jsonl` (token logging)
    - codex-wave-validator-worker.js contains `task_type` with value matching `wave-validation`
    - codex-wave-validator-worker.js parses `--phase`, `--wave`, `--cwd`, `--sentinel`, `--prompt-file` args
    - Both files pass `node -c` syntax check (exits 0)
    - codex-wave-validator.js is at least 130 lines long
    - codex-wave-validator-worker.js is at least 60 lines long
  </acceptance_criteria>
  <done>
    codex-wave-validator.js detects wave completion via SUMMARY.md writes, queries gsd-tools phase-plan-index for wave status, dispatches non-blocking Codex validation via codex-wave-validator-worker.js (dedicated worker file, not inline shell), persists results to sentinel files, and surfaces critical/advisory findings as additionalContext at the next tool call. Worker handles Codex invocation, verdict parsing, sentinel update, and token logging independently.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create codex-plan-reviewer.js script, register hooks, and enforce D-07 plan-phase block via SubagentStop</name>
  <files>~/.claude/hooks/codex-plan-reviewer.js, ~/.claude/settings.json</files>
  <read_first>
    - ~/.claude/hooks/codex-exec.js (runCodexExec for Codex invocation)
    - ~/.claude/hooks/codex-token-logger.js (token logging record schema)
    - ~/.claude/settings.json (current PostToolUse hook list — add wave-validator as 3rd entry; add SubagentStop for plan reviewer)
    - .planning/phases/02-review-gate-gsd-integration/02-RESEARCH.md (Pattern 3: GSD Plan-Phase Review Checkpoint, GSD Plan-Phase Integration Points)
  </read_first>
  <action>
**Part A: Create codex-plan-reviewer.js (GSD-03 + D-07 enforcement)**

Create `~/.claude/hooks/codex-plan-reviewer.js` — a Node.js script that serves BOTH purposes:
1. As a standalone script the plan-phase agent can call via Bash: `node codex-plan-reviewer.js --phase 02 --cwd /path`
2. As a SubagentStop hook that detects when a gsd-planner subagent completes and automatically triggers plan review, blocking the subagent stop until review is done (D-07 enforcement)

**Usage modes:**
- Standalone: `node codex-plan-reviewer.js --phase 02 --cwd /path/to/project`
- SubagentStop hook: Receives JSON on stdin with `hook_event_name: "SubagentStop"`

**Header (lines 1-20):**
- Shebang: `#!/usr/bin/env node`
- Header: `// codex-hook-version: 2.0.0` and `// Codex Plan Reviewer — SubagentStop hook + standalone script`
- Comment: `// Required by: GSD-03 (plan-phase Codex review), D-07 (plan-phase blocked until Codex reviews)`
- Comment: `// Mode 1 (hook): SubagentStop fires when gsd-planner completes; blocks until review done`
- Comment: `// Mode 2 (standalone): node codex-plan-reviewer.js --phase 02 --cwd /path/to/project`
- `'use strict';`
- Require: `fs`, `path`, `child_process` (execSync)
- Require: `./codex-exec` (runCodexExec, parseCodexTokens, computeCost)

**Mode detection (lines 21-45):**
- Check if `--phase` is in `process.argv` — if yes, running as standalone script
- If no `--phase` arg, read stdin (SubagentStop hook mode):
  - Stdin timeout: `const stdinTimeout = setTimeout(() => process.exit(0), 300000);` (300s — plan review can take time)
  - Standard stdin read pattern
  - Parse JSON
  - Check `data.hook_event_name === 'SubagentStop'`
  - Detect if this is a gsd-planner subagent: check `data.transcript_path` for `gsd-planner` or check `data.cwd` for a `.planning/` directory with PLAN.md files. The heuristic: look for recently-written PLAN.md files (modified within the last 120 seconds) in `.planning/phases/*/` — if found, this was likely a planner subagent
  - If not a planner subagent (no recent PLAN.md files found), `process.exit(0)` — let non-planner subagents complete normally
  - Extract `cwd` from `data.cwd`
  - Auto-detect phase: scan `.planning/phases/` for directories, find the one with the most-recently-written PLAN.md files, extract the phase number from the directory name

**Standalone argument parsing (lines 46-55):**
- Parse `--phase` and `--cwd` from `process.argv`
- Default `cwd` to `process.cwd()`
- Validate phase is provided, exit 1 with usage message if not

**Plan file discovery (lines 56-75):**
- Read the phase directory: `path.join(cwd, '.planning', 'phases')` — find subdirectory starting with `{paddedPhase}-`
- List all files matching `{paddedPhase}-*-PLAN.md` in that directory
- Read each PLAN.md file, concatenate contents with file path headers
- Truncate combined plan content to 8000 characters

**Review prompt construction (lines 76-95):**
- Build prompt:
```
You are reviewing GSD execution plans before they are finalized. Check each plan for:
1. Missing or vague task actions (would an executor need clarification?)
2. Missing verification commands
3. Dependency issues (plan references files from a later wave)
4. File conflicts (same file modified by parallel plans)
5. Requirements coverage (check if all listed requirements have corresponding tasks)

For each issue found, output:
[PLAN {plan-id}] [SEVERITY: HIGH|MEDIUM|LOW] {description}

If all plans are acceptable, output: "PLANS APPROVED — no issues found"

Plans to review:
{plan_content}
```

**Codex invocation (lines 96-120):**
- Call `await runCodexExec(reviewPrompt, { cwd, timeoutMs: 180000, model: 'gpt-5.4' })`
  - 180s timeout (plans are large, review takes longer)
- If `result.success === false`, write error to stderr. In standalone mode, exit 1. In hook mode, `process.exit(0)` (fail-open — don't block on Codex failure)
- Extract text output from JSONL using same `extractCodexText` pattern as codex-review-gate.js:
  - Parse each JSONL line
  - Collect `event_msg` with `payload.type === 'message'` — get `payload.content` or `payload.text`
  - Also check for `type === 'text'` events as fallback

**Write REVIEWS.md (lines 121-150):**
- Build `reviewContent`:
```markdown
# Phase {phase} — Codex Plan Review

**Reviewed:** {ISO timestamp}
**Model:** gpt-5.4
**Plans reviewed:** {list of plan filenames}

## Findings

{extracted Codex text output}

## Verdict

{First line of Codex output — "PLANS APPROVED" or issue summary}
```
- Write to `{phaseDir}/{paddedPhase}-REVIEWS.md` (e.g., `.planning/phases/02-review-gate-gsd-integration/02-REVIEWS.md`)
  - Filename MUST match GSD detection pattern: ends with `-REVIEWS.md` (Pitfall 5)

**D-07 enforcement — SubagentStop blocking (lines 151-175):**
- After writing REVIEWS.md, check if any HIGH severity issues were found:
  - Parse the extracted Codex text for lines matching `[SEVERITY: HIGH]`
  - If HIGH issues exist AND running in hook mode (SubagentStop):
    - Output: `{ "decision": "block", "reason": "Codex plan review found HIGH severity issues. Review written to {reviewsPath}. Planner must address issues before finalizing." }`
    - This blocks the subagent stop and gives the planner another turn to fix the plans per D-07
  - If no HIGH issues (or "PLANS APPROVED"):
    - Output: `{ "additionalContext": "Codex plan review complete: {verdict}. Written to {reviewsPath}" }`
    - This allows the subagent to complete normally
- In standalone mode, always print to stdout: `Codex plan review complete. Written to {reviewsPath}` and exit 0

**Token logging (lines 176-195):**
- If `result.tokens` is not null:
  - Build JSONL record with `task_type: 'plan-review'`
  - Append to `.planning/token-log.jsonl`
- Log format matches codex-token-logger.js schema exactly

**Part B: Register codex-wave-validator.js in PostToolUse and codex-plan-reviewer.js in SubagentStop**

**B1: PostToolUse registration:**
Add `codex-wave-validator.js` as the 3rd hook in the existing PostToolUse `Bash|Edit|Write|MultiEdit|Agent|Task` matcher entry in `~/.claude/settings.json`:

```json
{
  "type": "command",
  "command": "node \"/home/alucard/.claude/hooks/codex-wave-validator.js\"",
  "timeout": 10
}
```

The resulting PostToolUse hooks array should be:
1. `gsd-context-monitor.js` (timeout 10) — existing
2. `codex-token-logger.js` (timeout 10) — existing
3. `codex-wave-validator.js` (timeout 10) — NEW

**B2: SubagentStop registration (D-07 enforcement):**
Add a new `"SubagentStop"` key to the `hooks` object in `~/.claude/settings.json`:

```json
"SubagentStop": [
  {
    "hooks": [
      {
        "type": "command",
        "command": "node \"/home/alucard/.claude/hooks/codex-plan-reviewer.js\"",
        "timeout": 300
      }
    ]
  }
]
```

The 300s timeout matches the plan-reviewer's internal timeout. SubagentStop CAN block (returns `decision: "block"` with reason), which is the D-07 enforcement mechanism: when a gsd-planner subagent finishes, this hook fires, reviews the plans, and blocks if HIGH severity issues are found.

Critical rules for settings.json modification:
- Read the ENTIRE settings.json first
- Parse as JSON
- Push the wave-validator hook to the end of `hooks.PostToolUse[0].hooks` array
- Add the `SubagentStop` key to `hooks` object
- Do NOT modify the Stop hook added by Plan 01
- Do NOT modify any other settings
- Write back with 2-space indentation
  </action>
  <verify>
    <automated>node -c /home/alucard/.claude/hooks/codex-plan-reviewer.js && node -c /home/alucard/.claude/hooks/codex-wave-validator.js && node -c /home/alucard/.claude/hooks/codex-wave-validator-worker.js && node -e "const s = require('fs').readFileSync('/home/alucard/.claude/settings.json','utf8'); const j = JSON.parse(s); const hooks = j.hooks.PostToolUse[0].hooks; const hasWaveValidator = hooks.some(h => h.command.includes('codex-wave-validator')); const hasSubagentStop = j.hooks.SubagentStop && j.hooks.SubagentStop[0].hooks[0].command.includes('codex-plan-reviewer'); const hasStop = j.hooks.Stop && j.hooks.Stop[0].hooks[0].command.includes('codex-review-gate'); const count = hooks.length; console.log('Wave validator registered:', hasWaveValidator, 'SubagentStop registered:', hasSubagentStop, 'Stop still present:', hasStop, 'PostToolUse hooks:', count); if (!hasWaveValidator || !hasSubagentStop || !hasStop || count !== 3) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.claude/hooks/codex-plan-reviewer.js
    - codex-plan-reviewer.js starts with `#!/usr/bin/env node`
    - codex-plan-reviewer.js contains `GSD-03` and `D-07` in comments
    - codex-plan-reviewer.js contains `SubagentStop` detection logic (hook mode)
    - codex-plan-reviewer.js contains `--phase` argument parsing (standalone mode)
    - codex-plan-reviewer.js contains `decision: 'block'` output for HIGH severity issues in hook mode (D-07 enforcement)
    - codex-plan-reviewer.js contains `REVIEWS.md` in write path construction
    - codex-plan-reviewer.js contains `require('./codex-exec')` importing runCodexExec
    - codex-plan-reviewer.js contains `task_type` with value matching `plan-review`
    - codex-plan-reviewer.js contains `token-log.jsonl` for logging
    - `node -c ~/.claude/hooks/codex-plan-reviewer.js` exits 0 (valid syntax)
    - ~/.claude/settings.json PostToolUse hooks array has exactly 3 entries
    - The 3rd PostToolUse hook command references `codex-wave-validator.js`
    - The 3rd PostToolUse hook has `"timeout": 10`
    - ~/.claude/settings.json contains `"SubagentStop"` key in hooks object
    - SubagentStop hook command references `codex-plan-reviewer.js`
    - SubagentStop hook timeout is 300
    - The Stop hook from Plan 01 still exists in settings.json
    - All other settings.json keys are unchanged
    - `node -e "JSON.parse(require('fs').readFileSync('/home/alucard/.claude/settings.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>
    codex-plan-reviewer.js serves dual purpose: SubagentStop hook that enforces D-07 by blocking plan-phase completion on HIGH severity issues, AND standalone script for direct invocation. Writes GSD-compatible REVIEWS.md. codex-wave-validator.js registered in PostToolUse (3rd hook, timeout 10). codex-plan-reviewer.js registered in SubagentStop (timeout 300). Plan-phase finalization is enforced-blocked until Codex reviews per D-07.
  </done>
</task>

</tasks>

<verification>
1. `node -c ~/.claude/hooks/codex-wave-validator.js` — valid JavaScript syntax
2. `node -c ~/.claude/hooks/codex-wave-validator-worker.js` — valid JavaScript syntax
3. `node -c ~/.claude/hooks/codex-plan-reviewer.js` — valid JavaScript syntax
4. `node -e "JSON.parse(require('fs').readFileSync('/home/alucard/.claude/settings.json','utf8'))"` — valid JSON
5. `grep -c 'phase-plan-index' ~/.claude/hooks/codex-wave-validator.js` — returns 1+ (GSD query)
6. `grep -c 'REVIEWS.md' ~/.claude/hooks/codex-plan-reviewer.js` — returns 1+ (review file)
7. `grep -c 'SubagentStop' ~/.claude/hooks/codex-plan-reviewer.js` — returns 1+ (D-07 hook mode)
8. `grep -c "decision.*block" ~/.claude/hooks/codex-plan-reviewer.js` — returns 1+ (D-07 enforcement)
9. `grep 'codex-wave-validator' ~/.claude/settings.json` — registered in PostToolUse
10. `grep 'SubagentStop' ~/.claude/settings.json` — SubagentStop hook registered
11. `grep 'codex-plan-reviewer' ~/.claude/settings.json` — plan reviewer in SubagentStop
12. `grep 'codex-review-gate' ~/.claude/settings.json` — Plan 01 Stop hook still present
13. `node ~/.claude/hooks/codex-plan-reviewer.js --help 2>&1 | head -1` — shows usage (exits 1 without --phase)
14. `grep 'codex-wave-validator-worker' ~/.claude/hooks/codex-wave-validator.js` — worker file reference (not inline shell)
</verification>

<success_criteria>
- codex-wave-validator.js detects wave completion via SUMMARY.md writes, queries GSD wave status, dispatches non-blocking Codex validation via dedicated worker file, and surfaces results at natural stopping points
- codex-wave-validator-worker.js handles Codex invocation, verdict parsing, sentinel update, and token logging as a clean standalone process (not fragile inline shell)
- codex-plan-reviewer.js reviews PLAN.md files via Codex and writes REVIEWS.md matching GSD detection pattern
- codex-plan-reviewer.js enforces D-07 via SubagentStop hook: blocks plan-phase completion when HIGH severity issues found
- Wave validation is non-blocking (D-08) with critical issue halt (D-09)
- Results persist across sessions via sentinel files
- All scripts log to existing token-log.jsonl (D-06)
- GSD-01: Wave-boundary Codex validation dispatched automatically
- GSD-02: Background validation is non-blocking, results available at stopping points
- GSD-03: Plan-phase review produces REVIEWS.md that GSD's has_reviews flag detects
- D-07: Plan-phase finalization is enforced-blocked until Codex reviews via SubagentStop hook registration
</success_criteria>

<output>
After completion, create `.planning/phases/02-review-gate-gsd-integration/02-02-SUMMARY.md`
</output>
