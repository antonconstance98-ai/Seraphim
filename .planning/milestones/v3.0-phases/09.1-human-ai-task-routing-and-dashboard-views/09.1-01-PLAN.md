---
phase: 09.1-human-ai-task-routing-and-dashboard-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/architect.md
  - ~/.claude/plugins/seraphim/lib/markers.js
autonomous: true
requirements:
  - PIPE-04

must_haves:
  truths:
    - "blueprint.md output contains assignee attribute on every SERAPHIM:TASK marker"
    - "assignee is either 'human' or 'ai' — no other values"
    - "parseMarkers returns the assignee field when reading blueprint.md"
    - "default assignee is 'ai' — human is the exception"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/architect.md"
      provides: "Updated Architect command with assignee tagging instructions"
      contains: "assignee"
    - path: "~/.claude/plugins/seraphim/lib/markers.js"
      provides: "emitTask helper that includes assignee attribute"
      contains: "emitTask"
  key_links:
    - from: "architect.md Step 8 inline path"
      to: "blueprint.md TASK markers"
      via: "assignee attribute in HTML comment"
      pattern: "assignee=\"(human|ai)\""
    - from: "architect.md Step 8 external dispatch prompt"
      to: "blueprint.md TASK markers"
      via: "prompt instructions listing assignee attribute requirement"
      pattern: "assignee="
---

<objective>
Add assignee="human" or assignee="ai" attribute to every SERAPHIM:TASK marker produced by the Architect phase.

Purpose: Enables Forge to skip human-tagged tasks and the dashboard to display separate human/AI task lists. This is the foundation for Phase 9's cognitive division routing.
Output: Updated architect.md with cognitive division rules; emitTask helper added to markers.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-human-ai-task-routing-and-dashboard-views/09.1-CONTEXT.md

<interfaces>
Existing TASK marker format (from markers.js and architect.md):
  Current: SERAPHIM:TASK id="task-N" type="code|prose|analysis"
  New:     SERAPHIM:TASK id="task-N" type="code|prose|analysis" assignee="human|ai"

From ~/.claude/plugins/seraphim/lib/markers.js (current exports):
  parseMarkers(content)  -- returns [{type, ...attrs}] — already reads arbitrary attrs generically
  emitMarker(type, attrs)
  emitPhaseStart(phase, model, profile)
  emitPhaseComplete(attrs)
  emitApproach(id, verdict, reason)
  emitBlueprint(projectType, phase, taskCount)
  emitTrackFailed(model, error)
  -- emitTask is MISSING and must be added

parseMarkers already captures all key="val" pairs without any schema — no change needed to the parser.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add emitTask helper to markers.js</name>
  <files>~/.claude/plugins/seraphim/lib/markers.js</files>
  <behavior>
    - emitTask('task-1', 'code', 'ai') produces: SERAPHIM:TASK id="task-1" type="code" assignee="ai"
    - emitTask('task-2', 'prose', 'human') produces: SERAPHIM:TASK id="task-2" type="prose" assignee="human"
    - emitTask('task-3', 'analysis') produces assignee="ai" by default — third arg is optional
    - parseMarkers round-trip: emitTask output fed back into parseMarkers returns { type: 'TASK', id: 'task-1', type: 'code', assignee: 'human' }
  </behavior>
  <action>
Read ~/.claude/plugins/seraphim/lib/markers.js.

Add this function before module.exports:

  function emitTask(id, type, assignee) {
    return emitMarker('TASK', { id: String(id), type: String(type), assignee: assignee || 'ai' });
  }

Add 'emitTask' to module.exports alongside the existing exports.
  </action>
  <verify>
    <automated>node -e "const m = require('/root/.claude/plugins/seraphim/lib/markers.js'); const t = m.emitTask('task-1','code','human'); console.assert(t.includes('assignee=\"human\"')); console.assert(m.emitTask('x','prose').includes('assignee=\"ai\"')); console.assert(m.parseMarkers(t)[0].assignee === 'human'); console.log('PASS')"</automated>
  </verify>
  <done>emitTask exported from markers.js; node smoke-test prints PASS; assignee defaults to 'ai' when omitted.</done>
</task>

<task type="auto">
  <name>Task 2: Update architect.md with assignee tagging instructions</name>
  <files>~/.claude/plugins/seraphim/commands/architect.md</files>
  <action>
Read ~/.claude/plugins/seraphim/commands/architect.md.

Make the following targeted edits:

1. In Step 8 (inline Opus path), update the task breakdown instructions. After the existing point about task types (code/prose/analysis), add a new numbered point:

  5. Each task MUST include an assignee attribute: assignee="human" or assignee="ai".
     Default is assignee="ai". Assign human only when the task requires:
     - Divergent creative thinking or novel ideation (not pattern-matching)
     - Domain expertise the AI cannot verify (legal, medical, ethical judgment)
     - Physical world actions (running hardware, signing off on deployments)
     - Subjective quality approval (does this feel right for our brand?)
     Human tasks must still have specific file output paths and test criteria.

2. In Step 8 (inline Opus path), update the TASK marker format description. Change:
     SERAPHIM:TASK id="task-N" type="code"
   to:
     SERAPHIM:TASK id="task-N" type="code" assignee="ai"
   and add a human example:
     SERAPHIM:TASK id="task-N" type="analysis" assignee="human"

3. In Step 8 (external dispatch prompt block), add to the INSTRUCTIONS list:
  6. Each SERAPHIM:TASK marker MUST include assignee="human" or assignee="ai".
     Default is assignee="ai". Use assignee="human" only for: divergent creative ideation,
     domain expertise the AI cannot verify, physical/approval actions, subjective quality sign-off.

4. In Step 9 (output format for blueprint.md), update the example TASK marker lines from:
     SERAPHIM:TASK id="task-1" type="code"
   to:
     SERAPHIM:TASK id="task-1" type="code" assignee="ai"

5. In Step 10 (validate blueprint output), after the taskMarkers.length check, add:
     const untaggedTasks = taskMarkers.filter(m => !m.assignee);
     if (untaggedTasks.length > 0) {
       console.warn('Warning: ' + untaggedTasks.length + ' task(s) missing assignee attribute. Defaulting to ai.');
       // Do not abort — warn only, for backwards compatibility
     }

Do NOT change anything else. All other steps remain untouched.
  </action>
  <verify>
    <automated>grep -c 'assignee' /root/.claude/plugins/seraphim/commands/architect.md</automated>
  </verify>
  <done>architect.md contains at least 5 occurrences of 'assignee'; both inline and external-dispatch paths include the cognitive division rules; output format example shows assignee attribute; validation step warns on missing assignee.</done>
</task>

</tasks>

<verification>
Run: grep -c 'assignee' ~/.claude/plugins/seraphim/commands/architect.md
Expect: 5 or more matches.

Run: node -e "const m = require('/root/.claude/plugins/seraphim/lib/markers.js'); console.assert(typeof m.emitTask === 'function'); const out = m.emitTask('t1','code','human'); console.assert(out.includes('assignee=\"human\"')); console.log('markers OK')"
Expect: markers OK
</verification>

<success_criteria>
- emitTask function exported from markers.js, defaults assignee to 'ai'
- parseMarkers round-trips the assignee attribute correctly
- architect.md instructs both inline and dispatch paths to tag every task with assignee
- Cognitive division rules documented: 4 human-assignment triggers, all other tasks default ai
- Validation step in architect.md warns (not aborts) on missing assignee for backwards compatibility
</success_criteria>

<output>
After completion, create .planning/phases/09.1-human-ai-task-routing-and-dashboard-views/09.1-01-SUMMARY.md
</output>
