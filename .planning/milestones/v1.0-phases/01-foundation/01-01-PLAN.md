---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alucard/projects/Claude_X_Codex/AGENTS.md
  - /home/alucard/projects/Claude_X_Codex/.claude/settings.json
autonomous: false
requirements:
  - FNDTN-01
  - FNDTN-04
  - FNDTN-05
  - ROUT-01
user_setup:
  - service: openai
    why: "Codex CLI hooks need an API key for headless authentication (FNDTN-05)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys -> Create new secret key (https://platform.openai.com/api-keys)"
      - name: CLAUDE_CODE_SUBPROCESS_ENV_SCRUB
        source: "Set to 1 in ~/.bashrc — prevents API key exfiltration via hook scripts (CVE-2025-59536)"
must_haves:
  truths:
    - "AGENTS.md exists at repo root and Codex reads it for project context before executing any task"
    - "Claude Code version is 2.0.65+ and CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1 is set in the shell environment"
    - "OPENAI_API_KEY is set as an environment variable, never in a committed file"
    - "AGENTS.md contains the hard rule that Opus is the sole architect — Codex cannot make structural decisions"
  artifacts:
    - path: "AGENTS.md"
      provides: "Codex project brief with role definition, task boundaries, tech stack, conventions, security rules"
      contains: "This requires an architectural decision"
    - path: ".claude/settings.json"
      provides: "Project-level Codex routing config (opt-in flag, OFF by default)"
      contains: "routing_enabled"
  key_links:
    - from: "AGENTS.md"
      to: "Codex CLI"
      via: "Codex CLI auto-reads AGENTS.md from repo root before every exec call"
      pattern: "AGENTS\\.md"
    - from: ".claude/settings.json"
      to: "~/.claude/hooks/codex-router.js"
      via: "Routing hook reads project config to check opt-in flag"
      pattern: "codex.*routing_enabled"
---

<objective>
Establish the security baseline and Codex project briefing document that all subsequent plans depend on.

Purpose: Before any hooks invoke Codex, the environment must be hardened (API key protected, env scrub enabled) and Codex must have its project context file (AGENTS.md). This plan creates both foundations.

Output: AGENTS.md at repo root, project-level .claude/settings.json with opt-in config, verified security environment
</objective>

<execution_context>
@/home/alucard/.claude/get-shit-done/workflows/execute-plan.md
@/home/alucard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From existing ~/.claude/settings.json — the hook registration structure executor must understand -->
<!-- Project-scope .claude/settings.json is a SEPARATE file from user-scope ~/.claude/settings.json -->

From ~/.claude/settings.json (user scope — DO NOT MODIFY):
```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "node \"/home/alucard/.claude/hooks/claude-settings-guard.js\"", "timeout": 5 },
          { "type": "command", "command": "node \"/home/alucard/.claude/hooks/gsd-prompt-guard.js\"", "timeout": 5 }
        ]
      }
    ]
  }
}
```

From ~/.claude/hooks/claude-settings-guard.js (PreToolUse pattern — reference for hook structure):
```javascript
const output = {
  hookSpecificOutput: {
    hookEventName: 'PreToolUse',
    permissionDecision: 'deny',
    permissionDecisionReason: 'Reason shown to Claude when blocked'
  }
};
process.stdout.write(JSON.stringify(output));
process.exit(2);  // exit code 2 = deny
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AGENTS.md Codex project brief</name>
  <files>
    /home/alucard/projects/Claude_X_Codex/AGENTS.md
  </files>
  <read_first>
    /home/alucard/projects/Claude_X_Codex/CLAUDE.md
    /home/alucard/projects/Claude_X_Codex/.planning/phases/01-foundation/01-CONTEXT.md
    /home/alucard/projects/Claude_X_Codex/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create AGENTS.md at the repo root (`/home/alucard/projects/Claude_X_Codex/AGENTS.md`).

This file is auto-read by Codex CLI before every task. It must follow the structure from Research Pattern 5 and honor decisions D-01 through D-04.

**Required sections (in order):**

1. **Header:** `# AGENTS.md -- Codex Project Brief`

2. **What This Project Is** (per D-03, plain English, no jargon):
   "Claude X Codex is a multi-model integration that wires OpenAI Codex (GPT-5.4) into an existing Claude Code workflow. Claude Opus 4.6 acts as the architect and orchestrator. Codex handles fast, cheap execution. The goal is better results at lower cost by routing each task to the model that is best at it."

3. **Your Role** (per D-01, moderate guardrails):
   ```
   You are Codex (GPT-5.4), the executor model in a two-model setup.
   Claude Opus 4.6 is the architect and orchestrator.

   You may make minor judgment calls: variable naming, small refactors,
   code formatting, choosing between equivalent approaches when the spec
   does not specify. These are fine.

   You must NOT make structural or architectural decisions. If a task
   requires choosing a new library, changing file organization, modifying
   API contracts, or redesigning data flow, stop and respond with exactly:
   "This requires an architectural decision. Please route to Opus."
   ```

4. **What You Handle** (per D-05, all four task types):
   - Clearly-defined implementation tasks (spec provided)
   - Test generation (unit tests, integration tests from specs)
   - Bulk file operations (renaming, reformatting, migrations)
   - Code review and diff analysis

5. **What You Do NOT Handle:**
   - Architecture decisions (library selection, data modeling, API design)
   - Debugging complex or unknown problems
   - Exploratory analysis or research
   - Anything not explicitly scoped in the handoff spec
   - Security-sensitive operations (key management, auth flows)

6. **Tech Stack** (extracted from CLAUDE.md):
   - Runtime: Node.js v22.22.0
   - Codex CLI: @openai/codex v0.118.0
   - OpenAI SDK: openai v6.33.0
   - Hooks: Claude Code hooks (Node.js, stdin JSON, stdout JSON)
   - Config: ~/.claude/settings.json (user scope), .claude/settings.json (project scope)

7. **Conventions** (per D-04, living document — start with):
   - Hook scripts follow Node.js stdin/stdout JSON pattern
   - Timeout guard (10s) on stdin in every hook
   - Silent fail (exit 0) on parse errors — never block tool execution
   - PreToolUse hooks exit 2 to deny; output `permissionDecision: 'deny'`
   - PostToolUse hooks output `additionalContext` for advisory messages
   - All new files use `#!/usr/bin/env node` shebang
   - Conventions section will be updated at each GSD phase transition

8. **Security Rules:**
   - Never expose API keys in source code, logs, or console output
   - Never reference `process.env.OPENAI_API_KEY` in log/debug statements
   - Bind all services to 127.0.0.1 only
   - No sudo unless the task explicitly requires it
   - Do not commit .env files or secrets to git

9. **Communication Style** (per D-03):
   "Write plain English. Explain what you are about to do in one sentence before doing it. If something fails, explain the error simply and fix it. Avoid jargon — or define it right after using it."
  </action>
  <verify>
    <automated>test -f /home/alucard/projects/Claude_X_Codex/AGENTS.md && grep -q "architectural decision" /home/alucard/projects/Claude_X_Codex/AGENTS.md && grep -q "Please route to Opus" /home/alucard/projects/Claude_X_Codex/AGENTS.md && grep -q "What You Handle" /home/alucard/projects/Claude_X_Codex/AGENTS.md && grep -q "Security Rules" /home/alucard/projects/Claude_X_Codex/AGENTS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md exists at /home/alucard/projects/Claude_X_Codex/AGENTS.md
    - AGENTS.md contains "# AGENTS.md" as the first heading
    - AGENTS.md contains "This requires an architectural decision. Please route to Opus."
    - AGENTS.md contains "## What You Handle" with all four task types listed
    - AGENTS.md contains "## What You Do NOT Handle" with architecture decisions listed
    - AGENTS.md contains "## Security Rules" with "Never expose API keys" text
    - AGENTS.md contains "## Conventions" section (living document per D-04)
    - AGENTS.md contains "## Tech Stack" with "Node.js v22.22.0"
    - AGENTS.md does NOT contain any actual API key values
    - AGENTS.md uses plain English throughout (per D-03) — no unexplained jargon
  </acceptance_criteria>
  <done>AGENTS.md exists at repo root with all 9 required sections; contains the hard-stop phrase for architectural decisions; lists all four Codex task types; includes security rules and conventions</done>
</task>

<task type="auto">
  <name>Task 2: Create project-level .claude/settings.json with Codex opt-in config</name>
  <files>
    /home/alucard/projects/Claude_X_Codex/.claude/settings.json
  </files>
  <read_first>
    /home/alucard/.claude/settings.json
    /home/alucard/projects/Claude_X_Codex/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the project-scope `.claude/settings.json` at `/home/alucard/projects/Claude_X_Codex/.claude/settings.json`.

This is a SEPARATE file from `~/.claude/settings.json` (user scope). The project-scope file contains project-specific configuration. Per D-07, routing is OFF by default.

**File content — exact JSON:**
```json
{
  "codex": {
    "routing_enabled": false,
    "fallback_on_error": "prompt_user",
    "attribution_enabled": true,
    "timeout_seconds": 300,
    "preferred_model": "gpt-5.4",
    "api_model": "gpt-5.4-mini"
  }
}
```

Field explanations:
- `routing_enabled`: false (D-07 — opt-in, OFF by default; user must set to true to activate Codex routing)
- `fallback_on_error`: "prompt_user" (ROUT-03 — fail-CLOSED; do not silently auto-route to Opus, prompt user instead)
- `attribution_enabled`: true (D-08 — results note which model did the work)
- `timeout_seconds`: 300 (FNDTN-02 — 300s max for codex exec calls)
- `preferred_model`: "gpt-5.4" (ROUT-04 — CLI uses this model via subscription)
- `api_model`: "gpt-5.4-mini" (ROUT-04 — API uses cheaper model for advisory)

Also ensure the `.claude/` directory exists before writing the file.
  </action>
  <verify>
    <automated>test -f /home/alucard/projects/Claude_X_Codex/.claude/settings.json && node -e "const c=JSON.parse(require('fs').readFileSync('/home/alucard/projects/Claude_X_Codex/.claude/settings.json','utf8')); if(c.codex.routing_enabled===false && c.codex.timeout_seconds===300 && c.codex.fallback_on_error==='prompt_user' && c.codex.attribution_enabled===true) { console.log('PASS'); } else { console.log('FAIL'); }" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/settings.json exists at /home/alucard/projects/Claude_X_Codex/.claude/settings.json
    - File is valid JSON (node -e "JSON.parse(require('fs').readFileSync(...))" exits 0)
    - Contains `"routing_enabled": false` (D-07 opt-in OFF)
    - Contains `"fallback_on_error": "prompt_user"` (ROUT-03 fail-closed)
    - Contains `"attribution_enabled": true` (D-08)
    - Contains `"timeout_seconds": 300` (FNDTN-02)
    - Contains `"preferred_model": "gpt-5.4"` (ROUT-04)
    - Contains `"api_model": "gpt-5.4-mini"` (ROUT-04)
    - File does NOT contain hook registrations (those are in user-scope ~/.claude/settings.json)
    - File does NOT contain API keys or secrets
  </acceptance_criteria>
  <done>Project-scope .claude/settings.json exists with Codex config block; routing is OFF by default; all 6 config fields present with correct values</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set security environment variables</name>
  <what-built>AGENTS.md and project config are ready, but security environment variables must be set before any hooks can safely invoke Codex.</what-built>
  <how-to-verify>
The user needs to do two things:

**Step 1: Set CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1**
This prevents API key exfiltration via hook scripts (CVE-2025-59536). Add to ~/.bashrc:
```bash
echo 'export CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1' >> ~/.bashrc
source ~/.bashrc
```
Verify: `echo $CLAUDE_CODE_SUBPROCESS_ENV_SCRUB` should print `1`

**Step 2: Set OPENAI_API_KEY**
Required for hook-triggered Codex invocations (FNDTN-05). Get your API key from https://platform.openai.com/api-keys then:
```bash
echo 'export OPENAI_API_KEY="sk-your-key-here"' >> ~/.bashrc
source ~/.bashrc
```
Verify: `echo $OPENAI_API_KEY` should print your key (starts with "sk-")

**Step 3: Verify Codex can authenticate**
```bash
OPENAI_API_KEY="$OPENAI_API_KEY" codex exec --json "echo hello" 2>&1 | head -5
```
Should produce JSONL output (not an auth error).

**Important:** Never commit ~/.bashrc or share your API key. The key lives only in your shell environment.
  </how-to-verify>
  <resume-signal>Type "done" after setting both environment variables, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test -f AGENTS.md` — file exists at repo root
2. `grep "architectural decision" AGENTS.md` — hard-stop phrase present
3. `test -f .claude/settings.json` — project config exists
4. `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8')).codex.routing_enabled === false && console.log('OK')"` — routing is OFF
5. `echo $CLAUDE_CODE_SUBPROCESS_ENV_SCRUB` — prints `1`
6. `echo $OPENAI_API_KEY | head -c 3` — prints `sk-` (key is set, not exposed)
</verification>

<success_criteria>
- AGENTS.md exists and contains all 9 required sections per decisions D-01 through D-04
- .claude/settings.json exists with Codex routing OFF by default per D-07
- CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1 is active in shell environment per FNDTN-04
- OPENAI_API_KEY is set as env var (never in a committed file) per FNDTN-05
- Codex CLI can authenticate headlessly using the API key
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
