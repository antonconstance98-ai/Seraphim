---
phase: 35-phase-management-config-ui-tooling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/workstreams.md
  - ~/.claude/plugins/seraphim/commands/manager.md
  - ~/.claude/plugins/seraphim/commands/settings.md
autonomous: true
requirements: [MGMT-07, MGMT-08, CFG-01, CFG-02]

must_haves:
  truths:
    - "User can manage parallel workstreams via /seraphim:workstreams"
    - "User can manage phases from interactive command center via /seraphim:manager"
    - "User can configure workflow settings and model profiles via /seraphim:settings"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/workstreams.md"
      provides: "Workstream management command"
    - path: "~/.claude/plugins/seraphim/commands/manager.md"
      provides: "Interactive phase management center"
    - path: "~/.claude/plugins/seraphim/commands/settings.md"
      provides: "Unified settings command for profiles and toggles"
  key_links:
    - from: "commands/settings.md"
      to: "lib/config.js"
      via: "config.read/config.write for workflow toggles"
      pattern: "config\\.write"
---

<objective>
Create workstreams, manager, and settings commands for project management and configuration.

Purpose: Enable parallel workstream tracking, interactive phase management, and unified workflow configuration.
Output: Three `.md` command files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-phase-management-config-ui-tooling/35-CONTEXT.md
@.planning/phases/35-phase-management-config-ui-tooling/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workstreams and manager commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/workstreams.md
    ~/.claude/plugins/seraphim/commands/manager.md
  </files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/progress.md (reporting/status pattern)
    - ~/.claude/plugins/seraphim/lib/config.js (config read/write pattern)
    - .planning/ROADMAP.md (phase structure for manager display)
  </read_first>
  <action>
**workstreams.md** (MGMT-07, per D-04):
- `description: "Manage parallel workstreams"`
- `argument-hint: "[list|create|switch|status] [name]"`
- `allowed-tools: ["Read", "Write", "Bash"]`
- Step 1: Resolve project root
- Step 2: Parse subcommand from argument (default: `list`)
- Step 3: Operations via `node -e` inline:
  - `list`: Read `.planning/workstreams/*.json`, display table of name/status/phases/current_phase
  - `create <name>`: Ask user which phases belong to this workstream. Write `{name}.json` to `.planning/workstreams/` with state: `{name, phases:[], status:"active", current_phase:null, created_at, updated_at}`. Use atomic tmp+rename.
  - `switch <name>`: Set the named workstream as active in STATE.md (add `active_workstream: name` to frontmatter)
  - `status [name]`: Show detailed status of named workstream (or active one) — phases, completion %, current position
- Step 4: Ensure `.planning/workstreams/` directory exists (create with `recursive:true` if missing)

**manager.md** (MGMT-08):
- `description: "Interactive phase management command center"`
- `allowed-tools: ["Read", "Write", "Bash", "Glob"]`
- Step 1: Resolve project root
- Step 2: Read ROADMAP.md and STATE.md
- Step 3: Display rich formatted dashboard:
  ```
  === Phase Manager ===
  Milestone: v3.2 Idea-to-Shipped Journey

  Phases:
  1. [DONE] Phase 32: Foundations (3/3 plans)
  2. [DONE] Phase 33: Core Command Layer (5/5 plans)
  3. [ACTIVE] Phase 35: Phase Management (0/? plans)
  ...

  Actions:
  [1] Add a new phase
  [2] Insert urgent phase
  [3] Remove a phase
  [4] View phase details
  [5] Run health check
  [6] Complete milestone
  ```
- Step 4: Ask user to pick an action by number
- Step 5: Execute chosen action by calling the appropriate Seraphim command inline (e.g., action 1 triggers add-phase logic, action 5 triggers health logic)
  </action>
  <verify>
    <automated>for f in workstreams manager; do test -f "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/${f}.md" && grep -q "description:" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/${f}.md" && echo "${f} OK"; done | wc -l | grep -q 2 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - workstreams.md supports list/create/switch/status subcommands
    - workstreams.md writes state to .planning/workstreams/{name}.json with atomic writes
    - manager.md displays phase overview and numbered action list
    - Both have valid YAML frontmatter
  </acceptance_criteria>
  <done>Workstreams command manages parallel tracks, manager provides interactive command center</done>
</task>

<task type="auto">
  <name>Task 2: Create settings command with model profiles and workflow toggles</name>
  <files>~/.claude/plugins/seraphim/commands/settings.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/set-profile.md (existing profile switching pattern)
    - ~/.claude/plugins/seraphim/lib/config.js (CONFIG_DEFAULTS, read/write)
    - .planning/config.json (current workflow settings)
  </read_first>
  <action>
**settings.md** (CFG-01, CFG-02, per D-05 and D-06):
- `description: "Configure workflow settings and model profiles"`
- `argument-hint: "[setting] [value]"`
- `allowed-tools: ["Read", "Write", "Bash"]`
- Step 1: Resolve project root
- Step 2: If no arguments, display current settings in formatted table:
  ```
  === Seraphim Settings ===

  Model Profile: balanced
    Profiles: quality | balanced | budget | inherit

  Workflow Toggles:
  | Toggle | Value | Description |
  |--------|-------|-------------|
  | skip_discuss | false | Skip discussion phase |
  | auto_advance | true | Auto-advance after execution |
  | ui_phase | true | Generate UI specs for frontend phases |
  | ui_review | false | Run UI review after frontend phases |
  | plan_checker | true | Enable plan verification loop |
  | research_enabled | true | Enable research phase |
  | parallelization | true | Enable parallel wave execution |

  Usage: /seraphim:settings <toggle> <true|false>
         /seraphim:settings profile <quality|balanced|budget|inherit>
  ```
- Step 3: If argument is `profile <name>`: validate name is one of quality/balanced/budget/inherit. Write to `.planning/config.json` via `node -e` using config.js read/write. Per D-05, profiles map to model selections per command type stored in `~/.claude/plugins/seraphim/config/profiles.json`.
- Step 4: If argument is `<toggle> <value>`: validate toggle name against allowed list (per Pitfall 5 — only allow known keys from D-06). Validate value is true/false. Write to `.planning/config.json` workflow section.
- Step 5: Confirm change: "Set {setting} = {value}"
  </action>
  <verify>
    <automated>test -f "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/settings.md" && grep -q "description:" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/settings.md" && grep -q "workflow" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/settings.md" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - settings.md exists with YAML frontmatter
    - Displays current profile and all workflow toggles when called without args
    - Validates toggle names against allowed list per Pitfall 5
    - Writes via config.js read/write pattern
    - Supports profile switching per D-05
    - Supports workflow toggles per D-06: skip_discuss, auto_advance, ui_phase, ui_review, plan_checker, research_enabled, parallelization
  </acceptance_criteria>
  <done>Settings command provides unified configuration for model profiles and workflow toggles</done>
</task>

</tasks>

<verification>
- All three commands have valid YAML frontmatter
- settings.md references all 7 workflow toggles from D-06
- workstreams.md writes to .planning/workstreams/
</verification>

<success_criteria>
- Workstream state management via .planning/workstreams/ per D-04
- Manager provides interactive phase overview per MGMT-08
- Settings unifies profile + toggle configuration per CFG-01/CFG-02
</success_criteria>

<output>
After completion, create `.planning/phases/35-phase-management-config-ui-tooling/35-03-SUMMARY.md`
</output>
