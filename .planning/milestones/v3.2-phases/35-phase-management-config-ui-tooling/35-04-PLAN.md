---
phase: 35-phase-management-config-ui-tooling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/ui-spec.md
  - ~/.claude/plugins/seraphim/commands/ui-review.md
  - ~/.claude/plugins/seraphim/commands/add-tests.md
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "User can generate a UI design contract for frontend phases via /seraphim:ui-spec"
    - "User can run a 6-pillar UI audit via /seraphim:ui-review"
    - "User can generate tests for a completed phase via /seraphim:add-tests"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/ui-spec.md"
      provides: "UI specification generator"
    - path: "~/.claude/plugins/seraphim/commands/ui-review.md"
      provides: "6-pillar UI audit command"
    - path: "~/.claude/plugins/seraphim/commands/add-tests.md"
      provides: "Test generation command"
  key_links:
    - from: "commands/ui-spec.md"
      to: ".planning/phases/{N}-{slug}/UI-SPEC.md"
      via: "writes UI spec to phase directory"
      pattern: "UI-SPEC\\.md"
---

<objective>
Create UI and quality tooling commands (ui-spec, ui-review, add-tests).

Purpose: Enable UI design contracts for frontend phases, retroactive quality audits, and automated test generation from acceptance criteria.
Output: Three `.md` command files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-phase-management-config-ui-tooling/35-CONTEXT.md
@.planning/phases/35-phase-management-config-ui-tooling/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ui-spec and ui-review commands</name>
  <files>
    ~/.claude/plugins/seraphim/commands/ui-spec.md
    ~/.claude/plugins/seraphim/commands/ui-review.md
  </files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/discuss.md (AI analysis + structured output pattern)
    - .planning/ROADMAP.md (phase structure with UI hint)
  </read_first>
  <action>
**ui-spec.md** (UI-01, per D-08):
- `description: "Generate a UI design contract for a frontend phase"`
- `argument-hint: "[phase-number]"`
- `allowed-tools: ["Read", "Write", "Bash", "Glob"]`
- Step 1: Resolve project root
- Step 2: Determine phase — use argument or current phase from STATE.md
- Step 3: Read phase CONTEXT.md, RESEARCH.md, and PLAN.md files to understand what UI is being built
- Step 4: Generate `UI-SPEC.md` in the phase directory with these sections per D-08:
  - `# UI Spec: [Phase Name]`
  - `## Layout Wireframe` — ASCII box diagram showing page structure
  - `## Component List` — table with Component, Props, State, Notes columns
  - `## Interaction Patterns` — click/hover/submit/navigation behaviors
  - `## Responsive Breakpoints` — mobile (< 768px), tablet (768-1024px), desktop (> 1024px) layout changes
  - `## Accessibility Notes` — ARIA labels, keyboard navigation, screen reader considerations
- Step 5: Confirm output path

**ui-review.md** (UI-02, per D-09):
- `description: "Run a 6-pillar UI audit on a phase"`
- `argument-hint: "[phase-number]"`
- `allowed-tools: ["Read", "Write", "Bash", "Glob"]`
- Step 1: Resolve project root
- Step 2: Determine phase scope — find implementation files (`.tsx`, `.jsx`, `.css`, `.html`, `.scss`) in phase's `files_modified` from SUMMARY.md or PLAN.md
- Step 3: Per Pitfall 6 — if no UI files found, print "No UI files detected in this phase scope. Run on a frontend phase." and exit
- Step 4: Read each UI file, analyze against 6 pillars
- Step 5: Generate `.planning/ui-reviews/{phase}-UI-REVIEW.md` with scored table per D-09:
  | Pillar | Score (1-5) | Findings | Recommendations |
  |--------|-------------|----------|-----------------|
  | Layout | N | ... | ... |
  | Typography | N | ... | ... |
  | Color | N | ... | ... |
  | Spacing | N | ... | ... |
  | Accessibility | N | ... | ... |
  | Responsiveness | N | ... | ... |
- Step 6: Print overall score and summary. Note this is advisory, not blocking.
  </action>
  <verify>
    <automated>for f in ui-spec ui-review; do test -f "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/${f}.md" && grep -q "description:" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/${f}.md" && echo "${f} OK"; done | wc -l | grep -q 2 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ui-spec.md generates UI-SPEC.md with ASCII wireframes, component list, interaction patterns, breakpoints, accessibility per D-08
    - ui-review.md generates scored 6-pillar audit per D-09
    - ui-review.md guards against non-UI phases per Pitfall 6
    - Both have valid YAML frontmatter
  </acceptance_criteria>
  <done>UI spec generates design contracts, UI review runs 6-pillar audits with scores</done>
</task>

<task type="auto">
  <name>Task 2: Create add-tests command</name>
  <files>~/.claude/plugins/seraphim/commands/add-tests.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/execute-plan.md (phase file reading pattern)
  </read_first>
  <action>
**add-tests.md** (UI-03, per D-10):
- `description: "Generate tests for a completed phase"`
- `argument-hint: "[phase-number]"`
- `allowed-tools: ["Read", "Write", "Bash", "Glob"]`
- Step 1: Resolve project root
- Step 2: Determine phase from argument or STATE.md
- Step 3: Detect test framework via `node -e` reading package.json:
  - Check for `vitest` in deps/devDeps -> use vitest
  - Check for `jest` in deps/devDeps -> use jest
  - Neither found -> generate framework-agnostic skeleton with note "Install jest or vitest before running"
- Step 4: Read phase inputs for test criteria:
  - `.planning/phases/{N}-{slug}/VERIFICATION.md` — acceptance criteria
  - `.planning/phases/{N}-{slug}/*-PLAN.md` — done-criteria and acceptance_criteria from each task
  - `.planning/phases/{N}-{slug}/*-SUMMARY.md` — actual files created
- Step 5: Per Pitfall 7 — check if test file already exists at target path. If exists, read it, identify which acceptance criteria are already covered, only generate missing test cases.
- Step 6: Generate test files in `tests/` or `__tests__/` directory matching detected framework conventions:
  - One test file per source file or logical grouping
  - Each acceptance criterion becomes a `test()` or `it()` block
  - Import paths resolve to actual source files from SUMMARY.md
- Step 7: Report: "Generated N test files covering M acceptance criteria for Phase {N}"
  </action>
  <verify>
    <automated>test -f "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/add-tests.md" && grep -q "description:" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/add-tests.md" && grep -q "vitest\|jest" "/home/alucardmessangeroflight/.claude/plugins/seraphim/commands/add-tests.md" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - add-tests.md exists with YAML frontmatter
    - Detects test framework from package.json per D-10
    - Reads VERIFICATION.md and PLAN.md done-criteria for test generation
    - Checks for existing test files before generating per Pitfall 7
    - Generates framework-appropriate test blocks
  </acceptance_criteria>
  <done>Add-tests generates test files from acceptance criteria with framework auto-detection</done>
</task>

</tasks>

<verification>
- All three command files exist with valid YAML frontmatter
- ui-spec references ASCII wireframes and component list
- ui-review references 6-pillar scoring
- add-tests references framework detection
</verification>

<success_criteria>
- ui-spec produces design contracts per D-08
- ui-review runs 6-pillar audit per D-09
- add-tests generates tests from acceptance criteria per D-10
</success_criteria>

<output>
After completion, create `.planning/phases/35-phase-management-config-ui-tooling/35-04-SUMMARY.md`
</output>
