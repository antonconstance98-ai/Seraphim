---
phase: 34-research-session-navigation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/next.md
  - ~/.claude/plugins/seraphim/commands/do.md
  - ~/.claude/plugins/seraphim/commands/progress.md
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "User can run /seraphim:next and be routed to the correct next command based on artifact presence"
    - "User can run /seraphim:do with freeform text and get matched to the right command"
    - "User can run /seraphim:progress and see phase completion table with next action suggestion"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/next.md"
      provides: "State-machine navigation to next logical command"
    - path: "~/.claude/plugins/seraphim/commands/do.md"
      provides: "Freeform text to command router"
    - path: "~/.claude/plugins/seraphim/commands/progress.md"
      provides: "Phase progress table with completion bars and next action"
  key_links:
    - from: "next.md"
      to: "STATE.md + ROADMAP.md"
      via: "reads phase number and checks artifact presence"
      pattern: "CONTEXT.*PLAN.*SUMMARY.*VERIFICATION"
    - from: "progress.md"
      to: "ROADMAP.md"
      via: "reads phase list and calculates completion %"
      pattern: "ROADMAP"
---

<objective>
Build navigation and routing commands: next (state-machine routing), do (freeform text router), and progress (completion overview).

Purpose: Users can navigate their workflow without remembering which command comes next. Smart routing reduces cognitive load.
Output: next.md + do.md + progress.md commands
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-research-session-navigation/34-CONTEXT.md
@.planning/phases/34-research-session-navigation/34-RESEARCH.md

@~/.claude/plugins/seraphim/commands/discuss.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create next.md state-machine navigation command</name>
  <files>~/.claude/plugins/seraphim/commands/next.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/discuss.md (project root resolution, command structure)
    - .planning/STATE.md (understand phase field in frontmatter)
    - .planning/ROADMAP.md (understand phase directory naming convention)
  </read_first>
  <action>
**next.md** (per D-08, NAV-01):
- Frontmatter: `description: "Auto-advance to the next logical workflow step"`, no arguments, `allowed-tools: ["Read", "Bash"]`
- Step 1: Resolve project root
- Step 2: Read STATE.md — extract `phase:` from frontmatter (just the number, e.g., `34`)
- Step 3: Discover phase directory — glob `.planning/phases/{phase}-*/` and take first match (per pitfall 4 in RESEARCH.md — dynamic discovery, not hardcoded)
- Step 4: Check artifact presence in order (per D-08 decision table):
  1. Check for `*-CONTEXT.md` in phase dir — if missing, suggest `/seraphim:discuss {phase}`
  2. Check for `*-PLAN.md` — if missing, suggest `/seraphim:plan {phase}`
  3. Check for `*-SUMMARY.md` — if missing, suggest `/seraphim:execute {phase}`
  4. Check for `*-VERIFICATION.md` — if missing, suggest `/seraphim:verify {phase}`
  5. If ALL present — increment phase number, check if next phase directory exists, repeat. If no more phases, suggest "Milestone complete — run /seraphim:progress"
- Step 5: Display the suggestion clearly:
  ```
  Next step: /seraphim:discuss 34
  Reason: Phase 34 has no CONTEXT.md — discussion needed before planning.
  ```
- Do NOT auto-execute. Only suggest.

Use `node -e` with `fs.readdirSync` and glob matching for artifact checks. Read STATE.md frontmatter with a simple regex or the parseFrontmatter pattern from Phase 33.
  </action>
  <verify>
    <automated>grep -q "CONTEXT" ~/.claude/plugins/seraphim/commands/next.md && grep -q "PLAN" ~/.claude/plugins/seraphim/commands/next.md && grep -q "SUMMARY" ~/.claude/plugins/seraphim/commands/next.md && grep -q "VERIFICATION" ~/.claude/plugins/seraphim/commands/next.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/next.md
    - grep -q "CONTEXT" ~/.claude/plugins/seraphim/commands/next.md
    - grep -q "PLAN" ~/.claude/plugins/seraphim/commands/next.md
    - grep -q "VERIFICATION" ~/.claude/plugins/seraphim/commands/next.md
    - grep -q "discuss" ~/.claude/plugins/seraphim/commands/next.md
    - grep -q "execute" ~/.claude/plugins/seraphim/commands/next.md
  </acceptance_criteria>
  <done>next.md reads STATE.md + phase directory, checks artifact presence in order, suggests the correct next command without auto-executing</done>
</task>

<task type="auto">
  <name>Task 2: Create do.md keyword router and progress.md overview</name>
  <files>~/.claude/plugins/seraphim/commands/do.md, ~/.claude/plugins/seraphim/commands/progress.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/discuss.md (command structure pattern)
    - .planning/ROADMAP.md (phase list format for progress parsing)
  </read_first>
  <action>
**do.md** (per D-09, NAV-02):
- Frontmatter: `description: "Route freeform text to the right Seraphim command"`, `argument-hint: "<what you want to do>"`, `allowed-tools: ["Read", "Bash"]`
- Step 1: Parse the freeform text from `$ARGUMENTS`
- Step 2: Apply keyword map (per D-09):
  ```
  plan, planning, roadmap, design       -> /seraphim:plan
  execute, build, implement, code, run  -> /seraphim:execute
  debug, fix, broken, error, failing    -> /seraphim:debug
  research, investigate, explore, learn -> /seraphim:research-scope
  seed, idea, capture, note             -> /seraphim:seed
  progress, status, where, overview     -> /seraphim:progress
  pause, stop, save, break              -> /seraphim:pause
  resume, continue, pick up             -> /seraphim:resume
  discuss, decide, approach             -> /seraphim:discuss
  require, requirement, spec            -> /seraphim:requirements
  next, what now, advance               -> /seraphim:next
  ```
- Step 3: If single clear match, present it and ask user to confirm. If multiple matches (ambiguous), present top 3 as numbered options. If no match, say "I couldn't match that to a command. Try /seraphim:help for available commands."
- Step 4: NEVER auto-execute. Always present suggestion and let user confirm.

**progress.md** (per D-10, NAV-03):
- Frontmatter: `description: "Show project progress and suggest next action"`, no arguments, `allowed-tools: ["Read", "Bash"]`
- Step 1: Resolve project root
- Step 2: Read ROADMAP.md — parse phases from `## Phase Details` section. For each phase, extract name, plan count, completion status.
- Step 3: Read `.planning/phases/` directory — for each phase, count PLAN.md files and SUMMARY.md files. Completion % = summaries / plans * 100.
- Step 4: Display progress table:
  ```
  Phase  | Name                            | Progress
  -------|--------------------------------|----------
  32     | Foundations                     | [==========] 100% (3/3)
  33     | Core Command Layer              | [==========] 100% (5/5)
  34     | Research + Session + Navigation | [===       ]  30% (1/3)
  35     | Phase Management + Config       | [          ]   0% (0/?)
  ```
- Step 5: Show overall milestone progress (completed phases / total phases)
- Step 6: Suggest next action using same logic as next.md (check artifact presence for current phase)
  </action>
  <verify>
    <automated>grep -q "keyword" ~/.claude/plugins/seraphim/commands/do.md && grep -q "ROADMAP" ~/.claude/plugins/seraphim/commands/progress.md && grep -q "description:" ~/.claude/plugins/seraphim/commands/do.md && grep -q "description:" ~/.claude/plugins/seraphim/commands/progress.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/do.md
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/progress.md
    - grep -q "plan" ~/.claude/plugins/seraphim/commands/do.md
    - grep -q "execute" ~/.claude/plugins/seraphim/commands/do.md
    - grep -q "debug" ~/.claude/plugins/seraphim/commands/do.md
    - grep -q "ROADMAP" ~/.claude/plugins/seraphim/commands/progress.md
    - grep -q "phases" ~/.claude/plugins/seraphim/commands/progress.md
  </acceptance_criteria>
  <done>do.md routes freeform text via keyword matching with confirmation (never auto-executes); progress.md shows phase completion table with progress bars and next action suggestion</done>
</task>

</tasks>

<verification>
1. next.md checks artifacts in correct order: CONTEXT -> PLAN -> SUMMARY -> VERIFICATION
2. do.md has keyword map covering all major commands and never auto-executes
3. progress.md reads ROADMAP.md and calculates completion percentages
4. All three commands have valid YAML frontmatter
</verification>

<success_criteria>
- next.md routes to correct command based on artifact presence per D-08
- do.md matches freeform input to commands with user confirmation per D-09
- progress.md shows completion table with bars per D-10
- No command auto-executes — all suggest and wait for user
</success_criteria>

<output>
After completion, create `.planning/phases/34-research-session-navigation/34-03-SUMMARY.md`
</output>
