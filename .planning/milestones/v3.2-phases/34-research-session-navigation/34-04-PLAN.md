---
phase: 34-research-session-navigation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/map-codebase.md
autonomous: true
requirements: [RSRCH-05]

must_haves:
  truths:
    - "User can run /seraphim:map-codebase and receive a structured codebase analysis in .planning/codebase/"
    - "4 parallel mapper agents each write a focused analysis file"
    - "An index.md summary links to all four analysis files"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/map-codebase.md"
      provides: "Parallel codebase mapping command"
  key_links:
    - from: "map-codebase.md"
      to: ".planning/codebase/"
      via: "4 parallel subagents writing focused analysis files"
      pattern: "structure\\.md.*conventions\\.md.*stack\\.md.*concerns\\.md"
---

<objective>
Build the codebase mapping command that spawns parallel mapper agents.

Purpose: Users can analyze their codebase structure in one command, producing a structured map that other commands (research, planning) can reference.
Output: map-codebase.md command
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-research-session-navigation/34-CONTEXT.md
@.planning/phases/34-research-session-navigation/34-RESEARCH.md

@~/.claude/plugins/seraphim/commands/execute.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create map-codebase.md with parallel mapper agents</name>
  <files>~/.claude/plugins/seraphim/commands/map-codebase.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/execute.md (parallel subagent dispatch pattern)
    - ~/.claude/plugins/seraphim/commands/discuss.md (command structure, project root resolution)
  </read_first>
  <action>
**map-codebase.md** (per D-03, RSRCH-05):
- Frontmatter: `description: "Analyze codebase structure with parallel mapper agents"`, no required arguments, `allowed-tools: ["Read", "Write", "Bash", "Glob", "Grep"]`
- Step 1: Resolve project root
- Step 2: Check if `.planning/codebase/` already exists — if so, warn user and ask to confirm overwrite (anti-pattern from RESEARCH.md)
- Step 3: Create `.planning/codebase/` directory
- Step 4: Dispatch 4 parallel subagents (per D-03). Each agent gets a focused task and writes to a specific file. Use the Task() parallel dispatch pattern from execute.md:

  **Agent 1 — Structure**: Analyze directory layout, entry points, module organization. Write to `.planning/codebase/structure.md`. Include: top-level dirs, key files, import graph summary, entry points.

  **Agent 2 — Conventions**: Analyze coding patterns, naming conventions, comment style, error handling patterns. Write to `.planning/codebase/conventions.md`. Include: naming patterns, file organization, code style, common patterns.

  **Agent 3 — Stack**: Analyze technology stack, dependencies, versions, configuration. Write to `.planning/codebase/stack.md`. Include: languages, frameworks, package.json deps, config files, build tools.

  **Agent 4 — Concerns**: Analyze coupling, tech debt, risks, complexity hotspots. Write to `.planning/codebase/concerns.md`. Include: high-coupling areas, duplicated code, missing tests, complexity metrics.

- Step 5: After all 4 agents complete, write `.planning/codebase/index.md` — a summary document that links to all four files with a one-paragraph synopsis of each.
- Step 6: Display completion summary with file paths and key findings from each agent.

Each agent should use Read, Glob, Grep tools to analyze the codebase. Agents should focus on their assigned area and keep output under 200 lines.
  </action>
  <verify>
    <automated>grep -q "structure.md" ~/.claude/plugins/seraphim/commands/map-codebase.md && grep -q "conventions.md" ~/.claude/plugins/seraphim/commands/map-codebase.md && grep -q "stack.md" ~/.claude/plugins/seraphim/commands/map-codebase.md && grep -q "concerns.md" ~/.claude/plugins/seraphim/commands/map-codebase.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "description:" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "structure.md" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "conventions.md" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "stack.md" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "concerns.md" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "index.md" ~/.claude/plugins/seraphim/commands/map-codebase.md
    - grep -q "parallel" ~/.claude/plugins/seraphim/commands/map-codebase.md
  </acceptance_criteria>
  <done>map-codebase.md dispatches 4 parallel mapper agents that analyze structure/conventions/stack/concerns and produce .planning/codebase/ output with index</done>
</task>

</tasks>

<verification>
1. map-codebase.md references all 4 output files (structure, conventions, stack, concerns)
2. Command has valid YAML frontmatter with description
3. Parallel dispatch pattern is used (not sequential)
4. Existing codebase check prevents silent overwrite
</verification>

<success_criteria>
- map-codebase.md spawns 4 parallel mapper agents per D-03
- Each agent writes to a dedicated file in .planning/codebase/
- An index.md summary is generated after all agents complete
- Existing analysis check prevents accidental overwrite
</success_criteria>

<output>
After completion, create `.planning/phases/34-research-session-navigation/34-04-SUMMARY.md`
</output>
