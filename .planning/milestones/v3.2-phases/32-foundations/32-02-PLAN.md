---
phase: 32-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/lib/types.ts
  - dashboard/app/api/ingest/route.ts
  - ~/.claude/plugins/seraphim/commands/crucible.md
  - ~/.claude/plugins/seraphim/commands/judge.md
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "Decisions pushed to Neon include feature_id with a feat-NNN value when a feature is active"
    - "Crucible and judge commands pass the actual roadmap feature ID, not the pipeline phase string"
    - "Ingest route INSERT into decisions includes feature_id column and value"
  artifacts:
    - path: "dashboard/lib/types.ts"
      provides: "Decision interface with feature_id field"
      contains: "feature_id"
    - path: "dashboard/app/api/ingest/route.ts"
      provides: "Decisions INSERT with feature_id column"
      contains: "feature_id"
    - path: "~/.claude/plugins/seraphim/commands/crucible.md"
      provides: "Fixed feature_id source using readRoadmap"
      contains: "readRoadmap"
    - path: "~/.claude/plugins/seraphim/commands/judge.md"
      provides: "Fixed feature_id source using readRoadmap"
      contains: "readRoadmap"
  key_links:
    - from: "crucible.md"
      to: "roadmap.js readRoadmap()"
      via: "require and iterate milestones for in-progress feature"
      pattern: "readRoadmap"
    - from: "dashboard/app/api/ingest/route.ts"
      to: "Neon decisions table"
      via: "INSERT with feature_id column"
      pattern: "feature_id"
---

<objective>
Wire feature_id through the decisions pipeline so that every decision logged to Neon carries the actual roadmap feature ID (feat-NNN), not the pipeline phase string.

Purpose: FOUND-03 requires feature_id to flow through decisions-logger to Neon. Currently the ingest route omits feature_id entirely, and crucible/judge pass the wrong value (pipeline phase string instead of roadmap feature ID).
Output: Updated types, ingest route, and two command files with correct feature_id wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-foundations/32-CONTEXT.md
@.planning/phases/32-foundations/32-RESEARCH.md

@dashboard/lib/types.ts
@dashboard/app/api/ingest/route.ts
@~/.claude/plugins/seraphim/commands/crucible.md
@~/.claude/plugins/seraphim/commands/judge.md
@~/.claude/plugins/seraphim/lib/roadmap.js
@~/.claude/plugins/seraphim/lib/decisions-logger.js

<interfaces>
From ~/.claude/plugins/seraphim/lib/roadmap.js:
```javascript
function readRoadmap(projectRoot) // returns roadmap object with milestones[].features[]
// feature.status === 'in-progress' is the canonical active status (line 89)
```

From ~/.claude/plugins/seraphim/lib/decisions-logger.js:
```javascript
function buildRecord({ ..., feature_id = null }) // line 19 — already accepts feature_id with null default
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feature_id to Decision type and ingest route</name>
  <files>dashboard/lib/types.ts, dashboard/app/api/ingest/route.ts</files>
  <read_first>
    - dashboard/lib/types.ts (current Decision interface — confirm feature_id is missing)
    - dashboard/app/api/ingest/route.ts (current decisions INSERT — confirm feature_id omitted from column list and VALUES)
  </read_first>
  <action>
    Per D-07 and D-08, add feature_id to the decisions data path.

    **types.ts** — Add `feature_id?: string | null;` to the `Decision` interface, after the `loop_count` field (before `quality_signals`).

    **ingest/route.ts** — Find the decisions INSERT statement (approx lines 36-56). Add `feature_id` to:
    1. The column list: add `feature_id` after `quality_signals` (or after `loop_count`)
    2. The VALUES list: add `${d.feature_id ?? null}` in the matching position

    The Decision type in IngestPayload's `decisions` array will pick up the new field automatically since IngestPayload references Decision[].
  </action>
  <verify>
    <automated>grep -n "feature_id" dashboard/lib/types.ts dashboard/app/api/ingest/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "feature_id" dashboard/lib/types.ts` returns at least one line containing `feature_id?: string | null`
    - `grep "feature_id" dashboard/app/api/ingest/route.ts` returns at least two lines (one in column list, one in VALUES)
  </acceptance_criteria>
  <done>Decision type has feature_id field. Ingest route INSERT into decisions includes feature_id in both column list and VALUES.</done>
</task>

<task type="auto">
  <name>Task 2: Fix feature_id source in crucible.md and judge.md</name>
  <files>~/.claude/plugins/seraphim/commands/crucible.md, ~/.claude/plugins/seraphim/commands/judge.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/crucible.md (lines 300-350 — find the buildRecord call with feature_id: phaseId)
    - ~/.claude/plugins/seraphim/commands/judge.md (lines 220-270 — find the buildRecord call with feature_id: phaseId)
    - ~/.claude/plugins/seraphim/lib/roadmap.js (readRoadmap function signature and export)
  </read_first>
  <action>
    Per D-07 and D-08, replace the wrong `feature_id: phaseId` with the actual active feature ID from roadmap.json.

    **In both crucible.md (line ~336) and judge.md (line ~255):**

    1. Find the code block that calls `buildRecord()`. Before that call, add this snippet to resolve the active feature ID:
    ```javascript
    const { readRoadmap } = require(`${process.env.CLAUDE_PLUGIN_ROOT || '~/.claude/plugins/seraphim'}/lib/roadmap.js`);
    let activeFeatureId = null;
    try {
      const roadmap = readRoadmap(projectRoot);
      if (roadmap && roadmap.milestones) {
        for (const ms of roadmap.milestones) {
          for (const feat of (ms.features || [])) {
            if (feat.status === 'in-progress') {
              activeFeatureId = feat.id;
              break;
            }
          }
          if (activeFeatureId) break;
        }
      }
    } catch (_) { /* roadmap unavailable — keep null */ }
    ```

    2. In the `buildRecord()` call, change `feature_id: phaseId` to `feature_id: activeFeatureId`.

    IMPORTANT: `phaseId` is a pipeline phase string like "crucible" or "forge" — it is NOT a feature ID. The roadmap's in-progress feature (e.g., "feat-003") is the correct value. Per Pitfall 3 from research, confirm the status check uses `=== 'in-progress'` which matches roadmap.js line 89.

    If `projectRoot` is not already in scope at the logging block, look for how it's derived earlier in the file (likely from a working directory or config read) and use the same variable.
  </action>
  <verify>
    <automated>grep -n "feature_id:" ~/.claude/plugins/seraphim/commands/crucible.md ~/.claude/plugins/seraphim/commands/judge.md | grep -v "phaseId"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "feature_id: phaseId" ~/.claude/plugins/seraphim/commands/crucible.md` returns 0 matches
    - `grep "feature_id: phaseId" ~/.claude/plugins/seraphim/commands/judge.md` returns 0 matches
    - `grep "activeFeatureId" ~/.claude/plugins/seraphim/commands/crucible.md` returns at least 1 match
    - `grep "activeFeatureId" ~/.claude/plugins/seraphim/commands/judge.md` returns at least 1 match
    - `grep "readRoadmap" ~/.claude/plugins/seraphim/commands/crucible.md` returns at least 1 match
    - `grep "readRoadmap" ~/.claude/plugins/seraphim/commands/judge.md` returns at least 1 match
  </acceptance_criteria>
  <done>Both crucible.md and judge.md pass the actual roadmap feature ID (feat-NNN or null) instead of pipeline phase string. readRoadmap is imported and active feature is resolved via in-progress status check.</done>
</task>

</tasks>

<verification>
- `grep -c "feature_id: phaseId" ~/.claude/plugins/seraphim/commands/crucible.md ~/.claude/plugins/seraphim/commands/judge.md` returns 0 for both files
- `grep "feature_id" dashboard/app/api/ingest/route.ts` shows feature_id in the decisions INSERT
- `grep "feature_id" dashboard/lib/types.ts` shows the field in the Decision interface
</verification>

<success_criteria>
1. Decision type includes feature_id field (Pitfall 4 avoided)
2. Ingest route decisions INSERT includes feature_id column and value
3. Crucible and judge pass activeFeatureId from readRoadmap, not phaseId
4. All feature_id lookups use status === 'in-progress' check
</success_criteria>

<output>
After completion, create `.planning/phases/32-foundations/32-02-SUMMARY.md`
</output>
