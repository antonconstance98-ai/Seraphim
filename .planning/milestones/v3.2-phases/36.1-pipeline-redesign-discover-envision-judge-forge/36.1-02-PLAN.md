---
phase: 36.1-pipeline-redesign-discover-envision-judge-forge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/plugins/seraphim/commands/judge.md
  - ~/.claude/plugins/seraphim/commands/architect.md
autonomous: true
requirements: [PIPE-05, PIPE-06, PIPE-07, PIPE-08]

must_haves:
  truths:
    - "Judge accepts --mode vision for light collaborative review of vision.md"
    - "Judge accepts --mode architecture for adversarial stress-test of architect plans"
    - "Judge Pass 1 updates vision.md in-place with approved tweaks"
    - "Judge Pass 2 produces blueprint.md with precise execution spec"
    - "Judge Pass 2 emits loop_required marker when sending back to Architect"
    - "Architect reads vision.md (not judgment.md) as input"
    - "Architect produces 2-4 technical plans with SERAPHIM:PLAN markers"
  artifacts:
    - path: "~/.claude/plugins/seraphim/commands/judge.md"
      provides: "Two-pass judge with --mode flag"
      contains: "--mode"
    - path: "~/.claude/plugins/seraphim/commands/architect.md"
      provides: "Technical plan generation from vision.md"
      contains: "vision.md"
  key_links:
    - from: "architect.md"
      to: "vision.md"
      via: "reads as input spec"
      pattern: "vision.md"
    - from: "judge.md"
      to: "blueprint.md"
      via: "writes in architecture mode"
      pattern: "blueprint.md"
    - from: "judge.md"
      to: "loop_required"
      via: "marker signal to run.md for Architect rework"
      pattern: "loop_required"
---

<objective>
Rewrite Judge and Architect command files for the two-pass pipeline.

Purpose: Judge splits into two personalities — light vision review (Pass 1) and adversarial architecture stress-test (Pass 2). Architect now reads vision.md (not judgment.md) and produces multiple technical plans for Judge Pass 2 to evaluate.

Output: Two rewritten .md command files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36.1-pipeline-redesign-discover-envision-judge-forge/36.1-CONTEXT.md
@.planning/phases/36.1-pipeline-redesign-discover-envision-judge-forge/36.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite judge.md — two-pass with --mode flag</name>
  <files>~/.claude/plugins/seraphim/commands/judge.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/judge.md (current implementation — frontmatter, argument parsing, APPROACH verdict system, marker patterns)
    - ~/.claude/plugins/seraphim/lib/markers.js (emitMarker, parseMarkers — for PLAN marker parsing and PHASE_COMPLETE emission)
    - ~/.claude/plugins/seraphim/lib/phase-state.js (readState, writeState, incrementLoop)
    - ~/.claude/plugins/seraphim/executors/dispatch.js (resolveExecutorId for judge slot)
  </read_first>
  <action>
Extend judge.md with --mode flag support. Keep frontmatter, replace body.

**Step 1 — Parse arguments with --mode extraction.**
Extract phase-id and --mode flag from $ARGUMENTS. Use the pattern from RESEARCH.md:
```javascript
const args = '$ARGUMENTS'.split(/\s+/);
const phaseId = args.find(a => !a.startsWith('--'));
const modeArg = args.findIndex(a => a === '--mode');
const mode = modeArg !== -1 ? args[modeArg + 1] : 'architecture'; // default backward compat
```
Validate mode is 'vision' or 'architecture'. Abort with usage if invalid.

**Step 2 — Render banner.**
`renderWingBanner('judge', phaseId)` — include mode in banner output: "Judge (Vision Review)" or "Judge (Architecture Review)".

**Step 3 — Branch by mode.**

**Mode: vision (per D-13, D-14, D-15, D-16)**

a) Read `.seraphim/phases/{phaseId}/vision.md`. Abort if missing.

b) Analyze vision.md with a LIGHT, COLLABORATIVE tone (per D-14). Surface potential tweaks:
   - Gaps in logic
   - Unrealistic assumptions
   - Missing constraints
   - Internal contradictions
   This is NOT adversarial. The Judge is helping refine, like a trusted advisor saying "have you considered..."

c) Present each suggested tweak to the human individually (per D-15). For each tweak:
   - Show the tweak and reasoning
   - Human approves or disapproves
   - Track approved/rejected counts

d) Rewrite vision.md with ALL approved tweaks applied in-place (per D-16). The vision is now final.

e) Append PHASE_COMPLETE marker at end of vision.md:
   `<!-- SERAPHIM:PHASE_COMPLETE phase="judge" mode="vision" approved_tweaks="N" rejected_tweaks="N" -->`
   This marker is what run.md checks for the autonomous lockout (per D-30).

f) Update phase state: status="complete" for judge-vision pass.

**Mode: architecture (per D-21, D-22, D-23, D-24, D-25)**

a) Read `.seraphim/phases/{phaseId}/architect-plans.md`. Abort if missing: "Run /seraphim:architect first."

b) Parse SERAPHIM:PLAN markers from architect-plans.md using `parseMarkers()`.

c) ADVERSARIAL stress-test each plan (per D-21). For each plan, evaluate:
   - Feasibility: Can this actually be built with the stated constraints?
   - Scalability: Will this approach hold as the project grows?
   - Maintainability: How hard is this to change later?
   - Risk: What can go wrong? What's the blast radius?

d) Make a decision (per D-22):
   - (a) Pick the best winner outright
   - (b) Merge winning aspects from multiple plans
   - (c) Send back to Architect with harder rules/parameters — emit `loop_required="true"` marker. MAX 1 loop (managed by run.md, not judge.md per D-35).

e) If decision is (a) or (b): Write `blueprint.md` (per D-23, D-24, D-25).
   Blueprint format:
   ```markdown
   <!-- SERAPHIM:PHASE_START phase="judge" mode="architecture" -->

   # Blueprint: {project name}

   ## Technical Architecture
   {Selected/merged architecture with every decision locked}

   ## Tech Stack
   {Exact libraries, versions, justifications}

   ## Component Structure
   {Every component, its responsibility, its interfaces}

   ## Data Model
   {Schema, relationships, constraints}

   ## Build Phases
   {GSD-style phases with clear boundaries, dependencies, success criteria}
   Each phase has:
   - Phase name and goal
   - Dependencies on prior phases
   - Files to create/modify
   - Success criteria
   - Estimated complexity

   <!-- SERAPHIM:PHASE_COMPLETE phase="judge" mode="architecture" verdict="{winner|merged|rework}" loop_required="{true|false}" -->
   ```

   Per D-24: blueprint.md must be SO precise that a local LLM can follow it blindly. Every decision locked, every file path named, every interface defined.

f) If decision is (c): Write a rework directive instead of blueprint.md. Emit:
   `<!-- SERAPHIM:PHASE_COMPLETE phase="judge" mode="architecture" verdict="rework" loop_required="true" rework_instructions="..." -->`
   run.md reads this and re-invokes architect.md.

**Key differences from old judge.md:**
- No APPROACH verdict system (SURVIVES/FATAL_FLAW/CONDITIONAL) — replaced by plan stress-testing
- No judgment.md output — vision mode updates vision.md in-place, architecture mode writes blueprint.md
- Two completely different behaviors based on --mode
- Architecture mode produces blueprint.md directly (Judge is now the blueprint author, not Architect)

**Preserve from old judge.md:**
- Frontmatter format
- Banner rendering
- PHASE_START/PHASE_COMPLETE marker wrapping
  </action>
  <verify>
    <automated>grep -c "\-\-mode" ~/.claude/plugins/seraphim/commands/judge.md && grep -c "vision" ~/.claude/plugins/seraphim/commands/judge.md && grep -c "architecture" ~/.claude/plugins/seraphim/commands/judge.md && grep -c "blueprint.md" ~/.claude/plugins/seraphim/commands/judge.md && grep -c "loop_required" ~/.claude/plugins/seraphim/commands/judge.md</automated>
  </verify>
  <acceptance_criteria>
    - judge.md contains `--mode` argument parsing with 'vision' and 'architecture' values
    - judge.md contains `mode = modeArg !== -1 ? args[modeArg + 1] : 'architecture'` (default backward compat)
    - judge.md contains "vision.md" reference for Pass 1 input/output
    - judge.md contains "blueprint.md" reference for Pass 2 output
    - judge.md contains "architect-plans.md" reference for Pass 2 input
    - judge.md contains `loop_required` marker attribute for rework signal
    - judge.md contains `PHASE_COMPLETE phase="judge" mode="vision"` marker template
    - judge.md contains `PHASE_COMPLETE phase="judge" mode="architecture"` marker template
    - judge.md does NOT contain `judgment.md` as an output file
    - judge.md does NOT contain `SURVIVES` or `FATAL_FLAW` verdict language
  </acceptance_criteria>
  <done>judge.md supports two modes: --mode vision (light collaborative review, updates vision.md in-place) and --mode architecture (adversarial stress-test, produces blueprint.md or rework signal)</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite architect.md — reads vision.md, produces N technical plans</name>
  <files>~/.claude/plugins/seraphim/commands/architect.md</files>
  <read_first>
    - ~/.claude/plugins/seraphim/commands/architect.md (current implementation — frontmatter, blueprint generation, judgment.md reading pattern)
    - ~/.claude/plugins/seraphim/lib/markers.js (emitMarker for SERAPHIM:PLAN markers)
    - ~/.claude/plugins/seraphim/lib/phase-state.js (readState, writeState)
  </read_first>
  <action>
Rewrite architect.md. Keep frontmatter, replace body.

**Step 1 — Parse arguments, read state.**
Extract phase-id from $ARGUMENTS.

**Step 2 — Render banner.**
`renderWingBanner('architect', phaseId)`.

**Step 3 — Read vision.md (per D-17 — NOT judgment.md).**
Read `.seraphim/phases/{phaseId}/vision.md`. Abort if missing: "Run /seraphim:envision and /seraphim:judge --mode vision first."
Verify vision.md has the Judge-approved PHASE_COMPLETE marker (mode="vision"). If not, warn: "Vision has not been Judge-reviewed. Consider running /seraphim:judge --mode vision first."

**Step 4 — Assess complexity and determine plan count (per D-19).**
AI evaluates the vision's complexity:
- Simple projects (single feature, clear tech stack, few components): 2 plans
- Medium projects (multiple features, some integration, standard patterns): 3 plans (default)
- Complex projects (multi-system, novel architecture, significant integration): 4 plans

State the assessed complexity and plan count to the human.

**Step 5 — Generate N technical plans (per D-18, D-20).**
For each plan, create a distinct technical approach to achieve the SAME vision. Each plan covers:
- Architecture (monolith vs microservices, component boundaries, data flow)
- Tech stack choices (languages, frameworks, databases, tools)
- Component structure (what components exist, their responsibilities)
- Data model (schema, relationships)
- Implementation strategy (build order, risk mitigation)

Plans must be genuinely different approaches — not trivial variations. Same goal, different technical paths.

**Step 6 — Write architect-plans.md.**
Output to `.seraphim/phases/{phaseId}/architect-plans.md`:

```markdown
<!-- SERAPHIM:PHASE_START phase="architect" plan_count="N" complexity="medium" -->

# Technical Plans: {project name}

## Plan 1: {approach name}
<!-- SERAPHIM:PLAN id="plan-1" approach_name="{name}" -->

### Architecture
{...}

### Tech Stack
{...}

### Component Structure
{...}

### Data Model
{...}

### Implementation Strategy
{...}

<!-- SERAPHIM:PLAN_COMPLETE id="plan-1" -->

## Plan 2: {approach name}
<!-- SERAPHIM:PLAN id="plan-2" approach_name="{name}" -->
{...same structure...}
<!-- SERAPHIM:PLAN_COMPLETE id="plan-2" -->

{...repeat for N plans...}

<!-- SERAPHIM:PHASE_COMPLETE phase="architect" plan_count="N" -->
```

Use SERAPHIM:PLAN markers (not APPROACH markers — per Research anti-pattern note). This lets Judge Pass 2 parse them with existing parseMarkers().

**Step 7 — Update phase state.**

**Key differences from old architect.md:**
- Reads vision.md, NOT judgment.md (per D-17)
- Produces architect-plans.md with multiple plans, NOT blueprint.md (per D-18)
- Blueprint.md is now produced by Judge Pass 2, not Architect
- Uses SERAPHIM:PLAN markers, not SERAPHIM:APPROACH markers
- Plan count is dynamic (2-4) based on complexity assessment (per D-19)

**Preserve from old architect.md:**
- Frontmatter format
- Banner rendering
- PHASE_START/PHASE_COMPLETE markers
  </action>
  <verify>
    <automated>grep -c "vision.md" ~/.claude/plugins/seraphim/commands/architect.md && grep -c "architect-plans.md" ~/.claude/plugins/seraphim/commands/architect.md && grep -c "SERAPHIM:PLAN" ~/.claude/plugins/seraphim/commands/architect.md && grep -c "judgment.md" ~/.claude/plugins/seraphim/commands/architect.md | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - architect.md reads "vision.md" as input (not "judgment.md")
    - architect.md writes "architect-plans.md" as output (not "blueprint.md")
    - architect.md contains `SERAPHIM:PLAN id=` marker template (not APPROACH)
    - architect.md does NOT reference `judgment.md` as input
    - architect.md contains complexity assessment logic (simple=2, medium=3, complex=4)
    - architect.md contains `PLAN_COMPLETE` marker template
    - architect.md contains "Architecture" and "Tech Stack" and "Data Model" section templates
  </acceptance_criteria>
  <done>architect.md reads the Judge-approved vision.md, assesses complexity, generates 2-4 distinct technical plans with SERAPHIM:PLAN markers, and writes architect-plans.md for Judge Pass 2</done>
</task>

</tasks>

<verification>
- judge.md implements two distinct behaviors via --mode flag
- architect.md reads vision.md, not judgment.md
- architect.md produces architect-plans.md, not blueprint.md
- Judge architecture mode produces blueprint.md
- PLAN markers used (not APPROACH markers)
- loop_required signal emitted by Judge for run.md to read
</verification>

<success_criteria>
- judge.md supports --mode vision (light review, vision.md in-place update) and --mode architecture (adversarial, blueprint.md output)
- architect.md reads vision.md, produces 2-4 technical plans with SERAPHIM:PLAN markers
- No references to old judgment.md or APPROACH markers remain
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-pipeline-redesign-discover-envision-judge-forge/36.1-02-SUMMARY.md`
</output>
