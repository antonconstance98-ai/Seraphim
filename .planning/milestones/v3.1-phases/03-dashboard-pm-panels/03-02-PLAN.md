---
phase: 03-dashboard-pm-panels
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ~/.claude/plugins/seraphim/dashboard/components/MilestoneTree.tsx
  - ~/.claude/plugins/seraphim/dashboard/components/FeatureRow.tsx
  - ~/.claude/plugins/seraphim/dashboard/components/PipelineProgress.tsx
  - ~/.claude/plugins/seraphim/dashboard/components/HumanTasksByType.tsx
  - ~/.claude/plugins/seraphim/dashboard/components/TaskTypeGroup.tsx
  - ~/.claude/plugins/seraphim/dashboard/app/project/[name]/page.tsx
autonomous: true
requirements: [ROAD-06, TASK-06]
must_haves:
  truths:
    - "Plan tab shows milestone-feature tree with status dots and pipeline progress"
    - "Tasks tab shows human tasks grouped by type (decision, research, review, validation, skills)"
    - "Empty states render appropriate messages when no data exists"
  artifacts:
    - path: "~/.claude/plugins/seraphim/dashboard/components/MilestoneTree.tsx"
      provides: "Milestone-feature tree for Plan tab"
      contains: "MilestoneTree"
    - path: "~/.claude/plugins/seraphim/dashboard/components/FeatureRow.tsx"
      provides: "Feature row with status dot and pipeline progress"
      contains: "FeatureRow"
    - path: "~/.claude/plugins/seraphim/dashboard/components/PipelineProgress.tsx"
      provides: "6-dot pipeline phase indicator"
      contains: "PipelineProgress"
    - path: "~/.claude/plugins/seraphim/dashboard/components/HumanTasksByType.tsx"
      provides: "Tasks tab content grouped by type"
      contains: "HumanTasksByType"
    - path: "~/.claude/plugins/seraphim/dashboard/components/TaskTypeGroup.tsx"
      provides: "Single task type group section"
      contains: "TaskTypeGroup"
  key_links:
    - from: "app/project/[name]/page.tsx"
      to: "components/MilestoneTree.tsx"
      via: "import, rendered when tab === 'plan'"
      pattern: "tab === 'plan'.*MilestoneTree"
    - from: "app/project/[name]/page.tsx"
      to: "components/HumanTasksByType.tsx"
      via: "import, rendered when tab === 'tasks'"
      pattern: "tab === 'tasks'.*HumanTasksByType"
---

<objective>
Build Plan tab and Tasks tab content components, then wire them into the project detail page replacing the placeholders from Plan 01.

Purpose: ROAD-06 (roadmap panel) and TASK-06 (human tasks panel) are the two core PM panels on the project detail page.
Output: Working Plan and Tasks tabs with real Neon data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-pm-panels/03-CONTEXT.md
@.planning/phases/03-dashboard-pm-panels/03-RESEARCH.md
@.planning/phases/03-dashboard-pm-panels/03-UI-SPEC.md
@.planning/phases/03-dashboard-pm-panels/03-01-SUMMARY.md

@~/.claude/plugins/seraphim/dashboard/lib/types.ts
@~/.claude/plugins/seraphim/dashboard/lib/status-colors.ts
@~/.claude/plugins/seraphim/dashboard/lib/queries.ts
@~/.claude/plugins/seraphim/dashboard/app/project/[name]/page.tsx
@~/.claude/plugins/seraphim/dashboard/components/TaskList.tsx

<interfaces>
From lib/types.ts:
```typescript
export interface MilestoneSnapshot {
  version: string; name: string; status: string;
  feature_count: number; complete_count: number; cost_usd: number;
}
export interface FeatureSnapshot {
  feature_id: string; slug?: string; name: string; status: string;
  milestone_version: string; pipeline_phase?: string; cost_usd: number;
}
export interface HumanTaskSnapshot {
  task_id: string; type: string; status: string;
  feature_id?: string; urgency: string;
}
```

From lib/status-colors.ts (created in Plan 01):
```typescript
export const STATUS_COLORS: Record<string, { dot: string; badge: string }>;
// Keys: planned, in-progress, complete, blocked
```

From lib/queries.ts (created in Plan 01):
```typescript
export async function getMilestones(projectName: string): Promise<MilestoneSnapshot[]>;
export async function getFeatures(projectName: string): Promise<FeatureSnapshot[]>;
export async function getHumanTasks(projectName: string): Promise<HumanTaskSnapshot[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan tab components (MilestoneTree, FeatureRow, PipelineProgress)</name>
  <files>
    ~/.claude/plugins/seraphim/dashboard/components/MilestoneTree.tsx,
    ~/.claude/plugins/seraphim/dashboard/components/FeatureRow.tsx,
    ~/.claude/plugins/seraphim/dashboard/components/PipelineProgress.tsx
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/dashboard/lib/types.ts,
    ~/.claude/plugins/seraphim/dashboard/lib/status-colors.ts,
    .planning/phases/03-dashboard-pm-panels/03-UI-SPEC.md
  </read_first>
  <action>
All 3 components are Server Components (NO `use client` directive). Per D-02 and UI-SPEC.

1. **PipelineProgress.tsx** -- Shows which of the 6 pipeline phases are complete as mini dots.
   - Props: `{ pipelinePhase?: string }` where pipelinePhase is the current phase name (e.g. "architect")
   - Define PHASES = ['discover', 'envision', 'judge', 'architect', 'forge', 'crucible']
   - Find index of pipelinePhase in PHASES array. All phases before that index are complete.
   - If pipelinePhase is undefined or not found, show all dots as incomplete.
   - Render 6 inline dots: `<span className="flex gap-1">` containing 6 `<span>` elements
   - Complete dot: `w-1.5 h-1.5 rounded-full bg-indigo-500`
   - Incomplete dot: `w-1.5 h-1.5 rounded-full bg-white/20`
   - After dots, render phase count: `<span className="text-gray-500 text-xs ml-1">{completeCount}/6 phases</span>`

2. **FeatureRow.tsx** -- One row per feature inside a milestone.
   - Props: `{ feature: FeatureSnapshot }` (import type from `@/lib/types`)
   - Import STATUS_COLORS from `@/lib/status-colors`
   - Import PipelineProgress from `./PipelineProgress`
   - Container: `<div className="flex items-center gap-3 py-2 pl-4">`
   - Status dot: `<span className={`w-1.5 h-1.5 rounded-full ${colors.dot}`} aria-label={feature.status} />`
   - Feature name: `<span className="text-gray-300 text-sm">{feature.name}</span>`
   - Right side (ml-auto): `<PipelineProgress pipelinePhase={feature.pipeline_phase} />`
   - Look up colors via `STATUS_COLORS[feature.status] ?? STATUS_COLORS['planned']`

3. **MilestoneTree.tsx** -- Full Plan tab content per D-02.
   - Props: `{ milestones: MilestoneSnapshot[]; features: FeatureSnapshot[] }`
   - Import types from `@/lib/types`, STATUS_COLORS from `@/lib/status-colors`, FeatureRow from `./FeatureRow`
   - Empty state (milestones.length === 0): render `bg-[#1a1a1a] border border-white/10 rounded-xl p-6` card with heading "No roadmap yet" and body text per UI-SPEC copywriting contract
   - For each milestone: filter features by `milestone_version === m.version`
   - Milestone row: `<div className="flex items-center gap-3 py-3 border-b border-white/5 last:border-0">`
     - Status dot: `w-2 h-2 rounded-full ${colors.dot}` with `aria-label={m.status}`
     - Name: `text-white font-semibold text-sm`
     - Progress text (ml-auto): `text-gray-500 text-xs` showing `{m.complete_count}/{m.feature_count} features`
   - Below milestone row, render `<FeatureRow>` for each feature in that milestone
   - Outer container: `bg-[#1a1a1a] border border-white/10 rounded-xl p-6`
  </action>
  <verify>
    <automated>cd ~/.claude/plugins/seraphim/dashboard && grep -c "STATUS_COLORS" components/MilestoneTree.tsx && grep -c "FeatureRow" components/MilestoneTree.tsx && grep -c "PipelineProgress" components/FeatureRow.tsx && grep -c "bg-indigo-500" components/PipelineProgress.tsx && ! grep "use client" components/MilestoneTree.tsx components/FeatureRow.tsx components/PipelineProgress.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "export function MilestoneTree" components/MilestoneTree.tsx returns a match
    - grep "export function FeatureRow" components/FeatureRow.tsx returns a match
    - grep "export function PipelineProgress" components/PipelineProgress.tsx returns a match
    - grep "No roadmap yet" components/MilestoneTree.tsx returns a match (empty state)
    - grep "aria-label" components/MilestoneTree.tsx returns a match (accessibility)
    - grep "use client" components/MilestoneTree.tsx returns NO match (Server Component)
    - grep "use client" components/FeatureRow.tsx returns NO match (Server Component)
    - grep "STATUS_COLORS" components/FeatureRow.tsx returns a match
  </acceptance_criteria>
  <done>MilestoneTree renders milestone-feature tree with status dots and pipeline progress. FeatureRow shows individual features. PipelineProgress shows 6-dot phase indicator. All are Server Components. Empty state handled.</done>
</task>

<task type="auto">
  <name>Task 2: Tasks tab components + wire both tabs into project page</name>
  <files>
    ~/.claude/plugins/seraphim/dashboard/components/HumanTasksByType.tsx,
    ~/.claude/plugins/seraphim/dashboard/components/TaskTypeGroup.tsx,
    ~/.claude/plugins/seraphim/dashboard/app/project/[name]/page.tsx
  </files>
  <read_first>
    ~/.claude/plugins/seraphim/dashboard/components/TaskList.tsx,
    ~/.claude/plugins/seraphim/dashboard/app/project/[name]/page.tsx,
    ~/.claude/plugins/seraphim/dashboard/lib/types.ts,
    .planning/phases/03-dashboard-pm-panels/03-UI-SPEC.md
  </read_first>
  <action>
1. **TaskTypeGroup.tsx** -- Server Component. One section per task type.
   - Props: `{ type: string; tasks: HumanTaskSnapshot[] }`
   - Group header: `<h3 className="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2 mt-6">{type}</h3>`
   - For each task, render a card matching existing TaskList human variant style:
     - Container: `<div className="bg-[#242424] border border-amber-500/20 rounded-lg p-4 mb-2">`
     - Task ID: `<span className="text-amber-300 text-sm font-mono">{task.task_id}</span>`
     - Status badge: `<span className="text-xs bg-amber-500/20 text-amber-300 px-2 py-0.5 rounded-full ml-2">{task.status}</span>`
     - Urgency (if present): `<span className="text-xs text-gray-500 ml-2">{task.urgency}</span>`
     - Feature link (if feature_id): `<p className="text-gray-500 text-xs font-mono mt-1">Feature: {task.feature_id}</p>`

2. **HumanTasksByType.tsx** -- Server Component per D-03. Tasks tab content.
   - Props: `{ tasks: HumanTaskSnapshot[] }`
   - Import TaskTypeGroup from `./TaskTypeGroup`
   - Define TYPE_ORDER = ['decision', 'research', 'review', 'validation', 'skills']
   - Filter to pending tasks: `tasks.filter(t => t.status !== 'done')`
   - Empty state (no pending): render `<p className="text-gray-500 text-sm">No pending tasks. All human tasks have been completed or none have been created yet.</p>`
   - For each type in TYPE_ORDER, filter pending tasks by type, skip if 0 tasks, render `<TaskTypeGroup type={type} tasks={group} />`

3. **page.tsx** -- Replace Plan and Tasks tab placeholders with real components:
   - Add imports: `getMilestones`, `getFeatures`, `getHumanTasks` from `@/lib/queries`
   - Add imports: `MilestoneTree` from `@/components/MilestoneTree`, `HumanTasksByType` from `@/components/HumanTasksByType`
   - In the data fetch section (alongside existing getPhaseStates/getProjectDecisions), add getMilestones, getFeatures, getHumanTasks to the Promise.all:
     ```typescript
     const [phaseStates, decisions, milestones, features, pmTasks] = await Promise.all([
       getPhaseStates(projectName),
       getProjectDecisions(projectName),
       getMilestones(projectName),
       getFeatures(projectName),
       getHumanTasks(projectName),
     ]);
     ```
   - Replace plan placeholder with: `{tab === 'plan' && <MilestoneTree milestones={milestones} features={features} />}`
   - Replace tasks placeholder with: `{tab === 'tasks' && <HumanTasksByType tasks={pmTasks} />}`
   - Keep all existing metrics tab content unchanged
  </action>
  <verify>
    <automated>cd ~/.claude/plugins/seraphim/dashboard && grep -c "TaskTypeGroup" components/HumanTasksByType.tsx && grep -c "HumanTasksByType" app/project/\\[name\\]/page.tsx && grep -c "MilestoneTree" app/project/\\[name\\]/page.tsx && grep "getMilestones" app/project/\\[name\\]/page.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "export function HumanTasksByType" components/HumanTasksByType.tsx returns a match
    - grep "export function TaskTypeGroup" components/TaskTypeGroup.tsx returns a match
    - grep "No pending tasks" components/HumanTasksByType.tsx returns a match (empty state)
    - grep "use client" components/HumanTasksByType.tsx returns NO match (Server Component)
    - grep "MilestoneTree" app/project/[name]/page.tsx returns a match
    - grep "HumanTasksByType" app/project/[name]/page.tsx returns a match
    - grep "getMilestones" app/project/[name]/page.tsx returns a match
    - grep "getHumanTasks" app/project/[name]/page.tsx returns a match
    - Build passes with no errors
  </acceptance_criteria>
  <done>Plan tab shows milestone-feature tree from Neon data. Tasks tab shows human tasks grouped by type. Both tabs render real data via PM queries. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Plan tab (`?tab=plan`) renders MilestoneTree with status dots and pipeline progress
- Tasks tab (`?tab=tasks`) renders HumanTasksByType grouped by type
- Empty states show correct messages when no data exists
- All new components are Server Components (no `use client`)
</verification>

<success_criteria>
ROAD-06: Roadmap panel shows milestone-feature tree with visual status indicators. TASK-06: Human tasks panel shows tasks grouped by type with status and urgency. Both tabs render from Neon PM table data.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-pm-panels/03-02-SUMMARY.md`
</output>
